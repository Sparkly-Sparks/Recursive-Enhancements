SMODS.Atlas{ key = "enhancement", path = "Enhancements.png", px = 71, py = 95 }
SMODS.Enhancement { key = "bonusXmult", name = "Bonus Mult Card", atlas = "enhancement", pos = { x = 0, y = 3 }, config = { bonus = 30, mult = 4, }, in_pool = function(self,args) return false end, loc_vars = function(self, info_queue, card) return { vars = { card.ability.bonus, card.ability.mult } } end}
SMODS.Enhancement { key = "bonusXwild", name = "Bonus Wild Card", atlas = "enhancement", pos = { x = 0, y = 5 }, config = { bonus = 30, }, any_suit = true, in_pool = function(self,args) return false end, loc_vars = function(self, info_queue, card) return { vars = { card.ability.bonus } } end}
SMODS.Enhancement { key = "multXwild", name = "Mult Wild Card", atlas = "enhancement", pos = { x = 0, y = 6 }, config = { mult = 4, }, any_suit = true, in_pool = function(self,args) return false end, loc_vars = function(self, info_queue, card) return { vars = { card.ability.mult } } end}
SMODS.Enhancement { key = "bonusXmultXwild", name = "Bonus Mult Wild Card", atlas = "enhancement", pos = { x = 0, y = 7 }, config = { bonus = 30, mult = 4, }, any_suit = true, in_pool = function(self,args) return false end, loc_vars = function(self, info_queue, card) return { vars = { card.ability.bonus, card.ability.mult } } end}
SMODS.Enhancement { key = "bonusXglass", name = "Bonus Glass Card", atlas = "enhancement", pos = { x = 0, y = 9 }, config = { bonus = 30, Xmult = 2, extra = { odds = 4 } }, shatters = true, in_pool = function(self,args) return false end, loc_vars = function(self, info_queue, card) local numerator, denominator = SMODS.get_probability_vars(card, 1, card.ability.extra.odds, self.key) return { vars = { card.ability.bonus, card.ability.Xmult, numerator, denominator } } end, calculate = function(self, card, context)   if context.destroy_card and context.cardarea == G.play and context.destroy_card == card and SMODS.pseudorandom_probability(card, self.key, 1, card.ability.extra.odds) then card.glass_trigger = true return { remove = true } end end }
SMODS.Enhancement { key = "multXglass", name = "Mult Glass Card", atlas = "enhancement", pos = { x = 0, y = 10 }, config = { mult = 4, Xmult = 2, extra = { odds = 4 } }, shatters = true, in_pool = function(self,args) return false end, loc_vars = function(self, info_queue, card) local numerator, denominator = SMODS.get_probability_vars(card, 1, card.ability.extra.odds, self.key) return { vars = { card.ability.mult, card.ability.Xmult, numerator, denominator } } end, calculate = function(self, card, context)   if context.destroy_card and context.cardarea == G.play and context.destroy_card == card and SMODS.pseudorandom_probability(card, self.key, 1, card.ability.extra.odds) then card.glass_trigger = true return { remove = true } end end }
SMODS.Enhancement { key = "bonusXmultXglass", name = "Bonus Mult Glass Card", atlas = "enhancement", pos = { x = 0, y = 11 }, config = { bonus = 30, mult = 4, Xmult = 2, extra = { odds = 4 } }, shatters = true, in_pool = function(self,args) return false end, loc_vars = function(self, info_queue, card) local numerator, denominator = SMODS.get_probability_vars(card, 1, card.ability.extra.odds, self.key) return { vars = { card.ability.bonus, card.ability.mult, card.ability.Xmult, numerator, denominator } } end, calculate = function(self, card, context)   if context.destroy_card and context.cardarea == G.play and context.destroy_card == card and SMODS.pseudorandom_probability(card, self.key, 1, card.ability.extra.odds) then card.glass_trigger = true return { remove = true } end end }
SMODS.Enhancement { key = "wildXglass", name = "Wild Glass Card", atlas = "enhancement", pos = { x = 0, y = 12 }, config = { Xmult = 2, extra = { odds = 4 } }, any_suit = true, shatters = true, in_pool = function(self,args) return false end, loc_vars = function(self, info_queue, card) local numerator, denominator = SMODS.get_probability_vars(card, 1, card.ability.extra.odds, self.key) return { vars = { card.ability.Xmult, numerator, denominator } } end, calculate = function(self, card, context)   if context.destroy_card and context.cardarea == G.play and context.destroy_card == card and SMODS.pseudorandom_probability(card, self.key, 1, card.ability.extra.odds) then card.glass_trigger = true return { remove = true } end end }
SMODS.Enhancement { key = "bonusXwildXglass", name = "Bonus Wild Glass Card", atlas = "enhancement", pos = { x = 0, y = 13 }, config = { bonus = 30, Xmult = 2, extra = { odds = 4 } }, any_suit = true, shatters = true, in_pool = function(self,args) return false end, loc_vars = function(self, info_queue, card) local numerator, denominator = SMODS.get_probability_vars(card, 1, card.ability.extra.odds, self.key) return { vars = { card.ability.bonus, card.ability.Xmult, numerator, denominator } } end, calculate = function(self, card, context)   if context.destroy_card and context.cardarea == G.play and context.destroy_card == card and SMODS.pseudorandom_probability(card, self.key, 1, card.ability.extra.odds) then card.glass_trigger = true return { remove = true } end end }
SMODS.Enhancement { key = "multXwildXglass", name = "Mult Wild Glass Card", atlas = "enhancement", pos = { x = 0, y = 14 }, config = { mult = 4, Xmult = 2, extra = { odds = 4 } }, any_suit = true, shatters = true, in_pool = function(self,args) return false end, loc_vars = function(self, info_queue, card) local numerator, denominator = SMODS.get_probability_vars(card, 1, card.ability.extra.odds, self.key) return { vars = { card.ability.mult, card.ability.Xmult, numerator, denominator } } end, calculate = function(self, card, context)   if context.destroy_card and context.cardarea == G.play and context.destroy_card == card and SMODS.pseudorandom_probability(card, self.key, 1, card.ability.extra.odds) then card.glass_trigger = true return { remove = true } end end }
SMODS.Enhancement { key = "bonusXmultXwildXglass", name = "Bonus Mult Wild Glass Card", atlas = "enhancement", pos = { x = 0, y = 15 }, config = { bonus = 30, mult = 4, Xmult = 2, extra = { odds = 4 } }, any_suit = true, shatters = true, in_pool = function(self,args) return false end, loc_vars = function(self, info_queue, card) local numerator, denominator = SMODS.get_probability_vars(card, 1, card.ability.extra.odds, self.key) return { vars = { card.ability.bonus, card.ability.mult, card.ability.Xmult, numerator, denominator } } end, calculate = function(self, card, context)   if context.destroy_card and context.cardarea == G.play and context.destroy_card == card and SMODS.pseudorandom_probability(card, self.key, 1, card.ability.extra.odds) then card.glass_trigger = true return { remove = true } end end }
SMODS.Enhancement { key = "bonusXsteel", name = "Bonus Steel Card", atlas = "enhancement", pos = { x = 1, y = 1 }, config = { bonus = 30, h_x_mult = 1.5, }, in_pool = function(self,args) return false end, loc_vars = function(self, info_queue, card) return { vars = { card.ability.bonus, card.ability.h_x_mult } } end}
SMODS.Enhancement { key = "multXsteel", name = "Mult Steel Card", atlas = "enhancement", pos = { x = 1, y = 2 }, config = { mult = 4, h_x_mult = 1.5, }, in_pool = function(self,args) return false end, loc_vars = function(self, info_queue, card) return { vars = { card.ability.mult, card.ability.h_x_mult } } end}
SMODS.Enhancement { key = "bonusXmultXsteel", name = "Bonus Mult Steel Card", atlas = "enhancement", pos = { x = 1, y = 3 }, config = { bonus = 30, mult = 4, h_x_mult = 1.5, }, in_pool = function(self,args) return false end, loc_vars = function(self, info_queue, card) return { vars = { card.ability.bonus, card.ability.mult, card.ability.h_x_mult } } end}
SMODS.Enhancement { key = "wildXsteel", name = "Wild Steel Card", atlas = "enhancement", pos = { x = 1, y = 4 }, config = { h_x_mult = 1.5, }, any_suit = true, in_pool = function(self,args) return false end, loc_vars = function(self, info_queue, card) return { vars = { card.ability.h_x_mult } } end}
SMODS.Enhancement { key = "bonusXwildXsteel", name = "Bonus Wild Steel Card", atlas = "enhancement", pos = { x = 1, y = 5 }, config = { bonus = 30, h_x_mult = 1.5, }, any_suit = true, in_pool = function(self,args) return false end, loc_vars = function(self, info_queue, card) return { vars = { card.ability.bonus, card.ability.h_x_mult } } end}
SMODS.Enhancement { key = "multXwildXsteel", name = "Mult Wild Steel Card", atlas = "enhancement", pos = { x = 1, y = 6 }, config = { mult = 4, h_x_mult = 1.5, }, any_suit = true, in_pool = function(self,args) return false end, loc_vars = function(self, info_queue, card) return { vars = { card.ability.mult, card.ability.h_x_mult } } end}
SMODS.Enhancement { key = "bonusXmultXwildXsteel", name = "Bonus Mult Wild Steel Card", atlas = "enhancement", pos = { x = 1, y = 7 }, config = { bonus = 30, mult = 4, h_x_mult = 1.5, }, any_suit = true, in_pool = function(self,args) return false end, loc_vars = function(self, info_queue, card) return { vars = { card.ability.bonus, card.ability.mult, card.ability.h_x_mult } } end}
SMODS.Enhancement { key = "glassXsteel", name = "Glass Steel Card", atlas = "enhancement", pos = { x = 1, y = 8 }, config = { Xmult = 2, h_x_mult = 1.5, extra = { odds = 4 } }, shatters = true, in_pool = function(self,args) return false end, loc_vars = function(self, info_queue, card) local numerator, denominator = SMODS.get_probability_vars(card, 1, card.ability.extra.odds, self.key) return { vars = { card.ability.Xmult, numerator, denominator, card.ability.h_x_mult } } end, calculate = function(self, card, context)   if context.destroy_card and context.cardarea == G.play and context.destroy_card == card and SMODS.pseudorandom_probability(card, self.key, 1, card.ability.extra.odds) then card.glass_trigger = true return { remove = true } end end }
SMODS.Enhancement { key = "bonusXglassXsteel", name = "Bonus Glass Steel Card", atlas = "enhancement", pos = { x = 1, y = 9 }, config = { bonus = 30, Xmult = 2, h_x_mult = 1.5, extra = { odds = 4 } }, shatters = true, in_pool = function(self,args) return false end, loc_vars = function(self, info_queue, card) local numerator, denominator = SMODS.get_probability_vars(card, 1, card.ability.extra.odds, self.key) return { vars = { card.ability.bonus, card.ability.Xmult, numerator, denominator, card.ability.h_x_mult } } end, calculate = function(self, card, context)   if context.destroy_card and context.cardarea == G.play and context.destroy_card == card and SMODS.pseudorandom_probability(card, self.key, 1, card.ability.extra.odds) then card.glass_trigger = true return { remove = true } end end }
SMODS.Enhancement { key = "multXglassXsteel", name = "Mult Glass Steel Card", atlas = "enhancement", pos = { x = 1, y = 10 }, config = { mult = 4, Xmult = 2, h_x_mult = 1.5, extra = { odds = 4 } }, shatters = true, in_pool = function(self,args) return false end, loc_vars = function(self, info_queue, card) local numerator, denominator = SMODS.get_probability_vars(card, 1, card.ability.extra.odds, self.key) return { vars = { card.ability.mult, card.ability.Xmult, numerator, denominator, card.ability.h_x_mult } } end, calculate = function(self, card, context)   if context.destroy_card and context.cardarea == G.play and context.destroy_card == card and SMODS.pseudorandom_probability(card, self.key, 1, card.ability.extra.odds) then card.glass_trigger = true return { remove = true } end end }
SMODS.Enhancement { key = "bonusXmultXglassXsteel", name = "Bonus Mult Glass Steel Card", atlas = "enhancement", pos = { x = 1, y = 11 }, config = { bonus = 30, mult = 4, Xmult = 2, h_x_mult = 1.5, extra = { odds = 4 } }, shatters = true, in_pool = function(self,args) return false end, loc_vars = function(self, info_queue, card) local numerator, denominator = SMODS.get_probability_vars(card, 1, card.ability.extra.odds, self.key) return { vars = { card.ability.bonus, card.ability.mult, card.ability.Xmult, numerator, denominator, card.ability.h_x_mult } } end, calculate = function(self, card, context)   if context.destroy_card and context.cardarea == G.play and context.destroy_card == card and SMODS.pseudorandom_probability(card, self.key, 1, card.ability.extra.odds) then card.glass_trigger = true return { remove = true } end end }
SMODS.Enhancement { key = "wildXglassXsteel", name = "Wild Glass Steel Card", atlas = "enhancement", pos = { x = 1, y = 12 }, config = { Xmult = 2, h_x_mult = 1.5, extra = { odds = 4 } }, any_suit = true, shatters = true, in_pool = function(self,args) return false end, loc_vars = function(self, info_queue, card) local numerator, denominator = SMODS.get_probability_vars(card, 1, card.ability.extra.odds, self.key) return { vars = { card.ability.Xmult, numerator, denominator, card.ability.h_x_mult } } end, calculate = function(self, card, context)   if context.destroy_card and context.cardarea == G.play and context.destroy_card == card and SMODS.pseudorandom_probability(card, self.key, 1, card.ability.extra.odds) then card.glass_trigger = true return { remove = true } end end }
SMODS.Enhancement { key = "bonusXwildXglassXsteel", name = "Bonus Wild Glass Steel Card", atlas = "enhancement", pos = { x = 1, y = 13 }, config = { bonus = 30, Xmult = 2, h_x_mult = 1.5, extra = { odds = 4 } }, any_suit = true, shatters = true, in_pool = function(self,args) return false end, loc_vars = function(self, info_queue, card) local numerator, denominator = SMODS.get_probability_vars(card, 1, card.ability.extra.odds, self.key) return { vars = { card.ability.bonus, card.ability.Xmult, numerator, denominator, card.ability.h_x_mult } } end, calculate = function(self, card, context)   if context.destroy_card and context.cardarea == G.play and context.destroy_card == card and SMODS.pseudorandom_probability(card, self.key, 1, card.ability.extra.odds) then card.glass_trigger = true return { remove = true } end end }
SMODS.Enhancement { key = "multXwildXglassXsteel", name = "Mult Wild Glass Steel Card", atlas = "enhancement", pos = { x = 1, y = 14 }, config = { mult = 4, Xmult = 2, h_x_mult = 1.5, extra = { odds = 4 } }, any_suit = true, shatters = true, in_pool = function(self,args) return false end, loc_vars = function(self, info_queue, card) local numerator, denominator = SMODS.get_probability_vars(card, 1, card.ability.extra.odds, self.key) return { vars = { card.ability.mult, card.ability.Xmult, numerator, denominator, card.ability.h_x_mult } } end, calculate = function(self, card, context)   if context.destroy_card and context.cardarea == G.play and context.destroy_card == card and SMODS.pseudorandom_probability(card, self.key, 1, card.ability.extra.odds) then card.glass_trigger = true return { remove = true } end end }
SMODS.Enhancement { key = "bonusXmultXwildXglassXsteel", name = "Bonus Mult Wild Glass Steel Card", atlas = "enhancement", pos = { x = 1, y = 15 }, config = { bonus = 30, mult = 4, Xmult = 2, h_x_mult = 1.5, extra = { odds = 4 } }, any_suit = true, shatters = true, in_pool = function(self,args) return false end, loc_vars = function(self, info_queue, card) local numerator, denominator = SMODS.get_probability_vars(card, 1, card.ability.extra.odds, self.key) return { vars = { card.ability.bonus, card.ability.mult, card.ability.Xmult, numerator, denominator, card.ability.h_x_mult } } end, calculate = function(self, card, context)   if context.destroy_card and context.cardarea == G.play and context.destroy_card == card and SMODS.pseudorandom_probability(card, self.key, 1, card.ability.extra.odds) then card.glass_trigger = true return { remove = true } end end }
SMODS.Enhancement { key = "bonusXstone", name = "Bonus Stone Card", atlas = "enhancement", pos = { x = 2, y = 1 }, config = { bonus = 80, }, replace_base_card = true,no_rank = true,no_suit = true,always_scores = true, in_pool = function(self,args) return false end, loc_vars = function(self, info_queue, card) return { vars = { card.ability.bonus } } end}
SMODS.Enhancement { key = "multXstone", name = "Mult Stone Card", atlas = "enhancement", pos = { x = 2, y = 2 }, config = { mult = 4, bonus = 50, }, replace_base_card = true,no_rank = true,no_suit = true,always_scores = true, in_pool = function(self,args) return false end, loc_vars = function(self, info_queue, card) return { vars = { card.ability.mult, card.ability.bonus } } end}
SMODS.Enhancement { key = "bonusXmultXstone", name = "Bonus Mult Stone Card", atlas = "enhancement", pos = { x = 2, y = 3 }, config = { bonus = 80, mult = 4, }, replace_base_card = true,no_rank = true,no_suit = true,always_scores = true, in_pool = function(self,args) return false end, loc_vars = function(self, info_queue, card) return { vars = { card.ability.bonus, card.ability.mult } } end}
SMODS.Enhancement { key = "wildXstone", name = "Wild Stone Card", atlas = "enhancement", pos = { x = 2, y = 4 }, config = { bonus = 50, }, any_suit = true, replace_base_card = true,no_rank = true,always_scores = true, in_pool = function(self,args) return false end, loc_vars = function(self, info_queue, card) return { vars = { card.ability.bonus } } end}
SMODS.Enhancement { key = "bonusXwildXstone", name = "Bonus Wild Stone Card", atlas = "enhancement", pos = { x = 2, y = 5 }, config = { bonus = 80, }, any_suit = true, replace_base_card = true,no_rank = true,always_scores = true, in_pool = function(self,args) return false end, loc_vars = function(self, info_queue, card) return { vars = { card.ability.bonus } } end}
SMODS.Enhancement { key = "multXwildXstone", name = "Mult Wild Stone Card", atlas = "enhancement", pos = { x = 2, y = 6 }, config = { mult = 4, bonus = 50, }, any_suit = true, replace_base_card = true,no_rank = true,always_scores = true, in_pool = function(self,args) return false end, loc_vars = function(self, info_queue, card) return { vars = { card.ability.mult, card.ability.bonus } } end}
SMODS.Enhancement { key = "bonusXmultXwildXstone", name = "Bonus Mult Wild Stone Card", atlas = "enhancement", pos = { x = 2, y = 7 }, config = { bonus = 80, mult = 4, }, any_suit = true, replace_base_card = true,no_rank = true,always_scores = true, in_pool = function(self,args) return false end, loc_vars = function(self, info_queue, card) return { vars = { card.ability.bonus, card.ability.mult } } end}
SMODS.Enhancement { key = "glassXstone", name = "Glass Stone Card", atlas = "enhancement", pos = { x = 2, y = 8 }, config = { Xmult = 2, bonus = 50, extra = { odds = 4 } }, shatters = true, replace_base_card = true,no_rank = true,no_suit = true,always_scores = true, in_pool = function(self,args) return false end, loc_vars = function(self, info_queue, card) local numerator, denominator = SMODS.get_probability_vars(card, 1, card.ability.extra.odds, self.key) return { vars = { card.ability.Xmult, numerator, denominator, card.ability.bonus } } end, calculate = function(self, card, context)   if context.destroy_card and context.cardarea == G.play and context.destroy_card == card and SMODS.pseudorandom_probability(card, self.key, 1, card.ability.extra.odds) then card.glass_trigger = true return { remove = true } end end }
SMODS.Enhancement { key = "bonusXglassXstone", name = "Bonus Glass Stone Card", atlas = "enhancement", pos = { x = 2, y = 9 }, config = { bonus = 80, Xmult = 2, extra = { odds = 4 } }, shatters = true, replace_base_card = true,no_rank = true,no_suit = true,always_scores = true, in_pool = function(self,args) return false end, loc_vars = function(self, info_queue, card) local numerator, denominator = SMODS.get_probability_vars(card, 1, card.ability.extra.odds, self.key) return { vars = { card.ability.bonus, card.ability.Xmult, numerator, denominator } } end, calculate = function(self, card, context)   if context.destroy_card and context.cardarea == G.play and context.destroy_card == card and SMODS.pseudorandom_probability(card, self.key, 1, card.ability.extra.odds) then card.glass_trigger = true return { remove = true } end end }
SMODS.Enhancement { key = "multXglassXstone", name = "Mult Glass Stone Card", atlas = "enhancement", pos = { x = 2, y = 10 }, config = { mult = 4, Xmult = 2, bonus = 50, extra = { odds = 4 } }, shatters = true, replace_base_card = true,no_rank = true,no_suit = true,always_scores = true, in_pool = function(self,args) return false end, loc_vars = function(self, info_queue, card) local numerator, denominator = SMODS.get_probability_vars(card, 1, card.ability.extra.odds, self.key) return { vars = { card.ability.mult, card.ability.Xmult, numerator, denominator, card.ability.bonus } } end, calculate = function(self, card, context)   if context.destroy_card and context.cardarea == G.play and context.destroy_card == card and SMODS.pseudorandom_probability(card, self.key, 1, card.ability.extra.odds) then card.glass_trigger = true return { remove = true } end end }
SMODS.Enhancement { key = "bonusXmultXglassXstone", name = "Bonus Mult Glass Stone Card", atlas = "enhancement", pos = { x = 2, y = 11 }, config = { bonus = 80, mult = 4, Xmult = 2, extra = { odds = 4 } }, shatters = true, replace_base_card = true,no_rank = true,no_suit = true,always_scores = true, in_pool = function(self,args) return false end, loc_vars = function(self, info_queue, card) local numerator, denominator = SMODS.get_probability_vars(card, 1, card.ability.extra.odds, self.key) return { vars = { card.ability.bonus, card.ability.mult, card.ability.Xmult, numerator, denominator } } end, calculate = function(self, card, context)   if context.destroy_card and context.cardarea == G.play and context.destroy_card == card and SMODS.pseudorandom_probability(card, self.key, 1, card.ability.extra.odds) then card.glass_trigger = true return { remove = true } end end }
SMODS.Enhancement { key = "wildXglassXstone", name = "Wild Glass Stone Card", atlas = "enhancement", pos = { x = 2, y = 12 }, config = { Xmult = 2, bonus = 50, extra = { odds = 4 } }, any_suit = true, shatters = true, replace_base_card = true,no_rank = true,always_scores = true, in_pool = function(self,args) return false end, loc_vars = function(self, info_queue, card) local numerator, denominator = SMODS.get_probability_vars(card, 1, card.ability.extra.odds, self.key) return { vars = { card.ability.Xmult, numerator, denominator, card.ability.bonus } } end, calculate = function(self, card, context)   if context.destroy_card and context.cardarea == G.play and context.destroy_card == card and SMODS.pseudorandom_probability(card, self.key, 1, card.ability.extra.odds) then card.glass_trigger = true return { remove = true } end end }
SMODS.Enhancement { key = "bonusXwildXglassXstone", name = "Bonus Wild Glass Stone Card", atlas = "enhancement", pos = { x = 2, y = 13 }, config = { bonus = 80, Xmult = 2, extra = { odds = 4 } }, any_suit = true, shatters = true, replace_base_card = true,no_rank = true,always_scores = true, in_pool = function(self,args) return false end, loc_vars = function(self, info_queue, card) local numerator, denominator = SMODS.get_probability_vars(card, 1, card.ability.extra.odds, self.key) return { vars = { card.ability.bonus, card.ability.Xmult, numerator, denominator } } end, calculate = function(self, card, context)   if context.destroy_card and context.cardarea == G.play and context.destroy_card == card and SMODS.pseudorandom_probability(card, self.key, 1, card.ability.extra.odds) then card.glass_trigger = true return { remove = true } end end }
SMODS.Enhancement { key = "multXwildXglassXstone", name = "Mult Wild Glass Stone Card", atlas = "enhancement", pos = { x = 2, y = 14 }, config = { mult = 4, Xmult = 2, bonus = 50, extra = { odds = 4 } }, any_suit = true, shatters = true, replace_base_card = true,no_rank = true,always_scores = true, in_pool = function(self,args) return false end, loc_vars = function(self, info_queue, card) local numerator, denominator = SMODS.get_probability_vars(card, 1, card.ability.extra.odds, self.key) return { vars = { card.ability.mult, card.ability.Xmult, numerator, denominator, card.ability.bonus } } end, calculate = function(self, card, context)   if context.destroy_card and context.cardarea == G.play and context.destroy_card == card and SMODS.pseudorandom_probability(card, self.key, 1, card.ability.extra.odds) then card.glass_trigger = true return { remove = true } end end }
SMODS.Enhancement { key = "bonusXmultXwildXglassXstone", name = "Bonus Mult Wild Glass Stone Card", atlas = "enhancement", pos = { x = 2, y = 15 }, config = { bonus = 80, mult = 4, Xmult = 2, extra = { odds = 4 } }, any_suit = true, shatters = true, replace_base_card = true,no_rank = true,always_scores = true, in_pool = function(self,args) return false end, loc_vars = function(self, info_queue, card) local numerator, denominator = SMODS.get_probability_vars(card, 1, card.ability.extra.odds, self.key) return { vars = { card.ability.bonus, card.ability.mult, card.ability.Xmult, numerator, denominator } } end, calculate = function(self, card, context)   if context.destroy_card and context.cardarea == G.play and context.destroy_card == card and SMODS.pseudorandom_probability(card, self.key, 1, card.ability.extra.odds) then card.glass_trigger = true return { remove = true } end end }
SMODS.Enhancement { key = "steelXstone", name = "Steel Stone Card", atlas = "enhancement", pos = { x = 3, y = 0 }, config = { h_x_mult = 1.5, bonus = 50, }, replace_base_card = true,no_rank = true,no_suit = true,always_scores = true, in_pool = function(self,args) return false end, loc_vars = function(self, info_queue, card) return { vars = { card.ability.h_x_mult, card.ability.bonus } } end}
SMODS.Enhancement { key = "bonusXsteelXstone", name = "Bonus Steel Stone Card", atlas = "enhancement", pos = { x = 3, y = 1 }, config = { bonus = 80, h_x_mult = 1.5, }, replace_base_card = true,no_rank = true,no_suit = true,always_scores = true, in_pool = function(self,args) return false end, loc_vars = function(self, info_queue, card) return { vars = { card.ability.bonus, card.ability.h_x_mult } } end}
SMODS.Enhancement { key = "multXsteelXstone", name = "Mult Steel Stone Card", atlas = "enhancement", pos = { x = 3, y = 2 }, config = { mult = 4, h_x_mult = 1.5, bonus = 50, }, replace_base_card = true,no_rank = true,no_suit = true,always_scores = true, in_pool = function(self,args) return false end, loc_vars = function(self, info_queue, card) return { vars = { card.ability.mult, card.ability.h_x_mult, card.ability.bonus } } end}
SMODS.Enhancement { key = "bonusXmultXsteelXstone", name = "Bonus Mult Steel Stone Card", atlas = "enhancement", pos = { x = 3, y = 3 }, config = { bonus = 80, mult = 4, h_x_mult = 1.5, }, replace_base_card = true,no_rank = true,no_suit = true,always_scores = true, in_pool = function(self,args) return false end, loc_vars = function(self, info_queue, card) return { vars = { card.ability.bonus, card.ability.mult, card.ability.h_x_mult } } end}
SMODS.Enhancement { key = "wildXsteelXstone", name = "Wild Steel Stone Card", atlas = "enhancement", pos = { x = 3, y = 4 }, config = { h_x_mult = 1.5, bonus = 50, }, any_suit = true, replace_base_card = true,no_rank = true,always_scores = true, in_pool = function(self,args) return false end, loc_vars = function(self, info_queue, card) return { vars = { card.ability.h_x_mult, card.ability.bonus } } end}
SMODS.Enhancement { key = "bonusXwildXsteelXstone", name = "Bonus Wild Steel Stone Card", atlas = "enhancement", pos = { x = 3, y = 5 }, config = { bonus = 80, h_x_mult = 1.5, }, any_suit = true, replace_base_card = true,no_rank = true,always_scores = true, in_pool = function(self,args) return false end, loc_vars = function(self, info_queue, card) return { vars = { card.ability.bonus, card.ability.h_x_mult } } end}
SMODS.Enhancement { key = "multXwildXsteelXstone", name = "Mult Wild Steel Stone Card", atlas = "enhancement", pos = { x = 3, y = 6 }, config = { mult = 4, h_x_mult = 1.5, bonus = 50, }, any_suit = true, replace_base_card = true,no_rank = true,always_scores = true, in_pool = function(self,args) return false end, loc_vars = function(self, info_queue, card) return { vars = { card.ability.mult, card.ability.h_x_mult, card.ability.bonus } } end}
SMODS.Enhancement { key = "bonusXmultXwildXsteelXstone", name = "Bonus Mult Wild Steel Stone Card", atlas = "enhancement", pos = { x = 3, y = 7 }, config = { bonus = 80, mult = 4, h_x_mult = 1.5, }, any_suit = true, replace_base_card = true,no_rank = true,always_scores = true, in_pool = function(self,args) return false end, loc_vars = function(self, info_queue, card) return { vars = { card.ability.bonus, card.ability.mult, card.ability.h_x_mult } } end}
SMODS.Enhancement { key = "glassXsteelXstone", name = "Glass Steel Stone Card", atlas = "enhancement", pos = { x = 3, y = 8 }, config = { Xmult = 2, h_x_mult = 1.5, bonus = 50, extra = { odds = 4 } }, shatters = true, replace_base_card = true,no_rank = true,no_suit = true,always_scores = true, in_pool = function(self,args) return false end, loc_vars = function(self, info_queue, card) local numerator, denominator = SMODS.get_probability_vars(card, 1, card.ability.extra.odds, self.key) return { vars = { card.ability.Xmult, numerator, denominator, card.ability.h_x_mult, card.ability.bonus } } end, calculate = function(self, card, context)   if context.destroy_card and context.cardarea == G.play and context.destroy_card == card and SMODS.pseudorandom_probability(card, self.key, 1, card.ability.extra.odds) then card.glass_trigger = true return { remove = true } end end }
SMODS.Enhancement { key = "bonusXglassXsteelXstone", name = "Bonus Glass Steel Stone Card", atlas = "enhancement", pos = { x = 3, y = 9 }, config = { bonus = 80, Xmult = 2, h_x_mult = 1.5, extra = { odds = 4 } }, shatters = true, replace_base_card = true,no_rank = true,no_suit = true,always_scores = true, in_pool = function(self,args) return false end, loc_vars = function(self, info_queue, card) local numerator, denominator = SMODS.get_probability_vars(card, 1, card.ability.extra.odds, self.key) return { vars = { card.ability.bonus, card.ability.Xmult, numerator, denominator, card.ability.h_x_mult } } end, calculate = function(self, card, context)   if context.destroy_card and context.cardarea == G.play and context.destroy_card == card and SMODS.pseudorandom_probability(card, self.key, 1, card.ability.extra.odds) then card.glass_trigger = true return { remove = true } end end }
SMODS.Enhancement { key = "multXglassXsteelXstone", name = "Mult Glass Steel Stone Card", atlas = "enhancement", pos = { x = 3, y = 10 }, config = { mult = 4, Xmult = 2, h_x_mult = 1.5, bonus = 50, extra = { odds = 4 } }, shatters = true, replace_base_card = true,no_rank = true,no_suit = true,always_scores = true, in_pool = function(self,args) return false end, loc_vars = function(self, info_queue, card) local numerator, denominator = SMODS.get_probability_vars(card, 1, card.ability.extra.odds, self.key) return { vars = { card.ability.mult, card.ability.Xmult, numerator, denominator, card.ability.h_x_mult, card.ability.bonus } } end, calculate = function(self, card, context)   if context.destroy_card and context.cardarea == G.play and context.destroy_card == card and SMODS.pseudorandom_probability(card, self.key, 1, card.ability.extra.odds) then card.glass_trigger = true return { remove = true } end end }
SMODS.Enhancement { key = "bonusXmultXglassXsteelXstone", name = "Bonus Mult Glass Steel Stone Card", atlas = "enhancement", pos = { x = 3, y = 11 }, config = { bonus = 80, mult = 4, Xmult = 2, h_x_mult = 1.5, extra = { odds = 4 } }, shatters = true, replace_base_card = true,no_rank = true,no_suit = true,always_scores = true, in_pool = function(self,args) return false end, loc_vars = function(self, info_queue, card) local numerator, denominator = SMODS.get_probability_vars(card, 1, card.ability.extra.odds, self.key) return { vars = { card.ability.bonus, card.ability.mult, card.ability.Xmult, numerator, denominator, card.ability.h_x_mult } } end, calculate = function(self, card, context)   if context.destroy_card and context.cardarea == G.play and context.destroy_card == card and SMODS.pseudorandom_probability(card, self.key, 1, card.ability.extra.odds) then card.glass_trigger = true return { remove = true } end end }
SMODS.Enhancement { key = "wildXglassXsteelXstone", name = "Wild Glass Steel Stone Card", atlas = "enhancement", pos = { x = 3, y = 12 }, config = { Xmult = 2, h_x_mult = 1.5, bonus = 50, extra = { odds = 4 } }, any_suit = true, shatters = true, replace_base_card = true,no_rank = true,always_scores = true, in_pool = function(self,args) return false end, loc_vars = function(self, info_queue, card) local numerator, denominator = SMODS.get_probability_vars(card, 1, card.ability.extra.odds, self.key) return { vars = { card.ability.Xmult, numerator, denominator, card.ability.h_x_mult, card.ability.bonus } } end, calculate = function(self, card, context)   if context.destroy_card and context.cardarea == G.play and context.destroy_card == card and SMODS.pseudorandom_probability(card, self.key, 1, card.ability.extra.odds) then card.glass_trigger = true return { remove = true } end end }
SMODS.Enhancement { key = "bonusXwildXglassXsteelXstone", name = "Bonus Wild Glass Steel Stone Card", atlas = "enhancement", pos = { x = 3, y = 13 }, config = { bonus = 80, Xmult = 2, h_x_mult = 1.5, extra = { odds = 4 } }, any_suit = true, shatters = true, replace_base_card = true,no_rank = true,always_scores = true, in_pool = function(self,args) return false end, loc_vars = function(self, info_queue, card) local numerator, denominator = SMODS.get_probability_vars(card, 1, card.ability.extra.odds, self.key) return { vars = { card.ability.bonus, card.ability.Xmult, numerator, denominator, card.ability.h_x_mult } } end, calculate = function(self, card, context)   if context.destroy_card and context.cardarea == G.play and context.destroy_card == card and SMODS.pseudorandom_probability(card, self.key, 1, card.ability.extra.odds) then card.glass_trigger = true return { remove = true } end end }
SMODS.Enhancement { key = "multXwildXglassXsteelXstone", name = "Mult Wild Glass Steel Stone Card", atlas = "enhancement", pos = { x = 3, y = 14 }, config = { mult = 4, Xmult = 2, h_x_mult = 1.5, bonus = 50, extra = { odds = 4 } }, any_suit = true, shatters = true, replace_base_card = true,no_rank = true,always_scores = true, in_pool = function(self,args) return false end, loc_vars = function(self, info_queue, card) local numerator, denominator = SMODS.get_probability_vars(card, 1, card.ability.extra.odds, self.key) return { vars = { card.ability.mult, card.ability.Xmult, numerator, denominator, card.ability.h_x_mult, card.ability.bonus } } end, calculate = function(self, card, context)   if context.destroy_card and context.cardarea == G.play and context.destroy_card == card and SMODS.pseudorandom_probability(card, self.key, 1, card.ability.extra.odds) then card.glass_trigger = true return { remove = true } end end }
SMODS.Enhancement { key = "bonusXmultXwildXglassXsteelXstone", name = "Bonus Mult Wild Glass Steel Stone Card", atlas = "enhancement", pos = { x = 3, y = 15 }, config = { bonus = 80, mult = 4, Xmult = 2, h_x_mult = 1.5, extra = { odds = 4 } }, any_suit = true, shatters = true, replace_base_card = true,no_rank = true,always_scores = true, in_pool = function(self,args) return false end, loc_vars = function(self, info_queue, card) local numerator, denominator = SMODS.get_probability_vars(card, 1, card.ability.extra.odds, self.key) return { vars = { card.ability.bonus, card.ability.mult, card.ability.Xmult, numerator, denominator, card.ability.h_x_mult } } end, calculate = function(self, card, context)   if context.destroy_card and context.cardarea == G.play and context.destroy_card == card and SMODS.pseudorandom_probability(card, self.key, 1, card.ability.extra.odds) then card.glass_trigger = true return { remove = true } end end }
SMODS.Enhancement { key = "bonusXgold", name = "Bonus Gold Card", atlas = "enhancement", pos = { x = 4, y = 1 }, config = { bonus = 30, h_dollars = 3, }, in_pool = function(self,args) return false end, loc_vars = function(self, info_queue, card) return { vars = { card.ability.bonus, card.ability.h_dollars } } end}
SMODS.Enhancement { key = "multXgold", name = "Mult Gold Card", atlas = "enhancement", pos = { x = 4, y = 2 }, config = { mult = 4, h_dollars = 3, }, in_pool = function(self,args) return false end, loc_vars = function(self, info_queue, card) return { vars = { card.ability.mult, card.ability.h_dollars } } end}
SMODS.Enhancement { key = "bonusXmultXgold", name = "Bonus Mult Gold Card", atlas = "enhancement", pos = { x = 4, y = 3 }, config = { bonus = 30, mult = 4, h_dollars = 3, }, in_pool = function(self,args) return false end, loc_vars = function(self, info_queue, card) return { vars = { card.ability.bonus, card.ability.mult, card.ability.h_dollars } } end}
SMODS.Enhancement { key = "wildXgold", name = "Wild Gold Card", atlas = "enhancement", pos = { x = 4, y = 4 }, config = { h_dollars = 3, }, any_suit = true, in_pool = function(self,args) return false end, loc_vars = function(self, info_queue, card) return { vars = { card.ability.h_dollars } } end}
SMODS.Enhancement { key = "bonusXwildXgold", name = "Bonus Wild Gold Card", atlas = "enhancement", pos = { x = 4, y = 5 }, config = { bonus = 30, h_dollars = 3, }, any_suit = true, in_pool = function(self,args) return false end, loc_vars = function(self, info_queue, card) return { vars = { card.ability.bonus, card.ability.h_dollars } } end}
SMODS.Enhancement { key = "multXwildXgold", name = "Mult Wild Gold Card", atlas = "enhancement", pos = { x = 4, y = 6 }, config = { mult = 4, h_dollars = 3, }, any_suit = true, in_pool = function(self,args) return false end, loc_vars = function(self, info_queue, card) return { vars = { card.ability.mult, card.ability.h_dollars } } end}
SMODS.Enhancement { key = "bonusXmultXwildXgold", name = "Bonus Mult Wild Gold Card", atlas = "enhancement", pos = { x = 4, y = 7 }, config = { bonus = 30, mult = 4, h_dollars = 3, }, any_suit = true, in_pool = function(self,args) return false end, loc_vars = function(self, info_queue, card) return { vars = { card.ability.bonus, card.ability.mult, card.ability.h_dollars } } end}
SMODS.Enhancement { key = "glassXgold", name = "Glass Gold Card", atlas = "enhancement", pos = { x = 4, y = 8 }, config = { Xmult = 2, h_dollars = 3, extra = { odds = 4 } }, shatters = true, in_pool = function(self,args) return false end, loc_vars = function(self, info_queue, card) local numerator, denominator = SMODS.get_probability_vars(card, 1, card.ability.extra.odds, self.key) return { vars = { card.ability.Xmult, numerator, denominator, card.ability.h_dollars } } end, calculate = function(self, card, context)   if context.destroy_card and context.cardarea == G.play and context.destroy_card == card and SMODS.pseudorandom_probability(card, self.key, 1, card.ability.extra.odds) then card.glass_trigger = true return { remove = true } end end }
SMODS.Enhancement { key = "bonusXglassXgold", name = "Bonus Glass Gold Card", atlas = "enhancement", pos = { x = 4, y = 9 }, config = { bonus = 30, Xmult = 2, h_dollars = 3, extra = { odds = 4 } }, shatters = true, in_pool = function(self,args) return false end, loc_vars = function(self, info_queue, card) local numerator, denominator = SMODS.get_probability_vars(card, 1, card.ability.extra.odds, self.key) return { vars = { card.ability.bonus, card.ability.Xmult, numerator, denominator, card.ability.h_dollars } } end, calculate = function(self, card, context)   if context.destroy_card and context.cardarea == G.play and context.destroy_card == card and SMODS.pseudorandom_probability(card, self.key, 1, card.ability.extra.odds) then card.glass_trigger = true return { remove = true } end end }
SMODS.Enhancement { key = "multXglassXgold", name = "Mult Glass Gold Card", atlas = "enhancement", pos = { x = 4, y = 10 }, config = { mult = 4, Xmult = 2, h_dollars = 3, extra = { odds = 4 } }, shatters = true, in_pool = function(self,args) return false end, loc_vars = function(self, info_queue, card) local numerator, denominator = SMODS.get_probability_vars(card, 1, card.ability.extra.odds, self.key) return { vars = { card.ability.mult, card.ability.Xmult, numerator, denominator, card.ability.h_dollars } } end, calculate = function(self, card, context)   if context.destroy_card and context.cardarea == G.play and context.destroy_card == card and SMODS.pseudorandom_probability(card, self.key, 1, card.ability.extra.odds) then card.glass_trigger = true return { remove = true } end end }
SMODS.Enhancement { key = "bonusXmultXglassXgold", name = "Bonus Mult Glass Gold Card", atlas = "enhancement", pos = { x = 4, y = 11 }, config = { bonus = 30, mult = 4, Xmult = 2, h_dollars = 3, extra = { odds = 4 } }, shatters = true, in_pool = function(self,args) return false end, loc_vars = function(self, info_queue, card) local numerator, denominator = SMODS.get_probability_vars(card, 1, card.ability.extra.odds, self.key) return { vars = { card.ability.bonus, card.ability.mult, card.ability.Xmult, numerator, denominator, card.ability.h_dollars } } end, calculate = function(self, card, context)   if context.destroy_card and context.cardarea == G.play and context.destroy_card == card and SMODS.pseudorandom_probability(card, self.key, 1, card.ability.extra.odds) then card.glass_trigger = true return { remove = true } end end }
SMODS.Enhancement { key = "wildXglassXgold", name = "Wild Glass Gold Card", atlas = "enhancement", pos = { x = 4, y = 12 }, config = { Xmult = 2, h_dollars = 3, extra = { odds = 4 } }, any_suit = true, shatters = true, in_pool = function(self,args) return false end, loc_vars = function(self, info_queue, card) local numerator, denominator = SMODS.get_probability_vars(card, 1, card.ability.extra.odds, self.key) return { vars = { card.ability.Xmult, numerator, denominator, card.ability.h_dollars } } end, calculate = function(self, card, context)   if context.destroy_card and context.cardarea == G.play and context.destroy_card == card and SMODS.pseudorandom_probability(card, self.key, 1, card.ability.extra.odds) then card.glass_trigger = true return { remove = true } end end }
SMODS.Enhancement { key = "bonusXwildXglassXgold", name = "Bonus Wild Glass Gold Card", atlas = "enhancement", pos = { x = 4, y = 13 }, config = { bonus = 30, Xmult = 2, h_dollars = 3, extra = { odds = 4 } }, any_suit = true, shatters = true, in_pool = function(self,args) return false end, loc_vars = function(self, info_queue, card) local numerator, denominator = SMODS.get_probability_vars(card, 1, card.ability.extra.odds, self.key) return { vars = { card.ability.bonus, card.ability.Xmult, numerator, denominator, card.ability.h_dollars } } end, calculate = function(self, card, context)   if context.destroy_card and context.cardarea == G.play and context.destroy_card == card and SMODS.pseudorandom_probability(card, self.key, 1, card.ability.extra.odds) then card.glass_trigger = true return { remove = true } end end }
SMODS.Enhancement { key = "multXwildXglassXgold", name = "Mult Wild Glass Gold Card", atlas = "enhancement", pos = { x = 4, y = 14 }, config = { mult = 4, Xmult = 2, h_dollars = 3, extra = { odds = 4 } }, any_suit = true, shatters = true, in_pool = function(self,args) return false end, loc_vars = function(self, info_queue, card) local numerator, denominator = SMODS.get_probability_vars(card, 1, card.ability.extra.odds, self.key) return { vars = { card.ability.mult, card.ability.Xmult, numerator, denominator, card.ability.h_dollars } } end, calculate = function(self, card, context)   if context.destroy_card and context.cardarea == G.play and context.destroy_card == card and SMODS.pseudorandom_probability(card, self.key, 1, card.ability.extra.odds) then card.glass_trigger = true return { remove = true } end end }
SMODS.Enhancement { key = "bonusXmultXwildXglassXgold", name = "Bonus Mult Wild Glass Gold Card", atlas = "enhancement", pos = { x = 4, y = 15 }, config = { bonus = 30, mult = 4, Xmult = 2, h_dollars = 3, extra = { odds = 4 } }, any_suit = true, shatters = true, in_pool = function(self,args) return false end, loc_vars = function(self, info_queue, card) local numerator, denominator = SMODS.get_probability_vars(card, 1, card.ability.extra.odds, self.key) return { vars = { card.ability.bonus, card.ability.mult, card.ability.Xmult, numerator, denominator, card.ability.h_dollars } } end, calculate = function(self, card, context)   if context.destroy_card and context.cardarea == G.play and context.destroy_card == card and SMODS.pseudorandom_probability(card, self.key, 1, card.ability.extra.odds) then card.glass_trigger = true return { remove = true } end end }
SMODS.Enhancement { key = "steelXgold", name = "Steel Gold Card", atlas = "enhancement", pos = { x = 5, y = 0 }, config = { h_x_mult = 1.5, h_dollars = 3, }, in_pool = function(self,args) return false end, loc_vars = function(self, info_queue, card) return { vars = { card.ability.h_x_mult, card.ability.h_dollars } } end}
SMODS.Enhancement { key = "bonusXsteelXgold", name = "Bonus Steel Gold Card", atlas = "enhancement", pos = { x = 5, y = 1 }, config = { bonus = 30, h_x_mult = 1.5, h_dollars = 3, }, in_pool = function(self,args) return false end, loc_vars = function(self, info_queue, card) return { vars = { card.ability.bonus, card.ability.h_x_mult, card.ability.h_dollars } } end}
SMODS.Enhancement { key = "multXsteelXgold", name = "Mult Steel Gold Card", atlas = "enhancement", pos = { x = 5, y = 2 }, config = { mult = 4, h_x_mult = 1.5, h_dollars = 3, }, in_pool = function(self,args) return false end, loc_vars = function(self, info_queue, card) return { vars = { card.ability.mult, card.ability.h_x_mult, card.ability.h_dollars } } end}
SMODS.Enhancement { key = "bonusXmultXsteelXgold", name = "Bonus Mult Steel Gold Card", atlas = "enhancement", pos = { x = 5, y = 3 }, config = { bonus = 30, mult = 4, h_x_mult = 1.5, h_dollars = 3, }, in_pool = function(self,args) return false end, loc_vars = function(self, info_queue, card) return { vars = { card.ability.bonus, card.ability.mult, card.ability.h_x_mult, card.ability.h_dollars } } end}
SMODS.Enhancement { key = "wildXsteelXgold", name = "Wild Steel Gold Card", atlas = "enhancement", pos = { x = 5, y = 4 }, config = { h_x_mult = 1.5, h_dollars = 3, }, any_suit = true, in_pool = function(self,args) return false end, loc_vars = function(self, info_queue, card) return { vars = { card.ability.h_x_mult, card.ability.h_dollars } } end}
SMODS.Enhancement { key = "bonusXwildXsteelXgold", name = "Bonus Wild Steel Gold Card", atlas = "enhancement", pos = { x = 5, y = 5 }, config = { bonus = 30, h_x_mult = 1.5, h_dollars = 3, }, any_suit = true, in_pool = function(self,args) return false end, loc_vars = function(self, info_queue, card) return { vars = { card.ability.bonus, card.ability.h_x_mult, card.ability.h_dollars } } end}
SMODS.Enhancement { key = "multXwildXsteelXgold", name = "Mult Wild Steel Gold Card", atlas = "enhancement", pos = { x = 5, y = 6 }, config = { mult = 4, h_x_mult = 1.5, h_dollars = 3, }, any_suit = true, in_pool = function(self,args) return false end, loc_vars = function(self, info_queue, card) return { vars = { card.ability.mult, card.ability.h_x_mult, card.ability.h_dollars } } end}
SMODS.Enhancement { key = "bonusXmultXwildXsteelXgold", name = "Bonus Mult Wild Steel Gold Card", atlas = "enhancement", pos = { x = 5, y = 7 }, config = { bonus = 30, mult = 4, h_x_mult = 1.5, h_dollars = 3, }, any_suit = true, in_pool = function(self,args) return false end, loc_vars = function(self, info_queue, card) return { vars = { card.ability.bonus, card.ability.mult, card.ability.h_x_mult, card.ability.h_dollars } } end}
SMODS.Enhancement { key = "glassXsteelXgold", name = "Glass Steel Gold Card", atlas = "enhancement", pos = { x = 5, y = 8 }, config = { Xmult = 2, h_x_mult = 1.5, h_dollars = 3, extra = { odds = 4 } }, shatters = true, in_pool = function(self,args) return false end, loc_vars = function(self, info_queue, card) local numerator, denominator = SMODS.get_probability_vars(card, 1, card.ability.extra.odds, self.key) return { vars = { card.ability.Xmult, numerator, denominator, card.ability.h_x_mult, card.ability.h_dollars } } end, calculate = function(self, card, context)   if context.destroy_card and context.cardarea == G.play and context.destroy_card == card and SMODS.pseudorandom_probability(card, self.key, 1, card.ability.extra.odds) then card.glass_trigger = true return { remove = true } end end }
SMODS.Enhancement { key = "bonusXglassXsteelXgold", name = "Bonus Glass Steel Gold Card", atlas = "enhancement", pos = { x = 5, y = 9 }, config = { bonus = 30, Xmult = 2, h_x_mult = 1.5, h_dollars = 3, extra = { odds = 4 } }, shatters = true, in_pool = function(self,args) return false end, loc_vars = function(self, info_queue, card) local numerator, denominator = SMODS.get_probability_vars(card, 1, card.ability.extra.odds, self.key) return { vars = { card.ability.bonus, card.ability.Xmult, numerator, denominator, card.ability.h_x_mult, card.ability.h_dollars } } end, calculate = function(self, card, context)   if context.destroy_card and context.cardarea == G.play and context.destroy_card == card and SMODS.pseudorandom_probability(card, self.key, 1, card.ability.extra.odds) then card.glass_trigger = true return { remove = true } end end }
SMODS.Enhancement { key = "multXglassXsteelXgold", name = "Mult Glass Steel Gold Card", atlas = "enhancement", pos = { x = 5, y = 10 }, config = { mult = 4, Xmult = 2, h_x_mult = 1.5, h_dollars = 3, extra = { odds = 4 } }, shatters = true, in_pool = function(self,args) return false end, loc_vars = function(self, info_queue, card) local numerator, denominator = SMODS.get_probability_vars(card, 1, card.ability.extra.odds, self.key) return { vars = { card.ability.mult, card.ability.Xmult, numerator, denominator, card.ability.h_x_mult, card.ability.h_dollars } } end, calculate = function(self, card, context)   if context.destroy_card and context.cardarea == G.play and context.destroy_card == card and SMODS.pseudorandom_probability(card, self.key, 1, card.ability.extra.odds) then card.glass_trigger = true return { remove = true } end end }
SMODS.Enhancement { key = "bonusXmultXglassXsteelXgold", name = "Bonus Mult Glass Steel Gold Card", atlas = "enhancement", pos = { x = 5, y = 11 }, config = { bonus = 30, mult = 4, Xmult = 2, h_x_mult = 1.5, h_dollars = 3, extra = { odds = 4 } }, shatters = true, in_pool = function(self,args) return false end, loc_vars = function(self, info_queue, card) local numerator, denominator = SMODS.get_probability_vars(card, 1, card.ability.extra.odds, self.key) return { vars = { card.ability.bonus, card.ability.mult, card.ability.Xmult, numerator, denominator, card.ability.h_x_mult, card.ability.h_dollars } } end, calculate = function(self, card, context)   if context.destroy_card and context.cardarea == G.play and context.destroy_card == card and SMODS.pseudorandom_probability(card, self.key, 1, card.ability.extra.odds) then card.glass_trigger = true return { remove = true } end end }
SMODS.Enhancement { key = "wildXglassXsteelXgold", name = "Wild Glass Steel Gold Card", atlas = "enhancement", pos = { x = 5, y = 12 }, config = { Xmult = 2, h_x_mult = 1.5, h_dollars = 3, extra = { odds = 4 } }, any_suit = true, shatters = true, in_pool = function(self,args) return false end, loc_vars = function(self, info_queue, card) local numerator, denominator = SMODS.get_probability_vars(card, 1, card.ability.extra.odds, self.key) return { vars = { card.ability.Xmult, numerator, denominator, card.ability.h_x_mult, card.ability.h_dollars } } end, calculate = function(self, card, context)   if context.destroy_card and context.cardarea == G.play and context.destroy_card == card and SMODS.pseudorandom_probability(card, self.key, 1, card.ability.extra.odds) then card.glass_trigger = true return { remove = true } end end }
SMODS.Enhancement { key = "bonusXwildXglassXsteelXgold", name = "Bonus Wild Glass Steel Gold Card", atlas = "enhancement", pos = { x = 5, y = 13 }, config = { bonus = 30, Xmult = 2, h_x_mult = 1.5, h_dollars = 3, extra = { odds = 4 } }, any_suit = true, shatters = true, in_pool = function(self,args) return false end, loc_vars = function(self, info_queue, card) local numerator, denominator = SMODS.get_probability_vars(card, 1, card.ability.extra.odds, self.key) return { vars = { card.ability.bonus, card.ability.Xmult, numerator, denominator, card.ability.h_x_mult, card.ability.h_dollars } } end, calculate = function(self, card, context)   if context.destroy_card and context.cardarea == G.play and context.destroy_card == card and SMODS.pseudorandom_probability(card, self.key, 1, card.ability.extra.odds) then card.glass_trigger = true return { remove = true } end end }
SMODS.Enhancement { key = "multXwildXglassXsteelXgold", name = "Mult Wild Glass Steel Gold Card", atlas = "enhancement", pos = { x = 5, y = 14 }, config = { mult = 4, Xmult = 2, h_x_mult = 1.5, h_dollars = 3, extra = { odds = 4 } }, any_suit = true, shatters = true, in_pool = function(self,args) return false end, loc_vars = function(self, info_queue, card) local numerator, denominator = SMODS.get_probability_vars(card, 1, card.ability.extra.odds, self.key) return { vars = { card.ability.mult, card.ability.Xmult, numerator, denominator, card.ability.h_x_mult, card.ability.h_dollars } } end, calculate = function(self, card, context)   if context.destroy_card and context.cardarea == G.play and context.destroy_card == card and SMODS.pseudorandom_probability(card, self.key, 1, card.ability.extra.odds) then card.glass_trigger = true return { remove = true } end end }
SMODS.Enhancement { key = "bonusXmultXwildXglassXsteelXgold", name = "Bonus Mult Wild Glass Steel Gold Card", atlas = "enhancement", pos = { x = 5, y = 15 }, config = { bonus = 30, mult = 4, Xmult = 2, h_x_mult = 1.5, h_dollars = 3, extra = { odds = 4 } }, any_suit = true, shatters = true, in_pool = function(self,args) return false end, loc_vars = function(self, info_queue, card) local numerator, denominator = SMODS.get_probability_vars(card, 1, card.ability.extra.odds, self.key) return { vars = { card.ability.bonus, card.ability.mult, card.ability.Xmult, numerator, denominator, card.ability.h_x_mult, card.ability.h_dollars } } end, calculate = function(self, card, context)   if context.destroy_card and context.cardarea == G.play and context.destroy_card == card and SMODS.pseudorandom_probability(card, self.key, 1, card.ability.extra.odds) then card.glass_trigger = true return { remove = true } end end }
SMODS.Enhancement { key = "stoneXgold", name = "Stone Gold Card", atlas = "enhancement", pos = { x = 6, y = 0 }, config = { bonus = 50, h_dollars = 3, }, replace_base_card = true,no_rank = true,no_suit = true,always_scores = true, in_pool = function(self,args) return false end, loc_vars = function(self, info_queue, card) return { vars = { card.ability.bonus, card.ability.h_dollars } } end}
SMODS.Enhancement { key = "bonusXstoneXgold", name = "Bonus Stone Gold Card", atlas = "enhancement", pos = { x = 6, y = 1 }, config = { bonus = 80, h_dollars = 3, }, replace_base_card = true,no_rank = true,no_suit = true,always_scores = true, in_pool = function(self,args) return false end, loc_vars = function(self, info_queue, card) return { vars = { card.ability.bonus, card.ability.h_dollars } } end}
SMODS.Enhancement { key = "multXstoneXgold", name = "Mult Stone Gold Card", atlas = "enhancement", pos = { x = 6, y = 2 }, config = { mult = 4, bonus = 50, h_dollars = 3, }, replace_base_card = true,no_rank = true,no_suit = true,always_scores = true, in_pool = function(self,args) return false end, loc_vars = function(self, info_queue, card) return { vars = { card.ability.mult, card.ability.bonus, card.ability.h_dollars } } end}
SMODS.Enhancement { key = "bonusXmultXstoneXgold", name = "Bonus Mult Stone Gold Card", atlas = "enhancement", pos = { x = 6, y = 3 }, config = { bonus = 80, mult = 4, h_dollars = 3, }, replace_base_card = true,no_rank = true,no_suit = true,always_scores = true, in_pool = function(self,args) return false end, loc_vars = function(self, info_queue, card) return { vars = { card.ability.bonus, card.ability.mult, card.ability.h_dollars } } end}
SMODS.Enhancement { key = "wildXstoneXgold", name = "Wild Stone Gold Card", atlas = "enhancement", pos = { x = 6, y = 4 }, config = { bonus = 50, h_dollars = 3, }, any_suit = true, replace_base_card = true,no_rank = true,always_scores = true, in_pool = function(self,args) return false end, loc_vars = function(self, info_queue, card) return { vars = { card.ability.bonus, card.ability.h_dollars } } end}
SMODS.Enhancement { key = "bonusXwildXstoneXgold", name = "Bonus Wild Stone Gold Card", atlas = "enhancement", pos = { x = 6, y = 5 }, config = { bonus = 80, h_dollars = 3, }, any_suit = true, replace_base_card = true,no_rank = true,always_scores = true, in_pool = function(self,args) return false end, loc_vars = function(self, info_queue, card) return { vars = { card.ability.bonus, card.ability.h_dollars } } end}
SMODS.Enhancement { key = "multXwildXstoneXgold", name = "Mult Wild Stone Gold Card", atlas = "enhancement", pos = { x = 6, y = 6 }, config = { mult = 4, bonus = 50, h_dollars = 3, }, any_suit = true, replace_base_card = true,no_rank = true,always_scores = true, in_pool = function(self,args) return false end, loc_vars = function(self, info_queue, card) return { vars = { card.ability.mult, card.ability.bonus, card.ability.h_dollars } } end}
SMODS.Enhancement { key = "bonusXmultXwildXstoneXgold", name = "Bonus Mult Wild Stone Gold Card", atlas = "enhancement", pos = { x = 6, y = 7 }, config = { bonus = 80, mult = 4, h_dollars = 3, }, any_suit = true, replace_base_card = true,no_rank = true,always_scores = true, in_pool = function(self,args) return false end, loc_vars = function(self, info_queue, card) return { vars = { card.ability.bonus, card.ability.mult, card.ability.h_dollars } } end}
SMODS.Enhancement { key = "glassXstoneXgold", name = "Glass Stone Gold Card", atlas = "enhancement", pos = { x = 6, y = 8 }, config = { Xmult = 2, bonus = 50, h_dollars = 3, extra = { odds = 4 } }, shatters = true, replace_base_card = true,no_rank = true,no_suit = true,always_scores = true, in_pool = function(self,args) return false end, loc_vars = function(self, info_queue, card) local numerator, denominator = SMODS.get_probability_vars(card, 1, card.ability.extra.odds, self.key) return { vars = { card.ability.Xmult, numerator, denominator, card.ability.bonus, card.ability.h_dollars } } end, calculate = function(self, card, context)   if context.destroy_card and context.cardarea == G.play and context.destroy_card == card and SMODS.pseudorandom_probability(card, self.key, 1, card.ability.extra.odds) then card.glass_trigger = true return { remove = true } end end }
SMODS.Enhancement { key = "bonusXglassXstoneXgold", name = "Bonus Glass Stone Gold Card", atlas = "enhancement", pos = { x = 6, y = 9 }, config = { bonus = 80, Xmult = 2, h_dollars = 3, extra = { odds = 4 } }, shatters = true, replace_base_card = true,no_rank = true,no_suit = true,always_scores = true, in_pool = function(self,args) return false end, loc_vars = function(self, info_queue, card) local numerator, denominator = SMODS.get_probability_vars(card, 1, card.ability.extra.odds, self.key) return { vars = { card.ability.bonus, card.ability.Xmult, numerator, denominator, card.ability.h_dollars } } end, calculate = function(self, card, context)   if context.destroy_card and context.cardarea == G.play and context.destroy_card == card and SMODS.pseudorandom_probability(card, self.key, 1, card.ability.extra.odds) then card.glass_trigger = true return { remove = true } end end }
SMODS.Enhancement { key = "multXglassXstoneXgold", name = "Mult Glass Stone Gold Card", atlas = "enhancement", pos = { x = 6, y = 10 }, config = { mult = 4, Xmult = 2, bonus = 50, h_dollars = 3, extra = { odds = 4 } }, shatters = true, replace_base_card = true,no_rank = true,no_suit = true,always_scores = true, in_pool = function(self,args) return false end, loc_vars = function(self, info_queue, card) local numerator, denominator = SMODS.get_probability_vars(card, 1, card.ability.extra.odds, self.key) return { vars = { card.ability.mult, card.ability.Xmult, numerator, denominator, card.ability.bonus, card.ability.h_dollars } } end, calculate = function(self, card, context)   if context.destroy_card and context.cardarea == G.play and context.destroy_card == card and SMODS.pseudorandom_probability(card, self.key, 1, card.ability.extra.odds) then card.glass_trigger = true return { remove = true } end end }
SMODS.Enhancement { key = "bonusXmultXglassXstoneXgold", name = "Bonus Mult Glass Stone Gold Card", atlas = "enhancement", pos = { x = 6, y = 11 }, config = { bonus = 80, mult = 4, Xmult = 2, h_dollars = 3, extra = { odds = 4 } }, shatters = true, replace_base_card = true,no_rank = true,no_suit = true,always_scores = true, in_pool = function(self,args) return false end, loc_vars = function(self, info_queue, card) local numerator, denominator = SMODS.get_probability_vars(card, 1, card.ability.extra.odds, self.key) return { vars = { card.ability.bonus, card.ability.mult, card.ability.Xmult, numerator, denominator, card.ability.h_dollars } } end, calculate = function(self, card, context)   if context.destroy_card and context.cardarea == G.play and context.destroy_card == card and SMODS.pseudorandom_probability(card, self.key, 1, card.ability.extra.odds) then card.glass_trigger = true return { remove = true } end end }
SMODS.Enhancement { key = "wildXglassXstoneXgold", name = "Wild Glass Stone Gold Card", atlas = "enhancement", pos = { x = 6, y = 12 }, config = { Xmult = 2, bonus = 50, h_dollars = 3, extra = { odds = 4 } }, any_suit = true, shatters = true, replace_base_card = true,no_rank = true,always_scores = true, in_pool = function(self,args) return false end, loc_vars = function(self, info_queue, card) local numerator, denominator = SMODS.get_probability_vars(card, 1, card.ability.extra.odds, self.key) return { vars = { card.ability.Xmult, numerator, denominator, card.ability.bonus, card.ability.h_dollars } } end, calculate = function(self, card, context)   if context.destroy_card and context.cardarea == G.play and context.destroy_card == card and SMODS.pseudorandom_probability(card, self.key, 1, card.ability.extra.odds) then card.glass_trigger = true return { remove = true } end end }
SMODS.Enhancement { key = "bonusXwildXglassXstoneXgold", name = "Bonus Wild Glass Stone Gold Card", atlas = "enhancement", pos = { x = 6, y = 13 }, config = { bonus = 80, Xmult = 2, h_dollars = 3, extra = { odds = 4 } }, any_suit = true, shatters = true, replace_base_card = true,no_rank = true,always_scores = true, in_pool = function(self,args) return false end, loc_vars = function(self, info_queue, card) local numerator, denominator = SMODS.get_probability_vars(card, 1, card.ability.extra.odds, self.key) return { vars = { card.ability.bonus, card.ability.Xmult, numerator, denominator, card.ability.h_dollars } } end, calculate = function(self, card, context)   if context.destroy_card and context.cardarea == G.play and context.destroy_card == card and SMODS.pseudorandom_probability(card, self.key, 1, card.ability.extra.odds) then card.glass_trigger = true return { remove = true } end end }
SMODS.Enhancement { key = "multXwildXglassXstoneXgold", name = "Mult Wild Glass Stone Gold Card", atlas = "enhancement", pos = { x = 6, y = 14 }, config = { mult = 4, Xmult = 2, bonus = 50, h_dollars = 3, extra = { odds = 4 } }, any_suit = true, shatters = true, replace_base_card = true,no_rank = true,always_scores = true, in_pool = function(self,args) return false end, loc_vars = function(self, info_queue, card) local numerator, denominator = SMODS.get_probability_vars(card, 1, card.ability.extra.odds, self.key) return { vars = { card.ability.mult, card.ability.Xmult, numerator, denominator, card.ability.bonus, card.ability.h_dollars } } end, calculate = function(self, card, context)   if context.destroy_card and context.cardarea == G.play and context.destroy_card == card and SMODS.pseudorandom_probability(card, self.key, 1, card.ability.extra.odds) then card.glass_trigger = true return { remove = true } end end }
SMODS.Enhancement { key = "bonusXmultXwildXglassXstoneXgold", name = "Bonus Mult Wild Glass Stone Gold Card", atlas = "enhancement", pos = { x = 6, y = 15 }, config = { bonus = 80, mult = 4, Xmult = 2, h_dollars = 3, extra = { odds = 4 } }, any_suit = true, shatters = true, replace_base_card = true,no_rank = true,always_scores = true, in_pool = function(self,args) return false end, loc_vars = function(self, info_queue, card) local numerator, denominator = SMODS.get_probability_vars(card, 1, card.ability.extra.odds, self.key) return { vars = { card.ability.bonus, card.ability.mult, card.ability.Xmult, numerator, denominator, card.ability.h_dollars } } end, calculate = function(self, card, context)   if context.destroy_card and context.cardarea == G.play and context.destroy_card == card and SMODS.pseudorandom_probability(card, self.key, 1, card.ability.extra.odds) then card.glass_trigger = true return { remove = true } end end }
SMODS.Enhancement { key = "steelXstoneXgold", name = "Steel Stone Gold Card", atlas = "enhancement", pos = { x = 7, y = 0 }, config = { h_x_mult = 1.5, bonus = 50, h_dollars = 3, }, replace_base_card = true,no_rank = true,no_suit = true,always_scores = true, in_pool = function(self,args) return false end, loc_vars = function(self, info_queue, card) return { vars = { card.ability.h_x_mult, card.ability.bonus, card.ability.h_dollars } } end}
SMODS.Enhancement { key = "bonusXsteelXstoneXgold", name = "Bonus Steel Stone Gold Card", atlas = "enhancement", pos = { x = 7, y = 1 }, config = { bonus = 80, h_x_mult = 1.5, h_dollars = 3, }, replace_base_card = true,no_rank = true,no_suit = true,always_scores = true, in_pool = function(self,args) return false end, loc_vars = function(self, info_queue, card) return { vars = { card.ability.bonus, card.ability.h_x_mult, card.ability.h_dollars } } end}
SMODS.Enhancement { key = "multXsteelXstoneXgold", name = "Mult Steel Stone Gold Card", atlas = "enhancement", pos = { x = 7, y = 2 }, config = { mult = 4, h_x_mult = 1.5, bonus = 50, h_dollars = 3, }, replace_base_card = true,no_rank = true,no_suit = true,always_scores = true, in_pool = function(self,args) return false end, loc_vars = function(self, info_queue, card) return { vars = { card.ability.mult, card.ability.h_x_mult, card.ability.bonus, card.ability.h_dollars } } end}
SMODS.Enhancement { key = "bonusXmultXsteelXstoneXgold", name = "Bonus Mult Steel Stone Gold Card", atlas = "enhancement", pos = { x = 7, y = 3 }, config = { bonus = 80, mult = 4, h_x_mult = 1.5, h_dollars = 3, }, replace_base_card = true,no_rank = true,no_suit = true,always_scores = true, in_pool = function(self,args) return false end, loc_vars = function(self, info_queue, card) return { vars = { card.ability.bonus, card.ability.mult, card.ability.h_x_mult, card.ability.h_dollars } } end}
SMODS.Enhancement { key = "wildXsteelXstoneXgold", name = "Wild Steel Stone Gold Card", atlas = "enhancement", pos = { x = 7, y = 4 }, config = { h_x_mult = 1.5, bonus = 50, h_dollars = 3, }, any_suit = true, replace_base_card = true,no_rank = true,always_scores = true, in_pool = function(self,args) return false end, loc_vars = function(self, info_queue, card) return { vars = { card.ability.h_x_mult, card.ability.bonus, card.ability.h_dollars } } end}
SMODS.Enhancement { key = "bonusXwildXsteelXstoneXgold", name = "Bonus Wild Steel Stone Gold Card", atlas = "enhancement", pos = { x = 7, y = 5 }, config = { bonus = 80, h_x_mult = 1.5, h_dollars = 3, }, any_suit = true, replace_base_card = true,no_rank = true,always_scores = true, in_pool = function(self,args) return false end, loc_vars = function(self, info_queue, card) return { vars = { card.ability.bonus, card.ability.h_x_mult, card.ability.h_dollars } } end}
SMODS.Enhancement { key = "multXwildXsteelXstoneXgold", name = "Mult Wild Steel Stone Gold Card", atlas = "enhancement", pos = { x = 7, y = 6 }, config = { mult = 4, h_x_mult = 1.5, bonus = 50, h_dollars = 3, }, any_suit = true, replace_base_card = true,no_rank = true,always_scores = true, in_pool = function(self,args) return false end, loc_vars = function(self, info_queue, card) return { vars = { card.ability.mult, card.ability.h_x_mult, card.ability.bonus, card.ability.h_dollars } } end}
SMODS.Enhancement { key = "bonusXmultXwildXsteelXstoneXgold", name = "Bonus Mult Wild Steel Stone Gold Card", atlas = "enhancement", pos = { x = 7, y = 7 }, config = { bonus = 80, mult = 4, h_x_mult = 1.5, h_dollars = 3, }, any_suit = true, replace_base_card = true,no_rank = true,always_scores = true, in_pool = function(self,args) return false end, loc_vars = function(self, info_queue, card) return { vars = { card.ability.bonus, card.ability.mult, card.ability.h_x_mult, card.ability.h_dollars } } end}
SMODS.Enhancement { key = "glassXsteelXstoneXgold", name = "Glass Steel Stone Gold Card", atlas = "enhancement", pos = { x = 7, y = 8 }, config = { Xmult = 2, h_x_mult = 1.5, bonus = 50, h_dollars = 3, extra = { odds = 4 } }, shatters = true, replace_base_card = true,no_rank = true,no_suit = true,always_scores = true, in_pool = function(self,args) return false end, loc_vars = function(self, info_queue, card) local numerator, denominator = SMODS.get_probability_vars(card, 1, card.ability.extra.odds, self.key) return { vars = { card.ability.Xmult, numerator, denominator, card.ability.h_x_mult, card.ability.bonus, card.ability.h_dollars } } end, calculate = function(self, card, context)   if context.destroy_card and context.cardarea == G.play and context.destroy_card == card and SMODS.pseudorandom_probability(card, self.key, 1, card.ability.extra.odds) then card.glass_trigger = true return { remove = true } end end }
SMODS.Enhancement { key = "bonusXglassXsteelXstoneXgold", name = "Bonus Glass Steel Stone Gold Card", atlas = "enhancement", pos = { x = 7, y = 9 }, config = { bonus = 80, Xmult = 2, h_x_mult = 1.5, h_dollars = 3, extra = { odds = 4 } }, shatters = true, replace_base_card = true,no_rank = true,no_suit = true,always_scores = true, in_pool = function(self,args) return false end, loc_vars = function(self, info_queue, card) local numerator, denominator = SMODS.get_probability_vars(card, 1, card.ability.extra.odds, self.key) return { vars = { card.ability.bonus, card.ability.Xmult, numerator, denominator, card.ability.h_x_mult, card.ability.h_dollars } } end, calculate = function(self, card, context)   if context.destroy_card and context.cardarea == G.play and context.destroy_card == card and SMODS.pseudorandom_probability(card, self.key, 1, card.ability.extra.odds) then card.glass_trigger = true return { remove = true } end end }
SMODS.Enhancement { key = "multXglassXsteelXstoneXgold", name = "Mult Glass Steel Stone Gold Card", atlas = "enhancement", pos = { x = 7, y = 10 }, config = { mult = 4, Xmult = 2, h_x_mult = 1.5, bonus = 50, h_dollars = 3, extra = { odds = 4 } }, shatters = true, replace_base_card = true,no_rank = true,no_suit = true,always_scores = true, in_pool = function(self,args) return false end, loc_vars = function(self, info_queue, card) local numerator, denominator = SMODS.get_probability_vars(card, 1, card.ability.extra.odds, self.key) return { vars = { card.ability.mult, card.ability.Xmult, numerator, denominator, card.ability.h_x_mult, card.ability.bonus, card.ability.h_dollars } } end, calculate = function(self, card, context)   if context.destroy_card and context.cardarea == G.play and context.destroy_card == card and SMODS.pseudorandom_probability(card, self.key, 1, card.ability.extra.odds) then card.glass_trigger = true return { remove = true } end end }
SMODS.Enhancement { key = "bonusXmultXglassXsteelXstoneXgold", name = "Bonus Mult Glass Steel Stone Gold Card", atlas = "enhancement", pos = { x = 7, y = 11 }, config = { bonus = 80, mult = 4, Xmult = 2, h_x_mult = 1.5, h_dollars = 3, extra = { odds = 4 } }, shatters = true, replace_base_card = true,no_rank = true,no_suit = true,always_scores = true, in_pool = function(self,args) return false end, loc_vars = function(self, info_queue, card) local numerator, denominator = SMODS.get_probability_vars(card, 1, card.ability.extra.odds, self.key) return { vars = { card.ability.bonus, card.ability.mult, card.ability.Xmult, numerator, denominator, card.ability.h_x_mult, card.ability.h_dollars } } end, calculate = function(self, card, context)   if context.destroy_card and context.cardarea == G.play and context.destroy_card == card and SMODS.pseudorandom_probability(card, self.key, 1, card.ability.extra.odds) then card.glass_trigger = true return { remove = true } end end }
SMODS.Enhancement { key = "wildXglassXsteelXstoneXgold", name = "Wild Glass Steel Stone Gold Card", atlas = "enhancement", pos = { x = 7, y = 12 }, config = { Xmult = 2, h_x_mult = 1.5, bonus = 50, h_dollars = 3, extra = { odds = 4 } }, any_suit = true, shatters = true, replace_base_card = true,no_rank = true,always_scores = true, in_pool = function(self,args) return false end, loc_vars = function(self, info_queue, card) local numerator, denominator = SMODS.get_probability_vars(card, 1, card.ability.extra.odds, self.key) return { vars = { card.ability.Xmult, numerator, denominator, card.ability.h_x_mult, card.ability.bonus, card.ability.h_dollars } } end, calculate = function(self, card, context)   if context.destroy_card and context.cardarea == G.play and context.destroy_card == card and SMODS.pseudorandom_probability(card, self.key, 1, card.ability.extra.odds) then card.glass_trigger = true return { remove = true } end end }
SMODS.Enhancement { key = "bonusXwildXglassXsteelXstoneXgold", name = "Bonus Wild Glass Steel Stone Gold Card", atlas = "enhancement", pos = { x = 7, y = 13 }, config = { bonus = 80, Xmult = 2, h_x_mult = 1.5, h_dollars = 3, extra = { odds = 4 } }, any_suit = true, shatters = true, replace_base_card = true,no_rank = true,always_scores = true, in_pool = function(self,args) return false end, loc_vars = function(self, info_queue, card) local numerator, denominator = SMODS.get_probability_vars(card, 1, card.ability.extra.odds, self.key) return { vars = { card.ability.bonus, card.ability.Xmult, numerator, denominator, card.ability.h_x_mult, card.ability.h_dollars } } end, calculate = function(self, card, context)   if context.destroy_card and context.cardarea == G.play and context.destroy_card == card and SMODS.pseudorandom_probability(card, self.key, 1, card.ability.extra.odds) then card.glass_trigger = true return { remove = true } end end }
SMODS.Enhancement { key = "multXwildXglassXsteelXstoneXgold", name = "Mult Wild Glass Steel Stone Gold Card", atlas = "enhancement", pos = { x = 7, y = 14 }, config = { mult = 4, Xmult = 2, h_x_mult = 1.5, bonus = 50, h_dollars = 3, extra = { odds = 4 } }, any_suit = true, shatters = true, replace_base_card = true,no_rank = true,always_scores = true, in_pool = function(self,args) return false end, loc_vars = function(self, info_queue, card) local numerator, denominator = SMODS.get_probability_vars(card, 1, card.ability.extra.odds, self.key) return { vars = { card.ability.mult, card.ability.Xmult, numerator, denominator, card.ability.h_x_mult, card.ability.bonus, card.ability.h_dollars } } end, calculate = function(self, card, context)   if context.destroy_card and context.cardarea == G.play and context.destroy_card == card and SMODS.pseudorandom_probability(card, self.key, 1, card.ability.extra.odds) then card.glass_trigger = true return { remove = true } end end }
SMODS.Enhancement { key = "bonusXmultXwildXglassXsteelXstoneXgold", name = "Bonus Mult Wild Glass Steel Stone Gold Card", atlas = "enhancement", pos = { x = 7, y = 15 }, config = { bonus = 80, mult = 4, Xmult = 2, h_x_mult = 1.5, h_dollars = 3, extra = { odds = 4 } }, any_suit = true, shatters = true, replace_base_card = true,no_rank = true,always_scores = true, in_pool = function(self,args) return false end, loc_vars = function(self, info_queue, card) local numerator, denominator = SMODS.get_probability_vars(card, 1, card.ability.extra.odds, self.key) return { vars = { card.ability.bonus, card.ability.mult, card.ability.Xmult, numerator, denominator, card.ability.h_x_mult, card.ability.h_dollars } } end, calculate = function(self, card, context)   if context.destroy_card and context.cardarea == G.play and context.destroy_card == card and SMODS.pseudorandom_probability(card, self.key, 1, card.ability.extra.odds) then card.glass_trigger = true return { remove = true } end end }
SMODS.Enhancement { key = "bonusXlucky", name = "Bonus Lucky Card", atlas = "enhancement", pos = { x = 8, y = 1 }, config = { bonus = 30, extra = { mult = 20, dollars = 20, mult_odds = 5, dollars_odds = 15 } }, in_pool = function(self,args) return false end, loc_vars = function(self, info_queue, card) local mult_numerator, mult_denominator = SMODS.get_probability_vars(card, 1, card.ability.extra.mult_odds, self.key) local dollars_numerator, dollars_denominator = SMODS.get_probability_vars(card, 1, card.ability.extra.dollars_odds, self.key) return { vars = { card.ability.bonus, mult_numerator, dollars_numerator, card.ability.extra.mult, mult_denominator, card.ability.extra.dollars, dollars_denominator } } end, calculate = function(self, card, context) if context.main_scoring and context.cardarea == G.play then local ret = {} if SMODS.pseudorandom_probability(card, 'vremade_lucky_mult', 1, card.ability.extra.mult_odds) then card.lucky_trigger = true ret.mult = card.ability.extra.mult end if SMODS.pseudorandom_probability(card, 'vremade_lucky_money', 1, card.ability.extra.dollars_odds) then card.lucky_trigger = true ret.dollars = card.ability.extra.dollars end return ret end end }
SMODS.Enhancement { key = "multXlucky", name = "Mult Lucky Card", atlas = "enhancement", pos = { x = 8, y = 2 }, config = { mult = 4, extra = { mult = 20, dollars = 20, mult_odds = 5, dollars_odds = 15 } }, in_pool = function(self,args) return false end, loc_vars = function(self, info_queue, card) local mult_numerator, mult_denominator = SMODS.get_probability_vars(card, 1, card.ability.extra.mult_odds, self.key) local dollars_numerator, dollars_denominator = SMODS.get_probability_vars(card, 1, card.ability.extra.dollars_odds, self.key) return { vars = { card.ability.mult, mult_numerator, dollars_numerator, card.ability.extra.mult, mult_denominator, card.ability.extra.dollars, dollars_denominator } } end, calculate = function(self, card, context) if context.main_scoring and context.cardarea == G.play then local ret = {} if SMODS.pseudorandom_probability(card, 'vremade_lucky_mult', 1, card.ability.extra.mult_odds) then card.lucky_trigger = true ret.mult = card.ability.extra.mult end if SMODS.pseudorandom_probability(card, 'vremade_lucky_money', 1, card.ability.extra.dollars_odds) then card.lucky_trigger = true ret.dollars = card.ability.extra.dollars end return ret end end }
SMODS.Enhancement { key = "bonusXmultXlucky", name = "Bonus Mult Lucky Card", atlas = "enhancement", pos = { x = 8, y = 3 }, config = { bonus = 30, mult = 4, extra = { mult = 20, dollars = 20, mult_odds = 5, dollars_odds = 15 } }, in_pool = function(self,args) return false end, loc_vars = function(self, info_queue, card) local mult_numerator, mult_denominator = SMODS.get_probability_vars(card, 1, card.ability.extra.mult_odds, self.key) local dollars_numerator, dollars_denominator = SMODS.get_probability_vars(card, 1, card.ability.extra.dollars_odds, self.key) return { vars = { card.ability.bonus, card.ability.mult, mult_numerator, dollars_numerator, card.ability.extra.mult, mult_denominator, card.ability.extra.dollars, dollars_denominator } } end, calculate = function(self, card, context) if context.main_scoring and context.cardarea == G.play then local ret = {} if SMODS.pseudorandom_probability(card, 'vremade_lucky_mult', 1, card.ability.extra.mult_odds) then card.lucky_trigger = true ret.mult = card.ability.extra.mult end if SMODS.pseudorandom_probability(card, 'vremade_lucky_money', 1, card.ability.extra.dollars_odds) then card.lucky_trigger = true ret.dollars = card.ability.extra.dollars end return ret end end }
SMODS.Enhancement { key = "wildXlucky", name = "Wild Lucky Card", atlas = "enhancement", pos = { x = 8, y = 4 }, config = { extra = { mult = 20, dollars = 20, mult_odds = 5, dollars_odds = 15 } }, any_suit = true, in_pool = function(self,args) return false end, loc_vars = function(self, info_queue, card) local mult_numerator, mult_denominator = SMODS.get_probability_vars(card, 1, card.ability.extra.mult_odds, self.key) local dollars_numerator, dollars_denominator = SMODS.get_probability_vars(card, 1, card.ability.extra.dollars_odds, self.key) return { vars = { mult_numerator, dollars_numerator, card.ability.extra.mult, mult_denominator, card.ability.extra.dollars, dollars_denominator } } end, calculate = function(self, card, context) if context.main_scoring and context.cardarea == G.play then local ret = {} if SMODS.pseudorandom_probability(card, 'vremade_lucky_mult', 1, card.ability.extra.mult_odds) then card.lucky_trigger = true ret.mult = card.ability.extra.mult end if SMODS.pseudorandom_probability(card, 'vremade_lucky_money', 1, card.ability.extra.dollars_odds) then card.lucky_trigger = true ret.dollars = card.ability.extra.dollars end return ret end end }
SMODS.Enhancement { key = "bonusXwildXlucky", name = "Bonus Wild Lucky Card", atlas = "enhancement", pos = { x = 8, y = 5 }, config = { bonus = 30, extra = { mult = 20, dollars = 20, mult_odds = 5, dollars_odds = 15 } }, any_suit = true, in_pool = function(self,args) return false end, loc_vars = function(self, info_queue, card) local mult_numerator, mult_denominator = SMODS.get_probability_vars(card, 1, card.ability.extra.mult_odds, self.key) local dollars_numerator, dollars_denominator = SMODS.get_probability_vars(card, 1, card.ability.extra.dollars_odds, self.key) return { vars = { card.ability.bonus, mult_numerator, dollars_numerator, card.ability.extra.mult, mult_denominator, card.ability.extra.dollars, dollars_denominator } } end, calculate = function(self, card, context) if context.main_scoring and context.cardarea == G.play then local ret = {} if SMODS.pseudorandom_probability(card, 'vremade_lucky_mult', 1, card.ability.extra.mult_odds) then card.lucky_trigger = true ret.mult = card.ability.extra.mult end if SMODS.pseudorandom_probability(card, 'vremade_lucky_money', 1, card.ability.extra.dollars_odds) then card.lucky_trigger = true ret.dollars = card.ability.extra.dollars end return ret end end }
SMODS.Enhancement { key = "multXwildXlucky", name = "Mult Wild Lucky Card", atlas = "enhancement", pos = { x = 8, y = 6 }, config = { mult = 4, extra = { mult = 20, dollars = 20, mult_odds = 5, dollars_odds = 15 } }, any_suit = true, in_pool = function(self,args) return false end, loc_vars = function(self, info_queue, card) local mult_numerator, mult_denominator = SMODS.get_probability_vars(card, 1, card.ability.extra.mult_odds, self.key) local dollars_numerator, dollars_denominator = SMODS.get_probability_vars(card, 1, card.ability.extra.dollars_odds, self.key) return { vars = { card.ability.mult, mult_numerator, dollars_numerator, card.ability.extra.mult, mult_denominator, card.ability.extra.dollars, dollars_denominator } } end, calculate = function(self, card, context) if context.main_scoring and context.cardarea == G.play then local ret = {} if SMODS.pseudorandom_probability(card, 'vremade_lucky_mult', 1, card.ability.extra.mult_odds) then card.lucky_trigger = true ret.mult = card.ability.extra.mult end if SMODS.pseudorandom_probability(card, 'vremade_lucky_money', 1, card.ability.extra.dollars_odds) then card.lucky_trigger = true ret.dollars = card.ability.extra.dollars end return ret end end }
SMODS.Enhancement { key = "bonusXmultXwildXlucky", name = "Bonus Mult Wild Lucky Card", atlas = "enhancement", pos = { x = 8, y = 7 }, config = { bonus = 30, mult = 4, extra = { mult = 20, dollars = 20, mult_odds = 5, dollars_odds = 15 } }, any_suit = true, in_pool = function(self,args) return false end, loc_vars = function(self, info_queue, card) local mult_numerator, mult_denominator = SMODS.get_probability_vars(card, 1, card.ability.extra.mult_odds, self.key) local dollars_numerator, dollars_denominator = SMODS.get_probability_vars(card, 1, card.ability.extra.dollars_odds, self.key) return { vars = { card.ability.bonus, card.ability.mult, mult_numerator, dollars_numerator, card.ability.extra.mult, mult_denominator, card.ability.extra.dollars, dollars_denominator } } end, calculate = function(self, card, context) if context.main_scoring and context.cardarea == G.play then local ret = {} if SMODS.pseudorandom_probability(card, 'vremade_lucky_mult', 1, card.ability.extra.mult_odds) then card.lucky_trigger = true ret.mult = card.ability.extra.mult end if SMODS.pseudorandom_probability(card, 'vremade_lucky_money', 1, card.ability.extra.dollars_odds) then card.lucky_trigger = true ret.dollars = card.ability.extra.dollars end return ret end end }
SMODS.Enhancement { key = "glassXlucky", name = "Glass Lucky Card", atlas = "enhancement", pos = { x = 8, y = 8 }, config = { Xmult = 2, extra = { odds = 4, mult = 20, dollars = 20, mult_odds = 5, dollars_odds = 15 } }, shatters = true, in_pool = function(self,args) return false end, loc_vars = function(self, info_queue, card) local numerator, denominator = SMODS.get_probability_vars(card, 1, card.ability.extra.odds, self.key) local mult_numerator, mult_denominator = SMODS.get_probability_vars(card, 1, card.ability.extra.mult_odds, self.key) local dollars_numerator, dollars_denominator = SMODS.get_probability_vars(card, 1, card.ability.extra.dollars_odds, self.key) return { vars = { card.ability.Xmult, numerator, denominator, mult_numerator, dollars_numerator, card.ability.extra.mult, mult_denominator, card.ability.extra.dollars, dollars_denominator } } end, calculate = function(self, card, context)  if context.main_scoring and context.cardarea == G.play then local ret = {} if SMODS.pseudorandom_probability(card, 'vremade_lucky_mult', 1, card.ability.extra.mult_odds) then card.lucky_trigger = true ret.mult = card.ability.extra.mult end if SMODS.pseudorandom_probability(card, 'vremade_lucky_money', 1, card.ability.extra.dollars_odds) then card.lucky_trigger = true ret.dollars = card.ability.extra.dollars end return ret end  if context.destroy_card and context.cardarea == G.play and context.destroy_card == card and SMODS.pseudorandom_probability(card, self.key, 1, card.ability.extra.odds) then card.glass_trigger = true return { remove = true } end end }
SMODS.Enhancement { key = "bonusXglassXlucky", name = "Bonus Glass Lucky Card", atlas = "enhancement", pos = { x = 8, y = 9 }, config = { bonus = 30, Xmult = 2, extra = { odds = 4, mult = 20, dollars = 20, mult_odds = 5, dollars_odds = 15 } }, shatters = true, in_pool = function(self,args) return false end, loc_vars = function(self, info_queue, card) local numerator, denominator = SMODS.get_probability_vars(card, 1, card.ability.extra.odds, self.key) local mult_numerator, mult_denominator = SMODS.get_probability_vars(card, 1, card.ability.extra.mult_odds, self.key) local dollars_numerator, dollars_denominator = SMODS.get_probability_vars(card, 1, card.ability.extra.dollars_odds, self.key) return { vars = { card.ability.bonus, card.ability.Xmult, numerator, denominator, mult_numerator, dollars_numerator, card.ability.extra.mult, mult_denominator, card.ability.extra.dollars, dollars_denominator } } end, calculate = function(self, card, context)  if context.main_scoring and context.cardarea == G.play then local ret = {} if SMODS.pseudorandom_probability(card, 'vremade_lucky_mult', 1, card.ability.extra.mult_odds) then card.lucky_trigger = true ret.mult = card.ability.extra.mult end if SMODS.pseudorandom_probability(card, 'vremade_lucky_money', 1, card.ability.extra.dollars_odds) then card.lucky_trigger = true ret.dollars = card.ability.extra.dollars end return ret end  if context.destroy_card and context.cardarea == G.play and context.destroy_card == card and SMODS.pseudorandom_probability(card, self.key, 1, card.ability.extra.odds) then card.glass_trigger = true return { remove = true } end end }
SMODS.Enhancement { key = "multXglassXlucky", name = "Mult Glass Lucky Card", atlas = "enhancement", pos = { x = 8, y = 10 }, config = { mult = 4, Xmult = 2, extra = { odds = 4, mult = 20, dollars = 20, mult_odds = 5, dollars_odds = 15 } }, shatters = true, in_pool = function(self,args) return false end, loc_vars = function(self, info_queue, card) local numerator, denominator = SMODS.get_probability_vars(card, 1, card.ability.extra.odds, self.key) local mult_numerator, mult_denominator = SMODS.get_probability_vars(card, 1, card.ability.extra.mult_odds, self.key) local dollars_numerator, dollars_denominator = SMODS.get_probability_vars(card, 1, card.ability.extra.dollars_odds, self.key) return { vars = { card.ability.mult, card.ability.Xmult, numerator, denominator, mult_numerator, dollars_numerator, card.ability.extra.mult, mult_denominator, card.ability.extra.dollars, dollars_denominator } } end, calculate = function(self, card, context)  if context.main_scoring and context.cardarea == G.play then local ret = {} if SMODS.pseudorandom_probability(card, 'vremade_lucky_mult', 1, card.ability.extra.mult_odds) then card.lucky_trigger = true ret.mult = card.ability.extra.mult end if SMODS.pseudorandom_probability(card, 'vremade_lucky_money', 1, card.ability.extra.dollars_odds) then card.lucky_trigger = true ret.dollars = card.ability.extra.dollars end return ret end  if context.destroy_card and context.cardarea == G.play and context.destroy_card == card and SMODS.pseudorandom_probability(card, self.key, 1, card.ability.extra.odds) then card.glass_trigger = true return { remove = true } end end }
SMODS.Enhancement { key = "bonusXmultXglassXlucky", name = "Bonus Mult Glass Lucky Card", atlas = "enhancement", pos = { x = 8, y = 11 }, config = { bonus = 30, mult = 4, Xmult = 2, extra = { odds = 4, mult = 20, dollars = 20, mult_odds = 5, dollars_odds = 15 } }, shatters = true, in_pool = function(self,args) return false end, loc_vars = function(self, info_queue, card) local numerator, denominator = SMODS.get_probability_vars(card, 1, card.ability.extra.odds, self.key) local mult_numerator, mult_denominator = SMODS.get_probability_vars(card, 1, card.ability.extra.mult_odds, self.key) local dollars_numerator, dollars_denominator = SMODS.get_probability_vars(card, 1, card.ability.extra.dollars_odds, self.key) return { vars = { card.ability.bonus, card.ability.mult, card.ability.Xmult, numerator, denominator, mult_numerator, dollars_numerator, card.ability.extra.mult, mult_denominator, card.ability.extra.dollars, dollars_denominator } } end, calculate = function(self, card, context)  if context.main_scoring and context.cardarea == G.play then local ret = {} if SMODS.pseudorandom_probability(card, 'vremade_lucky_mult', 1, card.ability.extra.mult_odds) then card.lucky_trigger = true ret.mult = card.ability.extra.mult end if SMODS.pseudorandom_probability(card, 'vremade_lucky_money', 1, card.ability.extra.dollars_odds) then card.lucky_trigger = true ret.dollars = card.ability.extra.dollars end return ret end  if context.destroy_card and context.cardarea == G.play and context.destroy_card == card and SMODS.pseudorandom_probability(card, self.key, 1, card.ability.extra.odds) then card.glass_trigger = true return { remove = true } end end }
SMODS.Enhancement { key = "wildXglassXlucky", name = "Wild Glass Lucky Card", atlas = "enhancement", pos = { x = 8, y = 12 }, config = { Xmult = 2, extra = { odds = 4, mult = 20, dollars = 20, mult_odds = 5, dollars_odds = 15 } }, any_suit = true, shatters = true, in_pool = function(self,args) return false end, loc_vars = function(self, info_queue, card) local numerator, denominator = SMODS.get_probability_vars(card, 1, card.ability.extra.odds, self.key) local mult_numerator, mult_denominator = SMODS.get_probability_vars(card, 1, card.ability.extra.mult_odds, self.key) local dollars_numerator, dollars_denominator = SMODS.get_probability_vars(card, 1, card.ability.extra.dollars_odds, self.key) return { vars = { card.ability.Xmult, numerator, denominator, mult_numerator, dollars_numerator, card.ability.extra.mult, mult_denominator, card.ability.extra.dollars, dollars_denominator } } end, calculate = function(self, card, context)  if context.main_scoring and context.cardarea == G.play then local ret = {} if SMODS.pseudorandom_probability(card, 'vremade_lucky_mult', 1, card.ability.extra.mult_odds) then card.lucky_trigger = true ret.mult = card.ability.extra.mult end if SMODS.pseudorandom_probability(card, 'vremade_lucky_money', 1, card.ability.extra.dollars_odds) then card.lucky_trigger = true ret.dollars = card.ability.extra.dollars end return ret end  if context.destroy_card and context.cardarea == G.play and context.destroy_card == card and SMODS.pseudorandom_probability(card, self.key, 1, card.ability.extra.odds) then card.glass_trigger = true return { remove = true } end end }
SMODS.Enhancement { key = "bonusXwildXglassXlucky", name = "Bonus Wild Glass Lucky Card", atlas = "enhancement", pos = { x = 8, y = 13 }, config = { bonus = 30, Xmult = 2, extra = { odds = 4, mult = 20, dollars = 20, mult_odds = 5, dollars_odds = 15 } }, any_suit = true, shatters = true, in_pool = function(self,args) return false end, loc_vars = function(self, info_queue, card) local numerator, denominator = SMODS.get_probability_vars(card, 1, card.ability.extra.odds, self.key) local mult_numerator, mult_denominator = SMODS.get_probability_vars(card, 1, card.ability.extra.mult_odds, self.key) local dollars_numerator, dollars_denominator = SMODS.get_probability_vars(card, 1, card.ability.extra.dollars_odds, self.key) return { vars = { card.ability.bonus, card.ability.Xmult, numerator, denominator, mult_numerator, dollars_numerator, card.ability.extra.mult, mult_denominator, card.ability.extra.dollars, dollars_denominator } } end, calculate = function(self, card, context)  if context.main_scoring and context.cardarea == G.play then local ret = {} if SMODS.pseudorandom_probability(card, 'vremade_lucky_mult', 1, card.ability.extra.mult_odds) then card.lucky_trigger = true ret.mult = card.ability.extra.mult end if SMODS.pseudorandom_probability(card, 'vremade_lucky_money', 1, card.ability.extra.dollars_odds) then card.lucky_trigger = true ret.dollars = card.ability.extra.dollars end return ret end  if context.destroy_card and context.cardarea == G.play and context.destroy_card == card and SMODS.pseudorandom_probability(card, self.key, 1, card.ability.extra.odds) then card.glass_trigger = true return { remove = true } end end }
SMODS.Enhancement { key = "multXwildXglassXlucky", name = "Mult Wild Glass Lucky Card", atlas = "enhancement", pos = { x = 8, y = 14 }, config = { mult = 4, Xmult = 2, extra = { odds = 4, mult = 20, dollars = 20, mult_odds = 5, dollars_odds = 15 } }, any_suit = true, shatters = true, in_pool = function(self,args) return false end, loc_vars = function(self, info_queue, card) local numerator, denominator = SMODS.get_probability_vars(card, 1, card.ability.extra.odds, self.key) local mult_numerator, mult_denominator = SMODS.get_probability_vars(card, 1, card.ability.extra.mult_odds, self.key) local dollars_numerator, dollars_denominator = SMODS.get_probability_vars(card, 1, card.ability.extra.dollars_odds, self.key) return { vars = { card.ability.mult, card.ability.Xmult, numerator, denominator, mult_numerator, dollars_numerator, card.ability.extra.mult, mult_denominator, card.ability.extra.dollars, dollars_denominator } } end, calculate = function(self, card, context)  if context.main_scoring and context.cardarea == G.play then local ret = {} if SMODS.pseudorandom_probability(card, 'vremade_lucky_mult', 1, card.ability.extra.mult_odds) then card.lucky_trigger = true ret.mult = card.ability.extra.mult end if SMODS.pseudorandom_probability(card, 'vremade_lucky_money', 1, card.ability.extra.dollars_odds) then card.lucky_trigger = true ret.dollars = card.ability.extra.dollars end return ret end  if context.destroy_card and context.cardarea == G.play and context.destroy_card == card and SMODS.pseudorandom_probability(card, self.key, 1, card.ability.extra.odds) then card.glass_trigger = true return { remove = true } end end }
SMODS.Enhancement { key = "bonusXmultXwildXglassXlucky", name = "Bonus Mult Wild Glass Lucky Card", atlas = "enhancement", pos = { x = 8, y = 15 }, config = { bonus = 30, mult = 4, Xmult = 2, extra = { odds = 4, mult = 20, dollars = 20, mult_odds = 5, dollars_odds = 15 } }, any_suit = true, shatters = true, in_pool = function(self,args) return false end, loc_vars = function(self, info_queue, card) local numerator, denominator = SMODS.get_probability_vars(card, 1, card.ability.extra.odds, self.key) local mult_numerator, mult_denominator = SMODS.get_probability_vars(card, 1, card.ability.extra.mult_odds, self.key) local dollars_numerator, dollars_denominator = SMODS.get_probability_vars(card, 1, card.ability.extra.dollars_odds, self.key) return { vars = { card.ability.bonus, card.ability.mult, card.ability.Xmult, numerator, denominator, mult_numerator, dollars_numerator, card.ability.extra.mult, mult_denominator, card.ability.extra.dollars, dollars_denominator } } end, calculate = function(self, card, context)  if context.main_scoring and context.cardarea == G.play then local ret = {} if SMODS.pseudorandom_probability(card, 'vremade_lucky_mult', 1, card.ability.extra.mult_odds) then card.lucky_trigger = true ret.mult = card.ability.extra.mult end if SMODS.pseudorandom_probability(card, 'vremade_lucky_money', 1, card.ability.extra.dollars_odds) then card.lucky_trigger = true ret.dollars = card.ability.extra.dollars end return ret end  if context.destroy_card and context.cardarea == G.play and context.destroy_card == card and SMODS.pseudorandom_probability(card, self.key, 1, card.ability.extra.odds) then card.glass_trigger = true return { remove = true } end end }
SMODS.Enhancement { key = "steelXlucky", name = "Steel Lucky Card", atlas = "enhancement", pos = { x = 9, y = 0 }, config = { h_x_mult = 1.5, extra = { mult = 20, dollars = 20, mult_odds = 5, dollars_odds = 15 } }, in_pool = function(self,args) return false end, loc_vars = function(self, info_queue, card) local mult_numerator, mult_denominator = SMODS.get_probability_vars(card, 1, card.ability.extra.mult_odds, self.key) local dollars_numerator, dollars_denominator = SMODS.get_probability_vars(card, 1, card.ability.extra.dollars_odds, self.key) return { vars = { card.ability.h_x_mult, mult_numerator, dollars_numerator, card.ability.extra.mult, mult_denominator, card.ability.extra.dollars, dollars_denominator } } end, calculate = function(self, card, context) if context.main_scoring and context.cardarea == G.play then local ret = {} if SMODS.pseudorandom_probability(card, 'vremade_lucky_mult', 1, card.ability.extra.mult_odds) then card.lucky_trigger = true ret.mult = card.ability.extra.mult end if SMODS.pseudorandom_probability(card, 'vremade_lucky_money', 1, card.ability.extra.dollars_odds) then card.lucky_trigger = true ret.dollars = card.ability.extra.dollars end return ret end end }
SMODS.Enhancement { key = "bonusXsteelXlucky", name = "Bonus Steel Lucky Card", atlas = "enhancement", pos = { x = 9, y = 1 }, config = { bonus = 30, h_x_mult = 1.5, extra = { mult = 20, dollars = 20, mult_odds = 5, dollars_odds = 15 } }, in_pool = function(self,args) return false end, loc_vars = function(self, info_queue, card) local mult_numerator, mult_denominator = SMODS.get_probability_vars(card, 1, card.ability.extra.mult_odds, self.key) local dollars_numerator, dollars_denominator = SMODS.get_probability_vars(card, 1, card.ability.extra.dollars_odds, self.key) return { vars = { card.ability.bonus, card.ability.h_x_mult, mult_numerator, dollars_numerator, card.ability.extra.mult, mult_denominator, card.ability.extra.dollars, dollars_denominator } } end, calculate = function(self, card, context) if context.main_scoring and context.cardarea == G.play then local ret = {} if SMODS.pseudorandom_probability(card, 'vremade_lucky_mult', 1, card.ability.extra.mult_odds) then card.lucky_trigger = true ret.mult = card.ability.extra.mult end if SMODS.pseudorandom_probability(card, 'vremade_lucky_money', 1, card.ability.extra.dollars_odds) then card.lucky_trigger = true ret.dollars = card.ability.extra.dollars end return ret end end }
SMODS.Enhancement { key = "multXsteelXlucky", name = "Mult Steel Lucky Card", atlas = "enhancement", pos = { x = 9, y = 2 }, config = { mult = 4, h_x_mult = 1.5, extra = { mult = 20, dollars = 20, mult_odds = 5, dollars_odds = 15 } }, in_pool = function(self,args) return false end, loc_vars = function(self, info_queue, card) local mult_numerator, mult_denominator = SMODS.get_probability_vars(card, 1, card.ability.extra.mult_odds, self.key) local dollars_numerator, dollars_denominator = SMODS.get_probability_vars(card, 1, card.ability.extra.dollars_odds, self.key) return { vars = { card.ability.mult, card.ability.h_x_mult, mult_numerator, dollars_numerator, card.ability.extra.mult, mult_denominator, card.ability.extra.dollars, dollars_denominator } } end, calculate = function(self, card, context) if context.main_scoring and context.cardarea == G.play then local ret = {} if SMODS.pseudorandom_probability(card, 'vremade_lucky_mult', 1, card.ability.extra.mult_odds) then card.lucky_trigger = true ret.mult = card.ability.extra.mult end if SMODS.pseudorandom_probability(card, 'vremade_lucky_money', 1, card.ability.extra.dollars_odds) then card.lucky_trigger = true ret.dollars = card.ability.extra.dollars end return ret end end }
SMODS.Enhancement { key = "bonusXmultXsteelXlucky", name = "Bonus Mult Steel Lucky Card", atlas = "enhancement", pos = { x = 9, y = 3 }, config = { bonus = 30, mult = 4, h_x_mult = 1.5, extra = { mult = 20, dollars = 20, mult_odds = 5, dollars_odds = 15 } }, in_pool = function(self,args) return false end, loc_vars = function(self, info_queue, card) local mult_numerator, mult_denominator = SMODS.get_probability_vars(card, 1, card.ability.extra.mult_odds, self.key) local dollars_numerator, dollars_denominator = SMODS.get_probability_vars(card, 1, card.ability.extra.dollars_odds, self.key) return { vars = { card.ability.bonus, card.ability.mult, card.ability.h_x_mult, mult_numerator, dollars_numerator, card.ability.extra.mult, mult_denominator, card.ability.extra.dollars, dollars_denominator } } end, calculate = function(self, card, context) if context.main_scoring and context.cardarea == G.play then local ret = {} if SMODS.pseudorandom_probability(card, 'vremade_lucky_mult', 1, card.ability.extra.mult_odds) then card.lucky_trigger = true ret.mult = card.ability.extra.mult end if SMODS.pseudorandom_probability(card, 'vremade_lucky_money', 1, card.ability.extra.dollars_odds) then card.lucky_trigger = true ret.dollars = card.ability.extra.dollars end return ret end end }
SMODS.Enhancement { key = "wildXsteelXlucky", name = "Wild Steel Lucky Card", atlas = "enhancement", pos = { x = 9, y = 4 }, config = { h_x_mult = 1.5, extra = { mult = 20, dollars = 20, mult_odds = 5, dollars_odds = 15 } }, any_suit = true, in_pool = function(self,args) return false end, loc_vars = function(self, info_queue, card) local mult_numerator, mult_denominator = SMODS.get_probability_vars(card, 1, card.ability.extra.mult_odds, self.key) local dollars_numerator, dollars_denominator = SMODS.get_probability_vars(card, 1, card.ability.extra.dollars_odds, self.key) return { vars = { card.ability.h_x_mult, mult_numerator, dollars_numerator, card.ability.extra.mult, mult_denominator, card.ability.extra.dollars, dollars_denominator } } end, calculate = function(self, card, context) if context.main_scoring and context.cardarea == G.play then local ret = {} if SMODS.pseudorandom_probability(card, 'vremade_lucky_mult', 1, card.ability.extra.mult_odds) then card.lucky_trigger = true ret.mult = card.ability.extra.mult end if SMODS.pseudorandom_probability(card, 'vremade_lucky_money', 1, card.ability.extra.dollars_odds) then card.lucky_trigger = true ret.dollars = card.ability.extra.dollars end return ret end end }
SMODS.Enhancement { key = "bonusXwildXsteelXlucky", name = "Bonus Wild Steel Lucky Card", atlas = "enhancement", pos = { x = 9, y = 5 }, config = { bonus = 30, h_x_mult = 1.5, extra = { mult = 20, dollars = 20, mult_odds = 5, dollars_odds = 15 } }, any_suit = true, in_pool = function(self,args) return false end, loc_vars = function(self, info_queue, card) local mult_numerator, mult_denominator = SMODS.get_probability_vars(card, 1, card.ability.extra.mult_odds, self.key) local dollars_numerator, dollars_denominator = SMODS.get_probability_vars(card, 1, card.ability.extra.dollars_odds, self.key) return { vars = { card.ability.bonus, card.ability.h_x_mult, mult_numerator, dollars_numerator, card.ability.extra.mult, mult_denominator, card.ability.extra.dollars, dollars_denominator } } end, calculate = function(self, card, context) if context.main_scoring and context.cardarea == G.play then local ret = {} if SMODS.pseudorandom_probability(card, 'vremade_lucky_mult', 1, card.ability.extra.mult_odds) then card.lucky_trigger = true ret.mult = card.ability.extra.mult end if SMODS.pseudorandom_probability(card, 'vremade_lucky_money', 1, card.ability.extra.dollars_odds) then card.lucky_trigger = true ret.dollars = card.ability.extra.dollars end return ret end end }
SMODS.Enhancement { key = "multXwildXsteelXlucky", name = "Mult Wild Steel Lucky Card", atlas = "enhancement", pos = { x = 9, y = 6 }, config = { mult = 4, h_x_mult = 1.5, extra = { mult = 20, dollars = 20, mult_odds = 5, dollars_odds = 15 } }, any_suit = true, in_pool = function(self,args) return false end, loc_vars = function(self, info_queue, card) local mult_numerator, mult_denominator = SMODS.get_probability_vars(card, 1, card.ability.extra.mult_odds, self.key) local dollars_numerator, dollars_denominator = SMODS.get_probability_vars(card, 1, card.ability.extra.dollars_odds, self.key) return { vars = { card.ability.mult, card.ability.h_x_mult, mult_numerator, dollars_numerator, card.ability.extra.mult, mult_denominator, card.ability.extra.dollars, dollars_denominator } } end, calculate = function(self, card, context) if context.main_scoring and context.cardarea == G.play then local ret = {} if SMODS.pseudorandom_probability(card, 'vremade_lucky_mult', 1, card.ability.extra.mult_odds) then card.lucky_trigger = true ret.mult = card.ability.extra.mult end if SMODS.pseudorandom_probability(card, 'vremade_lucky_money', 1, card.ability.extra.dollars_odds) then card.lucky_trigger = true ret.dollars = card.ability.extra.dollars end return ret end end }
SMODS.Enhancement { key = "bonusXmultXwildXsteelXlucky", name = "Bonus Mult Wild Steel Lucky Card", atlas = "enhancement", pos = { x = 9, y = 7 }, config = { bonus = 30, mult = 4, h_x_mult = 1.5, extra = { mult = 20, dollars = 20, mult_odds = 5, dollars_odds = 15 } }, any_suit = true, in_pool = function(self,args) return false end, loc_vars = function(self, info_queue, card) local mult_numerator, mult_denominator = SMODS.get_probability_vars(card, 1, card.ability.extra.mult_odds, self.key) local dollars_numerator, dollars_denominator = SMODS.get_probability_vars(card, 1, card.ability.extra.dollars_odds, self.key) return { vars = { card.ability.bonus, card.ability.mult, card.ability.h_x_mult, mult_numerator, dollars_numerator, card.ability.extra.mult, mult_denominator, card.ability.extra.dollars, dollars_denominator } } end, calculate = function(self, card, context) if context.main_scoring and context.cardarea == G.play then local ret = {} if SMODS.pseudorandom_probability(card, 'vremade_lucky_mult', 1, card.ability.extra.mult_odds) then card.lucky_trigger = true ret.mult = card.ability.extra.mult end if SMODS.pseudorandom_probability(card, 'vremade_lucky_money', 1, card.ability.extra.dollars_odds) then card.lucky_trigger = true ret.dollars = card.ability.extra.dollars end return ret end end }
SMODS.Enhancement { key = "glassXsteelXlucky", name = "Glass Steel Lucky Card", atlas = "enhancement", pos = { x = 9, y = 8 }, config = { Xmult = 2, h_x_mult = 1.5, extra = { odds = 4, mult = 20, dollars = 20, mult_odds = 5, dollars_odds = 15 } }, shatters = true, in_pool = function(self,args) return false end, loc_vars = function(self, info_queue, card) local numerator, denominator = SMODS.get_probability_vars(card, 1, card.ability.extra.odds, self.key) local mult_numerator, mult_denominator = SMODS.get_probability_vars(card, 1, card.ability.extra.mult_odds, self.key) local dollars_numerator, dollars_denominator = SMODS.get_probability_vars(card, 1, card.ability.extra.dollars_odds, self.key) return { vars = { card.ability.Xmult, numerator, denominator, card.ability.h_x_mult, mult_numerator, dollars_numerator, card.ability.extra.mult, mult_denominator, card.ability.extra.dollars, dollars_denominator } } end, calculate = function(self, card, context)  if context.main_scoring and context.cardarea == G.play then local ret = {} if SMODS.pseudorandom_probability(card, 'vremade_lucky_mult', 1, card.ability.extra.mult_odds) then card.lucky_trigger = true ret.mult = card.ability.extra.mult end if SMODS.pseudorandom_probability(card, 'vremade_lucky_money', 1, card.ability.extra.dollars_odds) then card.lucky_trigger = true ret.dollars = card.ability.extra.dollars end return ret end  if context.destroy_card and context.cardarea == G.play and context.destroy_card == card and SMODS.pseudorandom_probability(card, self.key, 1, card.ability.extra.odds) then card.glass_trigger = true return { remove = true } end end }
SMODS.Enhancement { key = "bonusXglassXsteelXlucky", name = "Bonus Glass Steel Lucky Card", atlas = "enhancement", pos = { x = 9, y = 9 }, config = { bonus = 30, Xmult = 2, h_x_mult = 1.5, extra = { odds = 4, mult = 20, dollars = 20, mult_odds = 5, dollars_odds = 15 } }, shatters = true, in_pool = function(self,args) return false end, loc_vars = function(self, info_queue, card) local numerator, denominator = SMODS.get_probability_vars(card, 1, card.ability.extra.odds, self.key) local mult_numerator, mult_denominator = SMODS.get_probability_vars(card, 1, card.ability.extra.mult_odds, self.key) local dollars_numerator, dollars_denominator = SMODS.get_probability_vars(card, 1, card.ability.extra.dollars_odds, self.key) return { vars = { card.ability.bonus, card.ability.Xmult, numerator, denominator, card.ability.h_x_mult, mult_numerator, dollars_numerator, card.ability.extra.mult, mult_denominator, card.ability.extra.dollars, dollars_denominator } } end, calculate = function(self, card, context)  if context.main_scoring and context.cardarea == G.play then local ret = {} if SMODS.pseudorandom_probability(card, 'vremade_lucky_mult', 1, card.ability.extra.mult_odds) then card.lucky_trigger = true ret.mult = card.ability.extra.mult end if SMODS.pseudorandom_probability(card, 'vremade_lucky_money', 1, card.ability.extra.dollars_odds) then card.lucky_trigger = true ret.dollars = card.ability.extra.dollars end return ret end  if context.destroy_card and context.cardarea == G.play and context.destroy_card == card and SMODS.pseudorandom_probability(card, self.key, 1, card.ability.extra.odds) then card.glass_trigger = true return { remove = true } end end }
SMODS.Enhancement { key = "multXglassXsteelXlucky", name = "Mult Glass Steel Lucky Card", atlas = "enhancement", pos = { x = 9, y = 10 }, config = { mult = 4, Xmult = 2, h_x_mult = 1.5, extra = { odds = 4, mult = 20, dollars = 20, mult_odds = 5, dollars_odds = 15 } }, shatters = true, in_pool = function(self,args) return false end, loc_vars = function(self, info_queue, card) local numerator, denominator = SMODS.get_probability_vars(card, 1, card.ability.extra.odds, self.key) local mult_numerator, mult_denominator = SMODS.get_probability_vars(card, 1, card.ability.extra.mult_odds, self.key) local dollars_numerator, dollars_denominator = SMODS.get_probability_vars(card, 1, card.ability.extra.dollars_odds, self.key) return { vars = { card.ability.mult, card.ability.Xmult, numerator, denominator, card.ability.h_x_mult, mult_numerator, dollars_numerator, card.ability.extra.mult, mult_denominator, card.ability.extra.dollars, dollars_denominator } } end, calculate = function(self, card, context)  if context.main_scoring and context.cardarea == G.play then local ret = {} if SMODS.pseudorandom_probability(card, 'vremade_lucky_mult', 1, card.ability.extra.mult_odds) then card.lucky_trigger = true ret.mult = card.ability.extra.mult end if SMODS.pseudorandom_probability(card, 'vremade_lucky_money', 1, card.ability.extra.dollars_odds) then card.lucky_trigger = true ret.dollars = card.ability.extra.dollars end return ret end  if context.destroy_card and context.cardarea == G.play and context.destroy_card == card and SMODS.pseudorandom_probability(card, self.key, 1, card.ability.extra.odds) then card.glass_trigger = true return { remove = true } end end }
SMODS.Enhancement { key = "bonusXmultXglassXsteelXlucky", name = "Bonus Mult Glass Steel Lucky Card", atlas = "enhancement", pos = { x = 9, y = 11 }, config = { bonus = 30, mult = 4, Xmult = 2, h_x_mult = 1.5, extra = { odds = 4, mult = 20, dollars = 20, mult_odds = 5, dollars_odds = 15 } }, shatters = true, in_pool = function(self,args) return false end, loc_vars = function(self, info_queue, card) local numerator, denominator = SMODS.get_probability_vars(card, 1, card.ability.extra.odds, self.key) local mult_numerator, mult_denominator = SMODS.get_probability_vars(card, 1, card.ability.extra.mult_odds, self.key) local dollars_numerator, dollars_denominator = SMODS.get_probability_vars(card, 1, card.ability.extra.dollars_odds, self.key) return { vars = { card.ability.bonus, card.ability.mult, card.ability.Xmult, numerator, denominator, card.ability.h_x_mult, mult_numerator, dollars_numerator, card.ability.extra.mult, mult_denominator, card.ability.extra.dollars, dollars_denominator } } end, calculate = function(self, card, context)  if context.main_scoring and context.cardarea == G.play then local ret = {} if SMODS.pseudorandom_probability(card, 'vremade_lucky_mult', 1, card.ability.extra.mult_odds) then card.lucky_trigger = true ret.mult = card.ability.extra.mult end if SMODS.pseudorandom_probability(card, 'vremade_lucky_money', 1, card.ability.extra.dollars_odds) then card.lucky_trigger = true ret.dollars = card.ability.extra.dollars end return ret end  if context.destroy_card and context.cardarea == G.play and context.destroy_card == card and SMODS.pseudorandom_probability(card, self.key, 1, card.ability.extra.odds) then card.glass_trigger = true return { remove = true } end end }
SMODS.Enhancement { key = "wildXglassXsteelXlucky", name = "Wild Glass Steel Lucky Card", atlas = "enhancement", pos = { x = 9, y = 12 }, config = { Xmult = 2, h_x_mult = 1.5, extra = { odds = 4, mult = 20, dollars = 20, mult_odds = 5, dollars_odds = 15 } }, any_suit = true, shatters = true, in_pool = function(self,args) return false end, loc_vars = function(self, info_queue, card) local numerator, denominator = SMODS.get_probability_vars(card, 1, card.ability.extra.odds, self.key) local mult_numerator, mult_denominator = SMODS.get_probability_vars(card, 1, card.ability.extra.mult_odds, self.key) local dollars_numerator, dollars_denominator = SMODS.get_probability_vars(card, 1, card.ability.extra.dollars_odds, self.key) return { vars = { card.ability.Xmult, numerator, denominator, card.ability.h_x_mult, mult_numerator, dollars_numerator, card.ability.extra.mult, mult_denominator, card.ability.extra.dollars, dollars_denominator } } end, calculate = function(self, card, context)  if context.main_scoring and context.cardarea == G.play then local ret = {} if SMODS.pseudorandom_probability(card, 'vremade_lucky_mult', 1, card.ability.extra.mult_odds) then card.lucky_trigger = true ret.mult = card.ability.extra.mult end if SMODS.pseudorandom_probability(card, 'vremade_lucky_money', 1, card.ability.extra.dollars_odds) then card.lucky_trigger = true ret.dollars = card.ability.extra.dollars end return ret end  if context.destroy_card and context.cardarea == G.play and context.destroy_card == card and SMODS.pseudorandom_probability(card, self.key, 1, card.ability.extra.odds) then card.glass_trigger = true return { remove = true } end end }
SMODS.Enhancement { key = "bonusXwildXglassXsteelXlucky", name = "Bonus Wild Glass Steel Lucky Card", atlas = "enhancement", pos = { x = 9, y = 13 }, config = { bonus = 30, Xmult = 2, h_x_mult = 1.5, extra = { odds = 4, mult = 20, dollars = 20, mult_odds = 5, dollars_odds = 15 } }, any_suit = true, shatters = true, in_pool = function(self,args) return false end, loc_vars = function(self, info_queue, card) local numerator, denominator = SMODS.get_probability_vars(card, 1, card.ability.extra.odds, self.key) local mult_numerator, mult_denominator = SMODS.get_probability_vars(card, 1, card.ability.extra.mult_odds, self.key) local dollars_numerator, dollars_denominator = SMODS.get_probability_vars(card, 1, card.ability.extra.dollars_odds, self.key) return { vars = { card.ability.bonus, card.ability.Xmult, numerator, denominator, card.ability.h_x_mult, mult_numerator, dollars_numerator, card.ability.extra.mult, mult_denominator, card.ability.extra.dollars, dollars_denominator } } end, calculate = function(self, card, context)  if context.main_scoring and context.cardarea == G.play then local ret = {} if SMODS.pseudorandom_probability(card, 'vremade_lucky_mult', 1, card.ability.extra.mult_odds) then card.lucky_trigger = true ret.mult = card.ability.extra.mult end if SMODS.pseudorandom_probability(card, 'vremade_lucky_money', 1, card.ability.extra.dollars_odds) then card.lucky_trigger = true ret.dollars = card.ability.extra.dollars end return ret end  if context.destroy_card and context.cardarea == G.play and context.destroy_card == card and SMODS.pseudorandom_probability(card, self.key, 1, card.ability.extra.odds) then card.glass_trigger = true return { remove = true } end end }
SMODS.Enhancement { key = "multXwildXglassXsteelXlucky", name = "Mult Wild Glass Steel Lucky Card", atlas = "enhancement", pos = { x = 9, y = 14 }, config = { mult = 4, Xmult = 2, h_x_mult = 1.5, extra = { odds = 4, mult = 20, dollars = 20, mult_odds = 5, dollars_odds = 15 } }, any_suit = true, shatters = true, in_pool = function(self,args) return false end, loc_vars = function(self, info_queue, card) local numerator, denominator = SMODS.get_probability_vars(card, 1, card.ability.extra.odds, self.key) local mult_numerator, mult_denominator = SMODS.get_probability_vars(card, 1, card.ability.extra.mult_odds, self.key) local dollars_numerator, dollars_denominator = SMODS.get_probability_vars(card, 1, card.ability.extra.dollars_odds, self.key) return { vars = { card.ability.mult, card.ability.Xmult, numerator, denominator, card.ability.h_x_mult, mult_numerator, dollars_numerator, card.ability.extra.mult, mult_denominator, card.ability.extra.dollars, dollars_denominator } } end, calculate = function(self, card, context)  if context.main_scoring and context.cardarea == G.play then local ret = {} if SMODS.pseudorandom_probability(card, 'vremade_lucky_mult', 1, card.ability.extra.mult_odds) then card.lucky_trigger = true ret.mult = card.ability.extra.mult end if SMODS.pseudorandom_probability(card, 'vremade_lucky_money', 1, card.ability.extra.dollars_odds) then card.lucky_trigger = true ret.dollars = card.ability.extra.dollars end return ret end  if context.destroy_card and context.cardarea == G.play and context.destroy_card == card and SMODS.pseudorandom_probability(card, self.key, 1, card.ability.extra.odds) then card.glass_trigger = true return { remove = true } end end }
SMODS.Enhancement { key = "bonusXmultXwildXglassXsteelXlucky", name = "Bonus Mult Wild Glass Steel Lucky Card", atlas = "enhancement", pos = { x = 9, y = 15 }, config = { bonus = 30, mult = 4, Xmult = 2, h_x_mult = 1.5, extra = { odds = 4, mult = 20, dollars = 20, mult_odds = 5, dollars_odds = 15 } }, any_suit = true, shatters = true, in_pool = function(self,args) return false end, loc_vars = function(self, info_queue, card) local numerator, denominator = SMODS.get_probability_vars(card, 1, card.ability.extra.odds, self.key) local mult_numerator, mult_denominator = SMODS.get_probability_vars(card, 1, card.ability.extra.mult_odds, self.key) local dollars_numerator, dollars_denominator = SMODS.get_probability_vars(card, 1, card.ability.extra.dollars_odds, self.key) return { vars = { card.ability.bonus, card.ability.mult, card.ability.Xmult, numerator, denominator, card.ability.h_x_mult, mult_numerator, dollars_numerator, card.ability.extra.mult, mult_denominator, card.ability.extra.dollars, dollars_denominator } } end, calculate = function(self, card, context)  if context.main_scoring and context.cardarea == G.play then local ret = {} if SMODS.pseudorandom_probability(card, 'vremade_lucky_mult', 1, card.ability.extra.mult_odds) then card.lucky_trigger = true ret.mult = card.ability.extra.mult end if SMODS.pseudorandom_probability(card, 'vremade_lucky_money', 1, card.ability.extra.dollars_odds) then card.lucky_trigger = true ret.dollars = card.ability.extra.dollars end return ret end  if context.destroy_card and context.cardarea == G.play and context.destroy_card == card and SMODS.pseudorandom_probability(card, self.key, 1, card.ability.extra.odds) then card.glass_trigger = true return { remove = true } end end }
SMODS.Enhancement { key = "stoneXlucky", name = "Stone Lucky Card", atlas = "enhancement", pos = { x = 10, y = 0 }, config = { bonus = 50, extra = { mult = 20, dollars = 20, mult_odds = 5, dollars_odds = 15 } }, replace_base_card = true,no_rank = true,no_suit = true,always_scores = true, in_pool = function(self,args) return false end, loc_vars = function(self, info_queue, card) local mult_numerator, mult_denominator = SMODS.get_probability_vars(card, 1, card.ability.extra.mult_odds, self.key) local dollars_numerator, dollars_denominator = SMODS.get_probability_vars(card, 1, card.ability.extra.dollars_odds, self.key) return { vars = { card.ability.bonus, mult_numerator, dollars_numerator, card.ability.extra.mult, mult_denominator, card.ability.extra.dollars, dollars_denominator } } end, calculate = function(self, card, context) if context.main_scoring and context.cardarea == G.play then local ret = {} if SMODS.pseudorandom_probability(card, 'vremade_lucky_mult', 1, card.ability.extra.mult_odds) then card.lucky_trigger = true ret.mult = card.ability.extra.mult end if SMODS.pseudorandom_probability(card, 'vremade_lucky_money', 1, card.ability.extra.dollars_odds) then card.lucky_trigger = true ret.dollars = card.ability.extra.dollars end return ret end end }
SMODS.Enhancement { key = "bonusXstoneXlucky", name = "Bonus Stone Lucky Card", atlas = "enhancement", pos = { x = 10, y = 1 }, config = { bonus = 80, extra = { mult = 20, dollars = 20, mult_odds = 5, dollars_odds = 15 } }, replace_base_card = true,no_rank = true,no_suit = true,always_scores = true, in_pool = function(self,args) return false end, loc_vars = function(self, info_queue, card) local mult_numerator, mult_denominator = SMODS.get_probability_vars(card, 1, card.ability.extra.mult_odds, self.key) local dollars_numerator, dollars_denominator = SMODS.get_probability_vars(card, 1, card.ability.extra.dollars_odds, self.key) return { vars = { card.ability.bonus, mult_numerator, dollars_numerator, card.ability.extra.mult, mult_denominator, card.ability.extra.dollars, dollars_denominator } } end, calculate = function(self, card, context) if context.main_scoring and context.cardarea == G.play then local ret = {} if SMODS.pseudorandom_probability(card, 'vremade_lucky_mult', 1, card.ability.extra.mult_odds) then card.lucky_trigger = true ret.mult = card.ability.extra.mult end if SMODS.pseudorandom_probability(card, 'vremade_lucky_money', 1, card.ability.extra.dollars_odds) then card.lucky_trigger = true ret.dollars = card.ability.extra.dollars end return ret end end }
SMODS.Enhancement { key = "multXstoneXlucky", name = "Mult Stone Lucky Card", atlas = "enhancement", pos = { x = 10, y = 2 }, config = { mult = 4, bonus = 50, extra = { mult = 20, dollars = 20, mult_odds = 5, dollars_odds = 15 } }, replace_base_card = true,no_rank = true,no_suit = true,always_scores = true, in_pool = function(self,args) return false end, loc_vars = function(self, info_queue, card) local mult_numerator, mult_denominator = SMODS.get_probability_vars(card, 1, card.ability.extra.mult_odds, self.key) local dollars_numerator, dollars_denominator = SMODS.get_probability_vars(card, 1, card.ability.extra.dollars_odds, self.key) return { vars = { card.ability.mult, card.ability.bonus, mult_numerator, dollars_numerator, card.ability.extra.mult, mult_denominator, card.ability.extra.dollars, dollars_denominator } } end, calculate = function(self, card, context) if context.main_scoring and context.cardarea == G.play then local ret = {} if SMODS.pseudorandom_probability(card, 'vremade_lucky_mult', 1, card.ability.extra.mult_odds) then card.lucky_trigger = true ret.mult = card.ability.extra.mult end if SMODS.pseudorandom_probability(card, 'vremade_lucky_money', 1, card.ability.extra.dollars_odds) then card.lucky_trigger = true ret.dollars = card.ability.extra.dollars end return ret end end }
SMODS.Enhancement { key = "bonusXmultXstoneXlucky", name = "Bonus Mult Stone Lucky Card", atlas = "enhancement", pos = { x = 10, y = 3 }, config = { bonus = 80, mult = 4, extra = { mult = 20, dollars = 20, mult_odds = 5, dollars_odds = 15 } }, replace_base_card = true,no_rank = true,no_suit = true,always_scores = true, in_pool = function(self,args) return false end, loc_vars = function(self, info_queue, card) local mult_numerator, mult_denominator = SMODS.get_probability_vars(card, 1, card.ability.extra.mult_odds, self.key) local dollars_numerator, dollars_denominator = SMODS.get_probability_vars(card, 1, card.ability.extra.dollars_odds, self.key) return { vars = { card.ability.bonus, card.ability.mult, mult_numerator, dollars_numerator, card.ability.extra.mult, mult_denominator, card.ability.extra.dollars, dollars_denominator } } end, calculate = function(self, card, context) if context.main_scoring and context.cardarea == G.play then local ret = {} if SMODS.pseudorandom_probability(card, 'vremade_lucky_mult', 1, card.ability.extra.mult_odds) then card.lucky_trigger = true ret.mult = card.ability.extra.mult end if SMODS.pseudorandom_probability(card, 'vremade_lucky_money', 1, card.ability.extra.dollars_odds) then card.lucky_trigger = true ret.dollars = card.ability.extra.dollars end return ret end end }
SMODS.Enhancement { key = "wildXstoneXlucky", name = "Wild Stone Lucky Card", atlas = "enhancement", pos = { x = 10, y = 4 }, config = { bonus = 50, extra = { mult = 20, dollars = 20, mult_odds = 5, dollars_odds = 15 } }, any_suit = true, replace_base_card = true,no_rank = true,always_scores = true, in_pool = function(self,args) return false end, loc_vars = function(self, info_queue, card) local mult_numerator, mult_denominator = SMODS.get_probability_vars(card, 1, card.ability.extra.mult_odds, self.key) local dollars_numerator, dollars_denominator = SMODS.get_probability_vars(card, 1, card.ability.extra.dollars_odds, self.key) return { vars = { card.ability.bonus, mult_numerator, dollars_numerator, card.ability.extra.mult, mult_denominator, card.ability.extra.dollars, dollars_denominator } } end, calculate = function(self, card, context) if context.main_scoring and context.cardarea == G.play then local ret = {} if SMODS.pseudorandom_probability(card, 'vremade_lucky_mult', 1, card.ability.extra.mult_odds) then card.lucky_trigger = true ret.mult = card.ability.extra.mult end if SMODS.pseudorandom_probability(card, 'vremade_lucky_money', 1, card.ability.extra.dollars_odds) then card.lucky_trigger = true ret.dollars = card.ability.extra.dollars end return ret end end }
SMODS.Enhancement { key = "bonusXwildXstoneXlucky", name = "Bonus Wild Stone Lucky Card", atlas = "enhancement", pos = { x = 10, y = 5 }, config = { bonus = 80, extra = { mult = 20, dollars = 20, mult_odds = 5, dollars_odds = 15 } }, any_suit = true, replace_base_card = true,no_rank = true,always_scores = true, in_pool = function(self,args) return false end, loc_vars = function(self, info_queue, card) local mult_numerator, mult_denominator = SMODS.get_probability_vars(card, 1, card.ability.extra.mult_odds, self.key) local dollars_numerator, dollars_denominator = SMODS.get_probability_vars(card, 1, card.ability.extra.dollars_odds, self.key) return { vars = { card.ability.bonus, mult_numerator, dollars_numerator, card.ability.extra.mult, mult_denominator, card.ability.extra.dollars, dollars_denominator } } end, calculate = function(self, card, context) if context.main_scoring and context.cardarea == G.play then local ret = {} if SMODS.pseudorandom_probability(card, 'vremade_lucky_mult', 1, card.ability.extra.mult_odds) then card.lucky_trigger = true ret.mult = card.ability.extra.mult end if SMODS.pseudorandom_probability(card, 'vremade_lucky_money', 1, card.ability.extra.dollars_odds) then card.lucky_trigger = true ret.dollars = card.ability.extra.dollars end return ret end end }
SMODS.Enhancement { key = "multXwildXstoneXlucky", name = "Mult Wild Stone Lucky Card", atlas = "enhancement", pos = { x = 10, y = 6 }, config = { mult = 4, bonus = 50, extra = { mult = 20, dollars = 20, mult_odds = 5, dollars_odds = 15 } }, any_suit = true, replace_base_card = true,no_rank = true,always_scores = true, in_pool = function(self,args) return false end, loc_vars = function(self, info_queue, card) local mult_numerator, mult_denominator = SMODS.get_probability_vars(card, 1, card.ability.extra.mult_odds, self.key) local dollars_numerator, dollars_denominator = SMODS.get_probability_vars(card, 1, card.ability.extra.dollars_odds, self.key) return { vars = { card.ability.mult, card.ability.bonus, mult_numerator, dollars_numerator, card.ability.extra.mult, mult_denominator, card.ability.extra.dollars, dollars_denominator } } end, calculate = function(self, card, context) if context.main_scoring and context.cardarea == G.play then local ret = {} if SMODS.pseudorandom_probability(card, 'vremade_lucky_mult', 1, card.ability.extra.mult_odds) then card.lucky_trigger = true ret.mult = card.ability.extra.mult end if SMODS.pseudorandom_probability(card, 'vremade_lucky_money', 1, card.ability.extra.dollars_odds) then card.lucky_trigger = true ret.dollars = card.ability.extra.dollars end return ret end end }
SMODS.Enhancement { key = "bonusXmultXwildXstoneXlucky", name = "Bonus Mult Wild Stone Lucky Card", atlas = "enhancement", pos = { x = 10, y = 7 }, config = { bonus = 80, mult = 4, extra = { mult = 20, dollars = 20, mult_odds = 5, dollars_odds = 15 } }, any_suit = true, replace_base_card = true,no_rank = true,always_scores = true, in_pool = function(self,args) return false end, loc_vars = function(self, info_queue, card) local mult_numerator, mult_denominator = SMODS.get_probability_vars(card, 1, card.ability.extra.mult_odds, self.key) local dollars_numerator, dollars_denominator = SMODS.get_probability_vars(card, 1, card.ability.extra.dollars_odds, self.key) return { vars = { card.ability.bonus, card.ability.mult, mult_numerator, dollars_numerator, card.ability.extra.mult, mult_denominator, card.ability.extra.dollars, dollars_denominator } } end, calculate = function(self, card, context) if context.main_scoring and context.cardarea == G.play then local ret = {} if SMODS.pseudorandom_probability(card, 'vremade_lucky_mult', 1, card.ability.extra.mult_odds) then card.lucky_trigger = true ret.mult = card.ability.extra.mult end if SMODS.pseudorandom_probability(card, 'vremade_lucky_money', 1, card.ability.extra.dollars_odds) then card.lucky_trigger = true ret.dollars = card.ability.extra.dollars end return ret end end }
SMODS.Enhancement { key = "glassXstoneXlucky", name = "Glass Stone Lucky Card", atlas = "enhancement", pos = { x = 10, y = 8 }, config = { Xmult = 2, bonus = 50, extra = { odds = 4, mult = 20, dollars = 20, mult_odds = 5, dollars_odds = 15 } }, shatters = true, replace_base_card = true,no_rank = true,no_suit = true,always_scores = true, in_pool = function(self,args) return false end, loc_vars = function(self, info_queue, card) local numerator, denominator = SMODS.get_probability_vars(card, 1, card.ability.extra.odds, self.key) local mult_numerator, mult_denominator = SMODS.get_probability_vars(card, 1, card.ability.extra.mult_odds, self.key) local dollars_numerator, dollars_denominator = SMODS.get_probability_vars(card, 1, card.ability.extra.dollars_odds, self.key) return { vars = { card.ability.Xmult, numerator, denominator, card.ability.bonus, mult_numerator, dollars_numerator, card.ability.extra.mult, mult_denominator, card.ability.extra.dollars, dollars_denominator } } end, calculate = function(self, card, context)  if context.main_scoring and context.cardarea == G.play then local ret = {} if SMODS.pseudorandom_probability(card, 'vremade_lucky_mult', 1, card.ability.extra.mult_odds) then card.lucky_trigger = true ret.mult = card.ability.extra.mult end if SMODS.pseudorandom_probability(card, 'vremade_lucky_money', 1, card.ability.extra.dollars_odds) then card.lucky_trigger = true ret.dollars = card.ability.extra.dollars end return ret end  if context.destroy_card and context.cardarea == G.play and context.destroy_card == card and SMODS.pseudorandom_probability(card, self.key, 1, card.ability.extra.odds) then card.glass_trigger = true return { remove = true } end end }
SMODS.Enhancement { key = "bonusXglassXstoneXlucky", name = "Bonus Glass Stone Lucky Card", atlas = "enhancement", pos = { x = 10, y = 9 }, config = { bonus = 80, Xmult = 2, extra = { odds = 4, mult = 20, dollars = 20, mult_odds = 5, dollars_odds = 15 } }, shatters = true, replace_base_card = true,no_rank = true,no_suit = true,always_scores = true, in_pool = function(self,args) return false end, loc_vars = function(self, info_queue, card) local numerator, denominator = SMODS.get_probability_vars(card, 1, card.ability.extra.odds, self.key) local mult_numerator, mult_denominator = SMODS.get_probability_vars(card, 1, card.ability.extra.mult_odds, self.key) local dollars_numerator, dollars_denominator = SMODS.get_probability_vars(card, 1, card.ability.extra.dollars_odds, self.key) return { vars = { card.ability.bonus, card.ability.Xmult, numerator, denominator, mult_numerator, dollars_numerator, card.ability.extra.mult, mult_denominator, card.ability.extra.dollars, dollars_denominator } } end, calculate = function(self, card, context)  if context.main_scoring and context.cardarea == G.play then local ret = {} if SMODS.pseudorandom_probability(card, 'vremade_lucky_mult', 1, card.ability.extra.mult_odds) then card.lucky_trigger = true ret.mult = card.ability.extra.mult end if SMODS.pseudorandom_probability(card, 'vremade_lucky_money', 1, card.ability.extra.dollars_odds) then card.lucky_trigger = true ret.dollars = card.ability.extra.dollars end return ret end  if context.destroy_card and context.cardarea == G.play and context.destroy_card == card and SMODS.pseudorandom_probability(card, self.key, 1, card.ability.extra.odds) then card.glass_trigger = true return { remove = true } end end }
SMODS.Enhancement { key = "multXglassXstoneXlucky", name = "Mult Glass Stone Lucky Card", atlas = "enhancement", pos = { x = 10, y = 10 }, config = { mult = 4, Xmult = 2, bonus = 50, extra = { odds = 4, mult = 20, dollars = 20, mult_odds = 5, dollars_odds = 15 } }, shatters = true, replace_base_card = true,no_rank = true,no_suit = true,always_scores = true, in_pool = function(self,args) return false end, loc_vars = function(self, info_queue, card) local numerator, denominator = SMODS.get_probability_vars(card, 1, card.ability.extra.odds, self.key) local mult_numerator, mult_denominator = SMODS.get_probability_vars(card, 1, card.ability.extra.mult_odds, self.key) local dollars_numerator, dollars_denominator = SMODS.get_probability_vars(card, 1, card.ability.extra.dollars_odds, self.key) return { vars = { card.ability.mult, card.ability.Xmult, numerator, denominator, card.ability.bonus, mult_numerator, dollars_numerator, card.ability.extra.mult, mult_denominator, card.ability.extra.dollars, dollars_denominator } } end, calculate = function(self, card, context)  if context.main_scoring and context.cardarea == G.play then local ret = {} if SMODS.pseudorandom_probability(card, 'vremade_lucky_mult', 1, card.ability.extra.mult_odds) then card.lucky_trigger = true ret.mult = card.ability.extra.mult end if SMODS.pseudorandom_probability(card, 'vremade_lucky_money', 1, card.ability.extra.dollars_odds) then card.lucky_trigger = true ret.dollars = card.ability.extra.dollars end return ret end  if context.destroy_card and context.cardarea == G.play and context.destroy_card == card and SMODS.pseudorandom_probability(card, self.key, 1, card.ability.extra.odds) then card.glass_trigger = true return { remove = true } end end }
SMODS.Enhancement { key = "bonusXmultXglassXstoneXlucky", name = "Bonus Mult Glass Stone Lucky Card", atlas = "enhancement", pos = { x = 10, y = 11 }, config = { bonus = 80, mult = 4, Xmult = 2, extra = { odds = 4, mult = 20, dollars = 20, mult_odds = 5, dollars_odds = 15 } }, shatters = true, replace_base_card = true,no_rank = true,no_suit = true,always_scores = true, in_pool = function(self,args) return false end, loc_vars = function(self, info_queue, card) local numerator, denominator = SMODS.get_probability_vars(card, 1, card.ability.extra.odds, self.key) local mult_numerator, mult_denominator = SMODS.get_probability_vars(card, 1, card.ability.extra.mult_odds, self.key) local dollars_numerator, dollars_denominator = SMODS.get_probability_vars(card, 1, card.ability.extra.dollars_odds, self.key) return { vars = { card.ability.bonus, card.ability.mult, card.ability.Xmult, numerator, denominator, mult_numerator, dollars_numerator, card.ability.extra.mult, mult_denominator, card.ability.extra.dollars, dollars_denominator } } end, calculate = function(self, card, context)  if context.main_scoring and context.cardarea == G.play then local ret = {} if SMODS.pseudorandom_probability(card, 'vremade_lucky_mult', 1, card.ability.extra.mult_odds) then card.lucky_trigger = true ret.mult = card.ability.extra.mult end if SMODS.pseudorandom_probability(card, 'vremade_lucky_money', 1, card.ability.extra.dollars_odds) then card.lucky_trigger = true ret.dollars = card.ability.extra.dollars end return ret end  if context.destroy_card and context.cardarea == G.play and context.destroy_card == card and SMODS.pseudorandom_probability(card, self.key, 1, card.ability.extra.odds) then card.glass_trigger = true return { remove = true } end end }
SMODS.Enhancement { key = "wildXglassXstoneXlucky", name = "Wild Glass Stone Lucky Card", atlas = "enhancement", pos = { x = 10, y = 12 }, config = { Xmult = 2, bonus = 50, extra = { odds = 4, mult = 20, dollars = 20, mult_odds = 5, dollars_odds = 15 } }, any_suit = true, shatters = true, replace_base_card = true,no_rank = true,always_scores = true, in_pool = function(self,args) return false end, loc_vars = function(self, info_queue, card) local numerator, denominator = SMODS.get_probability_vars(card, 1, card.ability.extra.odds, self.key) local mult_numerator, mult_denominator = SMODS.get_probability_vars(card, 1, card.ability.extra.mult_odds, self.key) local dollars_numerator, dollars_denominator = SMODS.get_probability_vars(card, 1, card.ability.extra.dollars_odds, self.key) return { vars = { card.ability.Xmult, numerator, denominator, card.ability.bonus, mult_numerator, dollars_numerator, card.ability.extra.mult, mult_denominator, card.ability.extra.dollars, dollars_denominator } } end, calculate = function(self, card, context)  if context.main_scoring and context.cardarea == G.play then local ret = {} if SMODS.pseudorandom_probability(card, 'vremade_lucky_mult', 1, card.ability.extra.mult_odds) then card.lucky_trigger = true ret.mult = card.ability.extra.mult end if SMODS.pseudorandom_probability(card, 'vremade_lucky_money', 1, card.ability.extra.dollars_odds) then card.lucky_trigger = true ret.dollars = card.ability.extra.dollars end return ret end  if context.destroy_card and context.cardarea == G.play and context.destroy_card == card and SMODS.pseudorandom_probability(card, self.key, 1, card.ability.extra.odds) then card.glass_trigger = true return { remove = true } end end }
SMODS.Enhancement { key = "bonusXwildXglassXstoneXlucky", name = "Bonus Wild Glass Stone Lucky Card", atlas = "enhancement", pos = { x = 10, y = 13 }, config = { bonus = 80, Xmult = 2, extra = { odds = 4, mult = 20, dollars = 20, mult_odds = 5, dollars_odds = 15 } }, any_suit = true, shatters = true, replace_base_card = true,no_rank = true,always_scores = true, in_pool = function(self,args) return false end, loc_vars = function(self, info_queue, card) local numerator, denominator = SMODS.get_probability_vars(card, 1, card.ability.extra.odds, self.key) local mult_numerator, mult_denominator = SMODS.get_probability_vars(card, 1, card.ability.extra.mult_odds, self.key) local dollars_numerator, dollars_denominator = SMODS.get_probability_vars(card, 1, card.ability.extra.dollars_odds, self.key) return { vars = { card.ability.bonus, card.ability.Xmult, numerator, denominator, mult_numerator, dollars_numerator, card.ability.extra.mult, mult_denominator, card.ability.extra.dollars, dollars_denominator } } end, calculate = function(self, card, context)  if context.main_scoring and context.cardarea == G.play then local ret = {} if SMODS.pseudorandom_probability(card, 'vremade_lucky_mult', 1, card.ability.extra.mult_odds) then card.lucky_trigger = true ret.mult = card.ability.extra.mult end if SMODS.pseudorandom_probability(card, 'vremade_lucky_money', 1, card.ability.extra.dollars_odds) then card.lucky_trigger = true ret.dollars = card.ability.extra.dollars end return ret end  if context.destroy_card and context.cardarea == G.play and context.destroy_card == card and SMODS.pseudorandom_probability(card, self.key, 1, card.ability.extra.odds) then card.glass_trigger = true return { remove = true } end end }
SMODS.Enhancement { key = "multXwildXglassXstoneXlucky", name = "Mult Wild Glass Stone Lucky Card", atlas = "enhancement", pos = { x = 10, y = 14 }, config = { mult = 4, Xmult = 2, bonus = 50, extra = { odds = 4, mult = 20, dollars = 20, mult_odds = 5, dollars_odds = 15 } }, any_suit = true, shatters = true, replace_base_card = true,no_rank = true,always_scores = true, in_pool = function(self,args) return false end, loc_vars = function(self, info_queue, card) local numerator, denominator = SMODS.get_probability_vars(card, 1, card.ability.extra.odds, self.key) local mult_numerator, mult_denominator = SMODS.get_probability_vars(card, 1, card.ability.extra.mult_odds, self.key) local dollars_numerator, dollars_denominator = SMODS.get_probability_vars(card, 1, card.ability.extra.dollars_odds, self.key) return { vars = { card.ability.mult, card.ability.Xmult, numerator, denominator, card.ability.bonus, mult_numerator, dollars_numerator, card.ability.extra.mult, mult_denominator, card.ability.extra.dollars, dollars_denominator } } end, calculate = function(self, card, context)  if context.main_scoring and context.cardarea == G.play then local ret = {} if SMODS.pseudorandom_probability(card, 'vremade_lucky_mult', 1, card.ability.extra.mult_odds) then card.lucky_trigger = true ret.mult = card.ability.extra.mult end if SMODS.pseudorandom_probability(card, 'vremade_lucky_money', 1, card.ability.extra.dollars_odds) then card.lucky_trigger = true ret.dollars = card.ability.extra.dollars end return ret end  if context.destroy_card and context.cardarea == G.play and context.destroy_card == card and SMODS.pseudorandom_probability(card, self.key, 1, card.ability.extra.odds) then card.glass_trigger = true return { remove = true } end end }
SMODS.Enhancement { key = "bonusXmultXwildXglassXstoneXlucky", name = "Bonus Mult Wild Glass Stone Lucky Card", atlas = "enhancement", pos = { x = 10, y = 15 }, config = { bonus = 80, mult = 4, Xmult = 2, extra = { odds = 4, mult = 20, dollars = 20, mult_odds = 5, dollars_odds = 15 } }, any_suit = true, shatters = true, replace_base_card = true,no_rank = true,always_scores = true, in_pool = function(self,args) return false end, loc_vars = function(self, info_queue, card) local numerator, denominator = SMODS.get_probability_vars(card, 1, card.ability.extra.odds, self.key) local mult_numerator, mult_denominator = SMODS.get_probability_vars(card, 1, card.ability.extra.mult_odds, self.key) local dollars_numerator, dollars_denominator = SMODS.get_probability_vars(card, 1, card.ability.extra.dollars_odds, self.key) return { vars = { card.ability.bonus, card.ability.mult, card.ability.Xmult, numerator, denominator, mult_numerator, dollars_numerator, card.ability.extra.mult, mult_denominator, card.ability.extra.dollars, dollars_denominator } } end, calculate = function(self, card, context)  if context.main_scoring and context.cardarea == G.play then local ret = {} if SMODS.pseudorandom_probability(card, 'vremade_lucky_mult', 1, card.ability.extra.mult_odds) then card.lucky_trigger = true ret.mult = card.ability.extra.mult end if SMODS.pseudorandom_probability(card, 'vremade_lucky_money', 1, card.ability.extra.dollars_odds) then card.lucky_trigger = true ret.dollars = card.ability.extra.dollars end return ret end  if context.destroy_card and context.cardarea == G.play and context.destroy_card == card and SMODS.pseudorandom_probability(card, self.key, 1, card.ability.extra.odds) then card.glass_trigger = true return { remove = true } end end }
SMODS.Enhancement { key = "steelXstoneXlucky", name = "Steel Stone Lucky Card", atlas = "enhancement", pos = { x = 11, y = 0 }, config = { h_x_mult = 1.5, bonus = 50, extra = { mult = 20, dollars = 20, mult_odds = 5, dollars_odds = 15 } }, replace_base_card = true,no_rank = true,no_suit = true,always_scores = true, in_pool = function(self,args) return false end, loc_vars = function(self, info_queue, card) local mult_numerator, mult_denominator = SMODS.get_probability_vars(card, 1, card.ability.extra.mult_odds, self.key) local dollars_numerator, dollars_denominator = SMODS.get_probability_vars(card, 1, card.ability.extra.dollars_odds, self.key) return { vars = { card.ability.h_x_mult, card.ability.bonus, mult_numerator, dollars_numerator, card.ability.extra.mult, mult_denominator, card.ability.extra.dollars, dollars_denominator } } end, calculate = function(self, card, context) if context.main_scoring and context.cardarea == G.play then local ret = {} if SMODS.pseudorandom_probability(card, 'vremade_lucky_mult', 1, card.ability.extra.mult_odds) then card.lucky_trigger = true ret.mult = card.ability.extra.mult end if SMODS.pseudorandom_probability(card, 'vremade_lucky_money', 1, card.ability.extra.dollars_odds) then card.lucky_trigger = true ret.dollars = card.ability.extra.dollars end return ret end end }
SMODS.Enhancement { key = "bonusXsteelXstoneXlucky", name = "Bonus Steel Stone Lucky Card", atlas = "enhancement", pos = { x = 11, y = 1 }, config = { bonus = 80, h_x_mult = 1.5, extra = { mult = 20, dollars = 20, mult_odds = 5, dollars_odds = 15 } }, replace_base_card = true,no_rank = true,no_suit = true,always_scores = true, in_pool = function(self,args) return false end, loc_vars = function(self, info_queue, card) local mult_numerator, mult_denominator = SMODS.get_probability_vars(card, 1, card.ability.extra.mult_odds, self.key) local dollars_numerator, dollars_denominator = SMODS.get_probability_vars(card, 1, card.ability.extra.dollars_odds, self.key) return { vars = { card.ability.bonus, card.ability.h_x_mult, mult_numerator, dollars_numerator, card.ability.extra.mult, mult_denominator, card.ability.extra.dollars, dollars_denominator } } end, calculate = function(self, card, context) if context.main_scoring and context.cardarea == G.play then local ret = {} if SMODS.pseudorandom_probability(card, 'vremade_lucky_mult', 1, card.ability.extra.mult_odds) then card.lucky_trigger = true ret.mult = card.ability.extra.mult end if SMODS.pseudorandom_probability(card, 'vremade_lucky_money', 1, card.ability.extra.dollars_odds) then card.lucky_trigger = true ret.dollars = card.ability.extra.dollars end return ret end end }
SMODS.Enhancement { key = "multXsteelXstoneXlucky", name = "Mult Steel Stone Lucky Card", atlas = "enhancement", pos = { x = 11, y = 2 }, config = { mult = 4, h_x_mult = 1.5, bonus = 50, extra = { mult = 20, dollars = 20, mult_odds = 5, dollars_odds = 15 } }, replace_base_card = true,no_rank = true,no_suit = true,always_scores = true, in_pool = function(self,args) return false end, loc_vars = function(self, info_queue, card) local mult_numerator, mult_denominator = SMODS.get_probability_vars(card, 1, card.ability.extra.mult_odds, self.key) local dollars_numerator, dollars_denominator = SMODS.get_probability_vars(card, 1, card.ability.extra.dollars_odds, self.key) return { vars = { card.ability.mult, card.ability.h_x_mult, card.ability.bonus, mult_numerator, dollars_numerator, card.ability.extra.mult, mult_denominator, card.ability.extra.dollars, dollars_denominator } } end, calculate = function(self, card, context) if context.main_scoring and context.cardarea == G.play then local ret = {} if SMODS.pseudorandom_probability(card, 'vremade_lucky_mult', 1, card.ability.extra.mult_odds) then card.lucky_trigger = true ret.mult = card.ability.extra.mult end if SMODS.pseudorandom_probability(card, 'vremade_lucky_money', 1, card.ability.extra.dollars_odds) then card.lucky_trigger = true ret.dollars = card.ability.extra.dollars end return ret end end }
SMODS.Enhancement { key = "bonusXmultXsteelXstoneXlucky", name = "Bonus Mult Steel Stone Lucky Card", atlas = "enhancement", pos = { x = 11, y = 3 }, config = { bonus = 80, mult = 4, h_x_mult = 1.5, extra = { mult = 20, dollars = 20, mult_odds = 5, dollars_odds = 15 } }, replace_base_card = true,no_rank = true,no_suit = true,always_scores = true, in_pool = function(self,args) return false end, loc_vars = function(self, info_queue, card) local mult_numerator, mult_denominator = SMODS.get_probability_vars(card, 1, card.ability.extra.mult_odds, self.key) local dollars_numerator, dollars_denominator = SMODS.get_probability_vars(card, 1, card.ability.extra.dollars_odds, self.key) return { vars = { card.ability.bonus, card.ability.mult, card.ability.h_x_mult, mult_numerator, dollars_numerator, card.ability.extra.mult, mult_denominator, card.ability.extra.dollars, dollars_denominator } } end, calculate = function(self, card, context) if context.main_scoring and context.cardarea == G.play then local ret = {} if SMODS.pseudorandom_probability(card, 'vremade_lucky_mult', 1, card.ability.extra.mult_odds) then card.lucky_trigger = true ret.mult = card.ability.extra.mult end if SMODS.pseudorandom_probability(card, 'vremade_lucky_money', 1, card.ability.extra.dollars_odds) then card.lucky_trigger = true ret.dollars = card.ability.extra.dollars end return ret end end }
SMODS.Enhancement { key = "wildXsteelXstoneXlucky", name = "Wild Steel Stone Lucky Card", atlas = "enhancement", pos = { x = 11, y = 4 }, config = { h_x_mult = 1.5, bonus = 50, extra = { mult = 20, dollars = 20, mult_odds = 5, dollars_odds = 15 } }, any_suit = true, replace_base_card = true,no_rank = true,always_scores = true, in_pool = function(self,args) return false end, loc_vars = function(self, info_queue, card) local mult_numerator, mult_denominator = SMODS.get_probability_vars(card, 1, card.ability.extra.mult_odds, self.key) local dollars_numerator, dollars_denominator = SMODS.get_probability_vars(card, 1, card.ability.extra.dollars_odds, self.key) return { vars = { card.ability.h_x_mult, card.ability.bonus, mult_numerator, dollars_numerator, card.ability.extra.mult, mult_denominator, card.ability.extra.dollars, dollars_denominator } } end, calculate = function(self, card, context) if context.main_scoring and context.cardarea == G.play then local ret = {} if SMODS.pseudorandom_probability(card, 'vremade_lucky_mult', 1, card.ability.extra.mult_odds) then card.lucky_trigger = true ret.mult = card.ability.extra.mult end if SMODS.pseudorandom_probability(card, 'vremade_lucky_money', 1, card.ability.extra.dollars_odds) then card.lucky_trigger = true ret.dollars = card.ability.extra.dollars end return ret end end }
SMODS.Enhancement { key = "bonusXwildXsteelXstoneXlucky", name = "Bonus Wild Steel Stone Lucky Card", atlas = "enhancement", pos = { x = 11, y = 5 }, config = { bonus = 80, h_x_mult = 1.5, extra = { mult = 20, dollars = 20, mult_odds = 5, dollars_odds = 15 } }, any_suit = true, replace_base_card = true,no_rank = true,always_scores = true, in_pool = function(self,args) return false end, loc_vars = function(self, info_queue, card) local mult_numerator, mult_denominator = SMODS.get_probability_vars(card, 1, card.ability.extra.mult_odds, self.key) local dollars_numerator, dollars_denominator = SMODS.get_probability_vars(card, 1, card.ability.extra.dollars_odds, self.key) return { vars = { card.ability.bonus, card.ability.h_x_mult, mult_numerator, dollars_numerator, card.ability.extra.mult, mult_denominator, card.ability.extra.dollars, dollars_denominator } } end, calculate = function(self, card, context) if context.main_scoring and context.cardarea == G.play then local ret = {} if SMODS.pseudorandom_probability(card, 'vremade_lucky_mult', 1, card.ability.extra.mult_odds) then card.lucky_trigger = true ret.mult = card.ability.extra.mult end if SMODS.pseudorandom_probability(card, 'vremade_lucky_money', 1, card.ability.extra.dollars_odds) then card.lucky_trigger = true ret.dollars = card.ability.extra.dollars end return ret end end }
SMODS.Enhancement { key = "multXwildXsteelXstoneXlucky", name = "Mult Wild Steel Stone Lucky Card", atlas = "enhancement", pos = { x = 11, y = 6 }, config = { mult = 4, h_x_mult = 1.5, bonus = 50, extra = { mult = 20, dollars = 20, mult_odds = 5, dollars_odds = 15 } }, any_suit = true, replace_base_card = true,no_rank = true,always_scores = true, in_pool = function(self,args) return false end, loc_vars = function(self, info_queue, card) local mult_numerator, mult_denominator = SMODS.get_probability_vars(card, 1, card.ability.extra.mult_odds, self.key) local dollars_numerator, dollars_denominator = SMODS.get_probability_vars(card, 1, card.ability.extra.dollars_odds, self.key) return { vars = { card.ability.mult, card.ability.h_x_mult, card.ability.bonus, mult_numerator, dollars_numerator, card.ability.extra.mult, mult_denominator, card.ability.extra.dollars, dollars_denominator } } end, calculate = function(self, card, context) if context.main_scoring and context.cardarea == G.play then local ret = {} if SMODS.pseudorandom_probability(card, 'vremade_lucky_mult', 1, card.ability.extra.mult_odds) then card.lucky_trigger = true ret.mult = card.ability.extra.mult end if SMODS.pseudorandom_probability(card, 'vremade_lucky_money', 1, card.ability.extra.dollars_odds) then card.lucky_trigger = true ret.dollars = card.ability.extra.dollars end return ret end end }
SMODS.Enhancement { key = "bonusXmultXwildXsteelXstoneXlucky", name = "Bonus Mult Wild Steel Stone Lucky Card", atlas = "enhancement", pos = { x = 11, y = 7 }, config = { bonus = 80, mult = 4, h_x_mult = 1.5, extra = { mult = 20, dollars = 20, mult_odds = 5, dollars_odds = 15 } }, any_suit = true, replace_base_card = true,no_rank = true,always_scores = true, in_pool = function(self,args) return false end, loc_vars = function(self, info_queue, card) local mult_numerator, mult_denominator = SMODS.get_probability_vars(card, 1, card.ability.extra.mult_odds, self.key) local dollars_numerator, dollars_denominator = SMODS.get_probability_vars(card, 1, card.ability.extra.dollars_odds, self.key) return { vars = { card.ability.bonus, card.ability.mult, card.ability.h_x_mult, mult_numerator, dollars_numerator, card.ability.extra.mult, mult_denominator, card.ability.extra.dollars, dollars_denominator } } end, calculate = function(self, card, context) if context.main_scoring and context.cardarea == G.play then local ret = {} if SMODS.pseudorandom_probability(card, 'vremade_lucky_mult', 1, card.ability.extra.mult_odds) then card.lucky_trigger = true ret.mult = card.ability.extra.mult end if SMODS.pseudorandom_probability(card, 'vremade_lucky_money', 1, card.ability.extra.dollars_odds) then card.lucky_trigger = true ret.dollars = card.ability.extra.dollars end return ret end end }
SMODS.Enhancement { key = "glassXsteelXstoneXlucky", name = "Glass Steel Stone Lucky Card", atlas = "enhancement", pos = { x = 11, y = 8 }, config = { Xmult = 2, h_x_mult = 1.5, bonus = 50, extra = { odds = 4, mult = 20, dollars = 20, mult_odds = 5, dollars_odds = 15 } }, shatters = true, replace_base_card = true,no_rank = true,no_suit = true,always_scores = true, in_pool = function(self,args) return false end, loc_vars = function(self, info_queue, card) local numerator, denominator = SMODS.get_probability_vars(card, 1, card.ability.extra.odds, self.key) local mult_numerator, mult_denominator = SMODS.get_probability_vars(card, 1, card.ability.extra.mult_odds, self.key) local dollars_numerator, dollars_denominator = SMODS.get_probability_vars(card, 1, card.ability.extra.dollars_odds, self.key) return { vars = { card.ability.Xmult, numerator, denominator, card.ability.h_x_mult, card.ability.bonus, mult_numerator, dollars_numerator, card.ability.extra.mult, mult_denominator, card.ability.extra.dollars, dollars_denominator } } end, calculate = function(self, card, context)  if context.main_scoring and context.cardarea == G.play then local ret = {} if SMODS.pseudorandom_probability(card, 'vremade_lucky_mult', 1, card.ability.extra.mult_odds) then card.lucky_trigger = true ret.mult = card.ability.extra.mult end if SMODS.pseudorandom_probability(card, 'vremade_lucky_money', 1, card.ability.extra.dollars_odds) then card.lucky_trigger = true ret.dollars = card.ability.extra.dollars end return ret end  if context.destroy_card and context.cardarea == G.play and context.destroy_card == card and SMODS.pseudorandom_probability(card, self.key, 1, card.ability.extra.odds) then card.glass_trigger = true return { remove = true } end end }
SMODS.Enhancement { key = "bonusXglassXsteelXstoneXlucky", name = "Bonus Glass Steel Stone Lucky Card", atlas = "enhancement", pos = { x = 11, y = 9 }, config = { bonus = 80, Xmult = 2, h_x_mult = 1.5, extra = { odds = 4, mult = 20, dollars = 20, mult_odds = 5, dollars_odds = 15 } }, shatters = true, replace_base_card = true,no_rank = true,no_suit = true,always_scores = true, in_pool = function(self,args) return false end, loc_vars = function(self, info_queue, card) local numerator, denominator = SMODS.get_probability_vars(card, 1, card.ability.extra.odds, self.key) local mult_numerator, mult_denominator = SMODS.get_probability_vars(card, 1, card.ability.extra.mult_odds, self.key) local dollars_numerator, dollars_denominator = SMODS.get_probability_vars(card, 1, card.ability.extra.dollars_odds, self.key) return { vars = { card.ability.bonus, card.ability.Xmult, numerator, denominator, card.ability.h_x_mult, mult_numerator, dollars_numerator, card.ability.extra.mult, mult_denominator, card.ability.extra.dollars, dollars_denominator } } end, calculate = function(self, card, context)  if context.main_scoring and context.cardarea == G.play then local ret = {} if SMODS.pseudorandom_probability(card, 'vremade_lucky_mult', 1, card.ability.extra.mult_odds) then card.lucky_trigger = true ret.mult = card.ability.extra.mult end if SMODS.pseudorandom_probability(card, 'vremade_lucky_money', 1, card.ability.extra.dollars_odds) then card.lucky_trigger = true ret.dollars = card.ability.extra.dollars end return ret end  if context.destroy_card and context.cardarea == G.play and context.destroy_card == card and SMODS.pseudorandom_probability(card, self.key, 1, card.ability.extra.odds) then card.glass_trigger = true return { remove = true } end end }
SMODS.Enhancement { key = "multXglassXsteelXstoneXlucky", name = "Mult Glass Steel Stone Lucky Card", atlas = "enhancement", pos = { x = 11, y = 10 }, config = { mult = 4, Xmult = 2, h_x_mult = 1.5, bonus = 50, extra = { odds = 4, mult = 20, dollars = 20, mult_odds = 5, dollars_odds = 15 } }, shatters = true, replace_base_card = true,no_rank = true,no_suit = true,always_scores = true, in_pool = function(self,args) return false end, loc_vars = function(self, info_queue, card) local numerator, denominator = SMODS.get_probability_vars(card, 1, card.ability.extra.odds, self.key) local mult_numerator, mult_denominator = SMODS.get_probability_vars(card, 1, card.ability.extra.mult_odds, self.key) local dollars_numerator, dollars_denominator = SMODS.get_probability_vars(card, 1, card.ability.extra.dollars_odds, self.key) return { vars = { card.ability.mult, card.ability.Xmult, numerator, denominator, card.ability.h_x_mult, card.ability.bonus, mult_numerator, dollars_numerator, card.ability.extra.mult, mult_denominator, card.ability.extra.dollars, dollars_denominator } } end, calculate = function(self, card, context)  if context.main_scoring and context.cardarea == G.play then local ret = {} if SMODS.pseudorandom_probability(card, 'vremade_lucky_mult', 1, card.ability.extra.mult_odds) then card.lucky_trigger = true ret.mult = card.ability.extra.mult end if SMODS.pseudorandom_probability(card, 'vremade_lucky_money', 1, card.ability.extra.dollars_odds) then card.lucky_trigger = true ret.dollars = card.ability.extra.dollars end return ret end  if context.destroy_card and context.cardarea == G.play and context.destroy_card == card and SMODS.pseudorandom_probability(card, self.key, 1, card.ability.extra.odds) then card.glass_trigger = true return { remove = true } end end }
SMODS.Enhancement { key = "bonusXmultXglassXsteelXstoneXlucky", name = "Bonus Mult Glass Steel Stone Lucky Card", atlas = "enhancement", pos = { x = 11, y = 11 }, config = { bonus = 80, mult = 4, Xmult = 2, h_x_mult = 1.5, extra = { odds = 4, mult = 20, dollars = 20, mult_odds = 5, dollars_odds = 15 } }, shatters = true, replace_base_card = true,no_rank = true,no_suit = true,always_scores = true, in_pool = function(self,args) return false end, loc_vars = function(self, info_queue, card) local numerator, denominator = SMODS.get_probability_vars(card, 1, card.ability.extra.odds, self.key) local mult_numerator, mult_denominator = SMODS.get_probability_vars(card, 1, card.ability.extra.mult_odds, self.key) local dollars_numerator, dollars_denominator = SMODS.get_probability_vars(card, 1, card.ability.extra.dollars_odds, self.key) return { vars = { card.ability.bonus, card.ability.mult, card.ability.Xmult, numerator, denominator, card.ability.h_x_mult, mult_numerator, dollars_numerator, card.ability.extra.mult, mult_denominator, card.ability.extra.dollars, dollars_denominator } } end, calculate = function(self, card, context)  if context.main_scoring and context.cardarea == G.play then local ret = {} if SMODS.pseudorandom_probability(card, 'vremade_lucky_mult', 1, card.ability.extra.mult_odds) then card.lucky_trigger = true ret.mult = card.ability.extra.mult end if SMODS.pseudorandom_probability(card, 'vremade_lucky_money', 1, card.ability.extra.dollars_odds) then card.lucky_trigger = true ret.dollars = card.ability.extra.dollars end return ret end  if context.destroy_card and context.cardarea == G.play and context.destroy_card == card and SMODS.pseudorandom_probability(card, self.key, 1, card.ability.extra.odds) then card.glass_trigger = true return { remove = true } end end }
SMODS.Enhancement { key = "wildXglassXsteelXstoneXlucky", name = "Wild Glass Steel Stone Lucky Card", atlas = "enhancement", pos = { x = 11, y = 12 }, config = { Xmult = 2, h_x_mult = 1.5, bonus = 50, extra = { odds = 4, mult = 20, dollars = 20, mult_odds = 5, dollars_odds = 15 } }, any_suit = true, shatters = true, replace_base_card = true,no_rank = true,always_scores = true, in_pool = function(self,args) return false end, loc_vars = function(self, info_queue, card) local numerator, denominator = SMODS.get_probability_vars(card, 1, card.ability.extra.odds, self.key) local mult_numerator, mult_denominator = SMODS.get_probability_vars(card, 1, card.ability.extra.mult_odds, self.key) local dollars_numerator, dollars_denominator = SMODS.get_probability_vars(card, 1, card.ability.extra.dollars_odds, self.key) return { vars = { card.ability.Xmult, numerator, denominator, card.ability.h_x_mult, card.ability.bonus, mult_numerator, dollars_numerator, card.ability.extra.mult, mult_denominator, card.ability.extra.dollars, dollars_denominator } } end, calculate = function(self, card, context)  if context.main_scoring and context.cardarea == G.play then local ret = {} if SMODS.pseudorandom_probability(card, 'vremade_lucky_mult', 1, card.ability.extra.mult_odds) then card.lucky_trigger = true ret.mult = card.ability.extra.mult end if SMODS.pseudorandom_probability(card, 'vremade_lucky_money', 1, card.ability.extra.dollars_odds) then card.lucky_trigger = true ret.dollars = card.ability.extra.dollars end return ret end  if context.destroy_card and context.cardarea == G.play and context.destroy_card == card and SMODS.pseudorandom_probability(card, self.key, 1, card.ability.extra.odds) then card.glass_trigger = true return { remove = true } end end }
SMODS.Enhancement { key = "bonusXwildXglassXsteelXstoneXlucky", name = "Bonus Wild Glass Steel Stone Lucky Card", atlas = "enhancement", pos = { x = 11, y = 13 }, config = { bonus = 80, Xmult = 2, h_x_mult = 1.5, extra = { odds = 4, mult = 20, dollars = 20, mult_odds = 5, dollars_odds = 15 } }, any_suit = true, shatters = true, replace_base_card = true,no_rank = true,always_scores = true, in_pool = function(self,args) return false end, loc_vars = function(self, info_queue, card) local numerator, denominator = SMODS.get_probability_vars(card, 1, card.ability.extra.odds, self.key) local mult_numerator, mult_denominator = SMODS.get_probability_vars(card, 1, card.ability.extra.mult_odds, self.key) local dollars_numerator, dollars_denominator = SMODS.get_probability_vars(card, 1, card.ability.extra.dollars_odds, self.key) return { vars = { card.ability.bonus, card.ability.Xmult, numerator, denominator, card.ability.h_x_mult, mult_numerator, dollars_numerator, card.ability.extra.mult, mult_denominator, card.ability.extra.dollars, dollars_denominator } } end, calculate = function(self, card, context)  if context.main_scoring and context.cardarea == G.play then local ret = {} if SMODS.pseudorandom_probability(card, 'vremade_lucky_mult', 1, card.ability.extra.mult_odds) then card.lucky_trigger = true ret.mult = card.ability.extra.mult end if SMODS.pseudorandom_probability(card, 'vremade_lucky_money', 1, card.ability.extra.dollars_odds) then card.lucky_trigger = true ret.dollars = card.ability.extra.dollars end return ret end  if context.destroy_card and context.cardarea == G.play and context.destroy_card == card and SMODS.pseudorandom_probability(card, self.key, 1, card.ability.extra.odds) then card.glass_trigger = true return { remove = true } end end }
SMODS.Enhancement { key = "multXwildXglassXsteelXstoneXlucky", name = "Mult Wild Glass Steel Stone Lucky Card", atlas = "enhancement", pos = { x = 11, y = 14 }, config = { mult = 4, Xmult = 2, h_x_mult = 1.5, bonus = 50, extra = { odds = 4, mult = 20, dollars = 20, mult_odds = 5, dollars_odds = 15 } }, any_suit = true, shatters = true, replace_base_card = true,no_rank = true,always_scores = true, in_pool = function(self,args) return false end, loc_vars = function(self, info_queue, card) local numerator, denominator = SMODS.get_probability_vars(card, 1, card.ability.extra.odds, self.key) local mult_numerator, mult_denominator = SMODS.get_probability_vars(card, 1, card.ability.extra.mult_odds, self.key) local dollars_numerator, dollars_denominator = SMODS.get_probability_vars(card, 1, card.ability.extra.dollars_odds, self.key) return { vars = { card.ability.mult, card.ability.Xmult, numerator, denominator, card.ability.h_x_mult, card.ability.bonus, mult_numerator, dollars_numerator, card.ability.extra.mult, mult_denominator, card.ability.extra.dollars, dollars_denominator } } end, calculate = function(self, card, context)  if context.main_scoring and context.cardarea == G.play then local ret = {} if SMODS.pseudorandom_probability(card, 'vremade_lucky_mult', 1, card.ability.extra.mult_odds) then card.lucky_trigger = true ret.mult = card.ability.extra.mult end if SMODS.pseudorandom_probability(card, 'vremade_lucky_money', 1, card.ability.extra.dollars_odds) then card.lucky_trigger = true ret.dollars = card.ability.extra.dollars end return ret end  if context.destroy_card and context.cardarea == G.play and context.destroy_card == card and SMODS.pseudorandom_probability(card, self.key, 1, card.ability.extra.odds) then card.glass_trigger = true return { remove = true } end end }
SMODS.Enhancement { key = "bonusXmultXwildXglassXsteelXstoneXlucky", name = "Bonus Mult Wild Glass Steel Stone Lucky Card", atlas = "enhancement", pos = { x = 11, y = 15 }, config = { bonus = 80, mult = 4, Xmult = 2, h_x_mult = 1.5, extra = { odds = 4, mult = 20, dollars = 20, mult_odds = 5, dollars_odds = 15 } }, any_suit = true, shatters = true, replace_base_card = true,no_rank = true,always_scores = true, in_pool = function(self,args) return false end, loc_vars = function(self, info_queue, card) local numerator, denominator = SMODS.get_probability_vars(card, 1, card.ability.extra.odds, self.key) local mult_numerator, mult_denominator = SMODS.get_probability_vars(card, 1, card.ability.extra.mult_odds, self.key) local dollars_numerator, dollars_denominator = SMODS.get_probability_vars(card, 1, card.ability.extra.dollars_odds, self.key) return { vars = { card.ability.bonus, card.ability.mult, card.ability.Xmult, numerator, denominator, card.ability.h_x_mult, mult_numerator, dollars_numerator, card.ability.extra.mult, mult_denominator, card.ability.extra.dollars, dollars_denominator } } end, calculate = function(self, card, context)  if context.main_scoring and context.cardarea == G.play then local ret = {} if SMODS.pseudorandom_probability(card, 'vremade_lucky_mult', 1, card.ability.extra.mult_odds) then card.lucky_trigger = true ret.mult = card.ability.extra.mult end if SMODS.pseudorandom_probability(card, 'vremade_lucky_money', 1, card.ability.extra.dollars_odds) then card.lucky_trigger = true ret.dollars = card.ability.extra.dollars end return ret end  if context.destroy_card and context.cardarea == G.play and context.destroy_card == card and SMODS.pseudorandom_probability(card, self.key, 1, card.ability.extra.odds) then card.glass_trigger = true return { remove = true } end end }
SMODS.Enhancement { key = "goldXlucky", name = "Gold Lucky Card", atlas = "enhancement", pos = { x = 12, y = 0 }, config = { h_dollars = 3, extra = { mult = 20, dollars = 20, mult_odds = 5, dollars_odds = 15 } }, in_pool = function(self,args) return false end, loc_vars = function(self, info_queue, card) local mult_numerator, mult_denominator = SMODS.get_probability_vars(card, 1, card.ability.extra.mult_odds, self.key) local dollars_numerator, dollars_denominator = SMODS.get_probability_vars(card, 1, card.ability.extra.dollars_odds, self.key) return { vars = { card.ability.h_dollars, mult_numerator, dollars_numerator, card.ability.extra.mult, mult_denominator, card.ability.extra.dollars, dollars_denominator } } end, calculate = function(self, card, context) if context.main_scoring and context.cardarea == G.play then local ret = {} if SMODS.pseudorandom_probability(card, 'vremade_lucky_mult', 1, card.ability.extra.mult_odds) then card.lucky_trigger = true ret.mult = card.ability.extra.mult end if SMODS.pseudorandom_probability(card, 'vremade_lucky_money', 1, card.ability.extra.dollars_odds) then card.lucky_trigger = true ret.dollars = card.ability.extra.dollars end return ret end end }
SMODS.Enhancement { key = "bonusXgoldXlucky", name = "Bonus Gold Lucky Card", atlas = "enhancement", pos = { x = 12, y = 1 }, config = { bonus = 30, h_dollars = 3, extra = { mult = 20, dollars = 20, mult_odds = 5, dollars_odds = 15 } }, in_pool = function(self,args) return false end, loc_vars = function(self, info_queue, card) local mult_numerator, mult_denominator = SMODS.get_probability_vars(card, 1, card.ability.extra.mult_odds, self.key) local dollars_numerator, dollars_denominator = SMODS.get_probability_vars(card, 1, card.ability.extra.dollars_odds, self.key) return { vars = { card.ability.bonus, card.ability.h_dollars, mult_numerator, dollars_numerator, card.ability.extra.mult, mult_denominator, card.ability.extra.dollars, dollars_denominator } } end, calculate = function(self, card, context) if context.main_scoring and context.cardarea == G.play then local ret = {} if SMODS.pseudorandom_probability(card, 'vremade_lucky_mult', 1, card.ability.extra.mult_odds) then card.lucky_trigger = true ret.mult = card.ability.extra.mult end if SMODS.pseudorandom_probability(card, 'vremade_lucky_money', 1, card.ability.extra.dollars_odds) then card.lucky_trigger = true ret.dollars = card.ability.extra.dollars end return ret end end }
SMODS.Enhancement { key = "multXgoldXlucky", name = "Mult Gold Lucky Card", atlas = "enhancement", pos = { x = 12, y = 2 }, config = { mult = 4, h_dollars = 3, extra = { mult = 20, dollars = 20, mult_odds = 5, dollars_odds = 15 } }, in_pool = function(self,args) return false end, loc_vars = function(self, info_queue, card) local mult_numerator, mult_denominator = SMODS.get_probability_vars(card, 1, card.ability.extra.mult_odds, self.key) local dollars_numerator, dollars_denominator = SMODS.get_probability_vars(card, 1, card.ability.extra.dollars_odds, self.key) return { vars = { card.ability.mult, card.ability.h_dollars, mult_numerator, dollars_numerator, card.ability.extra.mult, mult_denominator, card.ability.extra.dollars, dollars_denominator } } end, calculate = function(self, card, context) if context.main_scoring and context.cardarea == G.play then local ret = {} if SMODS.pseudorandom_probability(card, 'vremade_lucky_mult', 1, card.ability.extra.mult_odds) then card.lucky_trigger = true ret.mult = card.ability.extra.mult end if SMODS.pseudorandom_probability(card, 'vremade_lucky_money', 1, card.ability.extra.dollars_odds) then card.lucky_trigger = true ret.dollars = card.ability.extra.dollars end return ret end end }
SMODS.Enhancement { key = "bonusXmultXgoldXlucky", name = "Bonus Mult Gold Lucky Card", atlas = "enhancement", pos = { x = 12, y = 3 }, config = { bonus = 30, mult = 4, h_dollars = 3, extra = { mult = 20, dollars = 20, mult_odds = 5, dollars_odds = 15 } }, in_pool = function(self,args) return false end, loc_vars = function(self, info_queue, card) local mult_numerator, mult_denominator = SMODS.get_probability_vars(card, 1, card.ability.extra.mult_odds, self.key) local dollars_numerator, dollars_denominator = SMODS.get_probability_vars(card, 1, card.ability.extra.dollars_odds, self.key) return { vars = { card.ability.bonus, card.ability.mult, card.ability.h_dollars, mult_numerator, dollars_numerator, card.ability.extra.mult, mult_denominator, card.ability.extra.dollars, dollars_denominator } } end, calculate = function(self, card, context) if context.main_scoring and context.cardarea == G.play then local ret = {} if SMODS.pseudorandom_probability(card, 'vremade_lucky_mult', 1, card.ability.extra.mult_odds) then card.lucky_trigger = true ret.mult = card.ability.extra.mult end if SMODS.pseudorandom_probability(card, 'vremade_lucky_money', 1, card.ability.extra.dollars_odds) then card.lucky_trigger = true ret.dollars = card.ability.extra.dollars end return ret end end }
SMODS.Enhancement { key = "wildXgoldXlucky", name = "Wild Gold Lucky Card", atlas = "enhancement", pos = { x = 12, y = 4 }, config = { h_dollars = 3, extra = { mult = 20, dollars = 20, mult_odds = 5, dollars_odds = 15 } }, any_suit = true, in_pool = function(self,args) return false end, loc_vars = function(self, info_queue, card) local mult_numerator, mult_denominator = SMODS.get_probability_vars(card, 1, card.ability.extra.mult_odds, self.key) local dollars_numerator, dollars_denominator = SMODS.get_probability_vars(card, 1, card.ability.extra.dollars_odds, self.key) return { vars = { card.ability.h_dollars, mult_numerator, dollars_numerator, card.ability.extra.mult, mult_denominator, card.ability.extra.dollars, dollars_denominator } } end, calculate = function(self, card, context) if context.main_scoring and context.cardarea == G.play then local ret = {} if SMODS.pseudorandom_probability(card, 'vremade_lucky_mult', 1, card.ability.extra.mult_odds) then card.lucky_trigger = true ret.mult = card.ability.extra.mult end if SMODS.pseudorandom_probability(card, 'vremade_lucky_money', 1, card.ability.extra.dollars_odds) then card.lucky_trigger = true ret.dollars = card.ability.extra.dollars end return ret end end }
SMODS.Enhancement { key = "bonusXwildXgoldXlucky", name = "Bonus Wild Gold Lucky Card", atlas = "enhancement", pos = { x = 12, y = 5 }, config = { bonus = 30, h_dollars = 3, extra = { mult = 20, dollars = 20, mult_odds = 5, dollars_odds = 15 } }, any_suit = true, in_pool = function(self,args) return false end, loc_vars = function(self, info_queue, card) local mult_numerator, mult_denominator = SMODS.get_probability_vars(card, 1, card.ability.extra.mult_odds, self.key) local dollars_numerator, dollars_denominator = SMODS.get_probability_vars(card, 1, card.ability.extra.dollars_odds, self.key) return { vars = { card.ability.bonus, card.ability.h_dollars, mult_numerator, dollars_numerator, card.ability.extra.mult, mult_denominator, card.ability.extra.dollars, dollars_denominator } } end, calculate = function(self, card, context) if context.main_scoring and context.cardarea == G.play then local ret = {} if SMODS.pseudorandom_probability(card, 'vremade_lucky_mult', 1, card.ability.extra.mult_odds) then card.lucky_trigger = true ret.mult = card.ability.extra.mult end if SMODS.pseudorandom_probability(card, 'vremade_lucky_money', 1, card.ability.extra.dollars_odds) then card.lucky_trigger = true ret.dollars = card.ability.extra.dollars end return ret end end }
SMODS.Enhancement { key = "multXwildXgoldXlucky", name = "Mult Wild Gold Lucky Card", atlas = "enhancement", pos = { x = 12, y = 6 }, config = { mult = 4, h_dollars = 3, extra = { mult = 20, dollars = 20, mult_odds = 5, dollars_odds = 15 } }, any_suit = true, in_pool = function(self,args) return false end, loc_vars = function(self, info_queue, card) local mult_numerator, mult_denominator = SMODS.get_probability_vars(card, 1, card.ability.extra.mult_odds, self.key) local dollars_numerator, dollars_denominator = SMODS.get_probability_vars(card, 1, card.ability.extra.dollars_odds, self.key) return { vars = { card.ability.mult, card.ability.h_dollars, mult_numerator, dollars_numerator, card.ability.extra.mult, mult_denominator, card.ability.extra.dollars, dollars_denominator } } end, calculate = function(self, card, context) if context.main_scoring and context.cardarea == G.play then local ret = {} if SMODS.pseudorandom_probability(card, 'vremade_lucky_mult', 1, card.ability.extra.mult_odds) then card.lucky_trigger = true ret.mult = card.ability.extra.mult end if SMODS.pseudorandom_probability(card, 'vremade_lucky_money', 1, card.ability.extra.dollars_odds) then card.lucky_trigger = true ret.dollars = card.ability.extra.dollars end return ret end end }
SMODS.Enhancement { key = "bonusXmultXwildXgoldXlucky", name = "Bonus Mult Wild Gold Lucky Card", atlas = "enhancement", pos = { x = 12, y = 7 }, config = { bonus = 30, mult = 4, h_dollars = 3, extra = { mult = 20, dollars = 20, mult_odds = 5, dollars_odds = 15 } }, any_suit = true, in_pool = function(self,args) return false end, loc_vars = function(self, info_queue, card) local mult_numerator, mult_denominator = SMODS.get_probability_vars(card, 1, card.ability.extra.mult_odds, self.key) local dollars_numerator, dollars_denominator = SMODS.get_probability_vars(card, 1, card.ability.extra.dollars_odds, self.key) return { vars = { card.ability.bonus, card.ability.mult, card.ability.h_dollars, mult_numerator, dollars_numerator, card.ability.extra.mult, mult_denominator, card.ability.extra.dollars, dollars_denominator } } end, calculate = function(self, card, context) if context.main_scoring and context.cardarea == G.play then local ret = {} if SMODS.pseudorandom_probability(card, 'vremade_lucky_mult', 1, card.ability.extra.mult_odds) then card.lucky_trigger = true ret.mult = card.ability.extra.mult end if SMODS.pseudorandom_probability(card, 'vremade_lucky_money', 1, card.ability.extra.dollars_odds) then card.lucky_trigger = true ret.dollars = card.ability.extra.dollars end return ret end end }
SMODS.Enhancement { key = "glassXgoldXlucky", name = "Glass Gold Lucky Card", atlas = "enhancement", pos = { x = 12, y = 8 }, config = { Xmult = 2, h_dollars = 3, extra = { odds = 4, mult = 20, dollars = 20, mult_odds = 5, dollars_odds = 15 } }, shatters = true, in_pool = function(self,args) return false end, loc_vars = function(self, info_queue, card) local numerator, denominator = SMODS.get_probability_vars(card, 1, card.ability.extra.odds, self.key) local mult_numerator, mult_denominator = SMODS.get_probability_vars(card, 1, card.ability.extra.mult_odds, self.key) local dollars_numerator, dollars_denominator = SMODS.get_probability_vars(card, 1, card.ability.extra.dollars_odds, self.key) return { vars = { card.ability.Xmult, numerator, denominator, card.ability.h_dollars, mult_numerator, dollars_numerator, card.ability.extra.mult, mult_denominator, card.ability.extra.dollars, dollars_denominator } } end, calculate = function(self, card, context)  if context.main_scoring and context.cardarea == G.play then local ret = {} if SMODS.pseudorandom_probability(card, 'vremade_lucky_mult', 1, card.ability.extra.mult_odds) then card.lucky_trigger = true ret.mult = card.ability.extra.mult end if SMODS.pseudorandom_probability(card, 'vremade_lucky_money', 1, card.ability.extra.dollars_odds) then card.lucky_trigger = true ret.dollars = card.ability.extra.dollars end return ret end  if context.destroy_card and context.cardarea == G.play and context.destroy_card == card and SMODS.pseudorandom_probability(card, self.key, 1, card.ability.extra.odds) then card.glass_trigger = true return { remove = true } end end }
SMODS.Enhancement { key = "bonusXglassXgoldXlucky", name = "Bonus Glass Gold Lucky Card", atlas = "enhancement", pos = { x = 12, y = 9 }, config = { bonus = 30, Xmult = 2, h_dollars = 3, extra = { odds = 4, mult = 20, dollars = 20, mult_odds = 5, dollars_odds = 15 } }, shatters = true, in_pool = function(self,args) return false end, loc_vars = function(self, info_queue, card) local numerator, denominator = SMODS.get_probability_vars(card, 1, card.ability.extra.odds, self.key) local mult_numerator, mult_denominator = SMODS.get_probability_vars(card, 1, card.ability.extra.mult_odds, self.key) local dollars_numerator, dollars_denominator = SMODS.get_probability_vars(card, 1, card.ability.extra.dollars_odds, self.key) return { vars = { card.ability.bonus, card.ability.Xmult, numerator, denominator, card.ability.h_dollars, mult_numerator, dollars_numerator, card.ability.extra.mult, mult_denominator, card.ability.extra.dollars, dollars_denominator } } end, calculate = function(self, card, context)  if context.main_scoring and context.cardarea == G.play then local ret = {} if SMODS.pseudorandom_probability(card, 'vremade_lucky_mult', 1, card.ability.extra.mult_odds) then card.lucky_trigger = true ret.mult = card.ability.extra.mult end if SMODS.pseudorandom_probability(card, 'vremade_lucky_money', 1, card.ability.extra.dollars_odds) then card.lucky_trigger = true ret.dollars = card.ability.extra.dollars end return ret end  if context.destroy_card and context.cardarea == G.play and context.destroy_card == card and SMODS.pseudorandom_probability(card, self.key, 1, card.ability.extra.odds) then card.glass_trigger = true return { remove = true } end end }
SMODS.Enhancement { key = "multXglassXgoldXlucky", name = "Mult Glass Gold Lucky Card", atlas = "enhancement", pos = { x = 12, y = 10 }, config = { mult = 4, Xmult = 2, h_dollars = 3, extra = { odds = 4, mult = 20, dollars = 20, mult_odds = 5, dollars_odds = 15 } }, shatters = true, in_pool = function(self,args) return false end, loc_vars = function(self, info_queue, card) local numerator, denominator = SMODS.get_probability_vars(card, 1, card.ability.extra.odds, self.key) local mult_numerator, mult_denominator = SMODS.get_probability_vars(card, 1, card.ability.extra.mult_odds, self.key) local dollars_numerator, dollars_denominator = SMODS.get_probability_vars(card, 1, card.ability.extra.dollars_odds, self.key) return { vars = { card.ability.mult, card.ability.Xmult, numerator, denominator, card.ability.h_dollars, mult_numerator, dollars_numerator, card.ability.extra.mult, mult_denominator, card.ability.extra.dollars, dollars_denominator } } end, calculate = function(self, card, context)  if context.main_scoring and context.cardarea == G.play then local ret = {} if SMODS.pseudorandom_probability(card, 'vremade_lucky_mult', 1, card.ability.extra.mult_odds) then card.lucky_trigger = true ret.mult = card.ability.extra.mult end if SMODS.pseudorandom_probability(card, 'vremade_lucky_money', 1, card.ability.extra.dollars_odds) then card.lucky_trigger = true ret.dollars = card.ability.extra.dollars end return ret end  if context.destroy_card and context.cardarea == G.play and context.destroy_card == card and SMODS.pseudorandom_probability(card, self.key, 1, card.ability.extra.odds) then card.glass_trigger = true return { remove = true } end end }
SMODS.Enhancement { key = "bonusXmultXglassXgoldXlucky", name = "Bonus Mult Glass Gold Lucky Card", atlas = "enhancement", pos = { x = 12, y = 11 }, config = { bonus = 30, mult = 4, Xmult = 2, h_dollars = 3, extra = { odds = 4, mult = 20, dollars = 20, mult_odds = 5, dollars_odds = 15 } }, shatters = true, in_pool = function(self,args) return false end, loc_vars = function(self, info_queue, card) local numerator, denominator = SMODS.get_probability_vars(card, 1, card.ability.extra.odds, self.key) local mult_numerator, mult_denominator = SMODS.get_probability_vars(card, 1, card.ability.extra.mult_odds, self.key) local dollars_numerator, dollars_denominator = SMODS.get_probability_vars(card, 1, card.ability.extra.dollars_odds, self.key) return { vars = { card.ability.bonus, card.ability.mult, card.ability.Xmult, numerator, denominator, card.ability.h_dollars, mult_numerator, dollars_numerator, card.ability.extra.mult, mult_denominator, card.ability.extra.dollars, dollars_denominator } } end, calculate = function(self, card, context)  if context.main_scoring and context.cardarea == G.play then local ret = {} if SMODS.pseudorandom_probability(card, 'vremade_lucky_mult', 1, card.ability.extra.mult_odds) then card.lucky_trigger = true ret.mult = card.ability.extra.mult end if SMODS.pseudorandom_probability(card, 'vremade_lucky_money', 1, card.ability.extra.dollars_odds) then card.lucky_trigger = true ret.dollars = card.ability.extra.dollars end return ret end  if context.destroy_card and context.cardarea == G.play and context.destroy_card == card and SMODS.pseudorandom_probability(card, self.key, 1, card.ability.extra.odds) then card.glass_trigger = true return { remove = true } end end }
SMODS.Enhancement { key = "wildXglassXgoldXlucky", name = "Wild Glass Gold Lucky Card", atlas = "enhancement", pos = { x = 12, y = 12 }, config = { Xmult = 2, h_dollars = 3, extra = { odds = 4, mult = 20, dollars = 20, mult_odds = 5, dollars_odds = 15 } }, any_suit = true, shatters = true, in_pool = function(self,args) return false end, loc_vars = function(self, info_queue, card) local numerator, denominator = SMODS.get_probability_vars(card, 1, card.ability.extra.odds, self.key) local mult_numerator, mult_denominator = SMODS.get_probability_vars(card, 1, card.ability.extra.mult_odds, self.key) local dollars_numerator, dollars_denominator = SMODS.get_probability_vars(card, 1, card.ability.extra.dollars_odds, self.key) return { vars = { card.ability.Xmult, numerator, denominator, card.ability.h_dollars, mult_numerator, dollars_numerator, card.ability.extra.mult, mult_denominator, card.ability.extra.dollars, dollars_denominator } } end, calculate = function(self, card, context)  if context.main_scoring and context.cardarea == G.play then local ret = {} if SMODS.pseudorandom_probability(card, 'vremade_lucky_mult', 1, card.ability.extra.mult_odds) then card.lucky_trigger = true ret.mult = card.ability.extra.mult end if SMODS.pseudorandom_probability(card, 'vremade_lucky_money', 1, card.ability.extra.dollars_odds) then card.lucky_trigger = true ret.dollars = card.ability.extra.dollars end return ret end  if context.destroy_card and context.cardarea == G.play and context.destroy_card == card and SMODS.pseudorandom_probability(card, self.key, 1, card.ability.extra.odds) then card.glass_trigger = true return { remove = true } end end }
SMODS.Enhancement { key = "bonusXwildXglassXgoldXlucky", name = "Bonus Wild Glass Gold Lucky Card", atlas = "enhancement", pos = { x = 12, y = 13 }, config = { bonus = 30, Xmult = 2, h_dollars = 3, extra = { odds = 4, mult = 20, dollars = 20, mult_odds = 5, dollars_odds = 15 } }, any_suit = true, shatters = true, in_pool = function(self,args) return false end, loc_vars = function(self, info_queue, card) local numerator, denominator = SMODS.get_probability_vars(card, 1, card.ability.extra.odds, self.key) local mult_numerator, mult_denominator = SMODS.get_probability_vars(card, 1, card.ability.extra.mult_odds, self.key) local dollars_numerator, dollars_denominator = SMODS.get_probability_vars(card, 1, card.ability.extra.dollars_odds, self.key) return { vars = { card.ability.bonus, card.ability.Xmult, numerator, denominator, card.ability.h_dollars, mult_numerator, dollars_numerator, card.ability.extra.mult, mult_denominator, card.ability.extra.dollars, dollars_denominator } } end, calculate = function(self, card, context)  if context.main_scoring and context.cardarea == G.play then local ret = {} if SMODS.pseudorandom_probability(card, 'vremade_lucky_mult', 1, card.ability.extra.mult_odds) then card.lucky_trigger = true ret.mult = card.ability.extra.mult end if SMODS.pseudorandom_probability(card, 'vremade_lucky_money', 1, card.ability.extra.dollars_odds) then card.lucky_trigger = true ret.dollars = card.ability.extra.dollars end return ret end  if context.destroy_card and context.cardarea == G.play and context.destroy_card == card and SMODS.pseudorandom_probability(card, self.key, 1, card.ability.extra.odds) then card.glass_trigger = true return { remove = true } end end }
SMODS.Enhancement { key = "multXwildXglassXgoldXlucky", name = "Mult Wild Glass Gold Lucky Card", atlas = "enhancement", pos = { x = 12, y = 14 }, config = { mult = 4, Xmult = 2, h_dollars = 3, extra = { odds = 4, mult = 20, dollars = 20, mult_odds = 5, dollars_odds = 15 } }, any_suit = true, shatters = true, in_pool = function(self,args) return false end, loc_vars = function(self, info_queue, card) local numerator, denominator = SMODS.get_probability_vars(card, 1, card.ability.extra.odds, self.key) local mult_numerator, mult_denominator = SMODS.get_probability_vars(card, 1, card.ability.extra.mult_odds, self.key) local dollars_numerator, dollars_denominator = SMODS.get_probability_vars(card, 1, card.ability.extra.dollars_odds, self.key) return { vars = { card.ability.mult, card.ability.Xmult, numerator, denominator, card.ability.h_dollars, mult_numerator, dollars_numerator, card.ability.extra.mult, mult_denominator, card.ability.extra.dollars, dollars_denominator } } end, calculate = function(self, card, context)  if context.main_scoring and context.cardarea == G.play then local ret = {} if SMODS.pseudorandom_probability(card, 'vremade_lucky_mult', 1, card.ability.extra.mult_odds) then card.lucky_trigger = true ret.mult = card.ability.extra.mult end if SMODS.pseudorandom_probability(card, 'vremade_lucky_money', 1, card.ability.extra.dollars_odds) then card.lucky_trigger = true ret.dollars = card.ability.extra.dollars end return ret end  if context.destroy_card and context.cardarea == G.play and context.destroy_card == card and SMODS.pseudorandom_probability(card, self.key, 1, card.ability.extra.odds) then card.glass_trigger = true return { remove = true } end end }
SMODS.Enhancement { key = "bonusXmultXwildXglassXgoldXlucky", name = "Bonus Mult Wild Glass Gold Lucky Card", atlas = "enhancement", pos = { x = 12, y = 15 }, config = { bonus = 30, mult = 4, Xmult = 2, h_dollars = 3, extra = { odds = 4, mult = 20, dollars = 20, mult_odds = 5, dollars_odds = 15 } }, any_suit = true, shatters = true, in_pool = function(self,args) return false end, loc_vars = function(self, info_queue, card) local numerator, denominator = SMODS.get_probability_vars(card, 1, card.ability.extra.odds, self.key) local mult_numerator, mult_denominator = SMODS.get_probability_vars(card, 1, card.ability.extra.mult_odds, self.key) local dollars_numerator, dollars_denominator = SMODS.get_probability_vars(card, 1, card.ability.extra.dollars_odds, self.key) return { vars = { card.ability.bonus, card.ability.mult, card.ability.Xmult, numerator, denominator, card.ability.h_dollars, mult_numerator, dollars_numerator, card.ability.extra.mult, mult_denominator, card.ability.extra.dollars, dollars_denominator } } end, calculate = function(self, card, context)  if context.main_scoring and context.cardarea == G.play then local ret = {} if SMODS.pseudorandom_probability(card, 'vremade_lucky_mult', 1, card.ability.extra.mult_odds) then card.lucky_trigger = true ret.mult = card.ability.extra.mult end if SMODS.pseudorandom_probability(card, 'vremade_lucky_money', 1, card.ability.extra.dollars_odds) then card.lucky_trigger = true ret.dollars = card.ability.extra.dollars end return ret end  if context.destroy_card and context.cardarea == G.play and context.destroy_card == card and SMODS.pseudorandom_probability(card, self.key, 1, card.ability.extra.odds) then card.glass_trigger = true return { remove = true } end end }
SMODS.Enhancement { key = "steelXgoldXlucky", name = "Steel Gold Lucky Card", atlas = "enhancement", pos = { x = 13, y = 0 }, config = { h_x_mult = 1.5, h_dollars = 3, extra = { mult = 20, dollars = 20, mult_odds = 5, dollars_odds = 15 } }, in_pool = function(self,args) return false end, loc_vars = function(self, info_queue, card) local mult_numerator, mult_denominator = SMODS.get_probability_vars(card, 1, card.ability.extra.mult_odds, self.key) local dollars_numerator, dollars_denominator = SMODS.get_probability_vars(card, 1, card.ability.extra.dollars_odds, self.key) return { vars = { card.ability.h_x_mult, card.ability.h_dollars, mult_numerator, dollars_numerator, card.ability.extra.mult, mult_denominator, card.ability.extra.dollars, dollars_denominator } } end, calculate = function(self, card, context) if context.main_scoring and context.cardarea == G.play then local ret = {} if SMODS.pseudorandom_probability(card, 'vremade_lucky_mult', 1, card.ability.extra.mult_odds) then card.lucky_trigger = true ret.mult = card.ability.extra.mult end if SMODS.pseudorandom_probability(card, 'vremade_lucky_money', 1, card.ability.extra.dollars_odds) then card.lucky_trigger = true ret.dollars = card.ability.extra.dollars end return ret end end }
SMODS.Enhancement { key = "bonusXsteelXgoldXlucky", name = "Bonus Steel Gold Lucky Card", atlas = "enhancement", pos = { x = 13, y = 1 }, config = { bonus = 30, h_x_mult = 1.5, h_dollars = 3, extra = { mult = 20, dollars = 20, mult_odds = 5, dollars_odds = 15 } }, in_pool = function(self,args) return false end, loc_vars = function(self, info_queue, card) local mult_numerator, mult_denominator = SMODS.get_probability_vars(card, 1, card.ability.extra.mult_odds, self.key) local dollars_numerator, dollars_denominator = SMODS.get_probability_vars(card, 1, card.ability.extra.dollars_odds, self.key) return { vars = { card.ability.bonus, card.ability.h_x_mult, card.ability.h_dollars, mult_numerator, dollars_numerator, card.ability.extra.mult, mult_denominator, card.ability.extra.dollars, dollars_denominator } } end, calculate = function(self, card, context) if context.main_scoring and context.cardarea == G.play then local ret = {} if SMODS.pseudorandom_probability(card, 'vremade_lucky_mult', 1, card.ability.extra.mult_odds) then card.lucky_trigger = true ret.mult = card.ability.extra.mult end if SMODS.pseudorandom_probability(card, 'vremade_lucky_money', 1, card.ability.extra.dollars_odds) then card.lucky_trigger = true ret.dollars = card.ability.extra.dollars end return ret end end }
SMODS.Enhancement { key = "multXsteelXgoldXlucky", name = "Mult Steel Gold Lucky Card", atlas = "enhancement", pos = { x = 13, y = 2 }, config = { mult = 4, h_x_mult = 1.5, h_dollars = 3, extra = { mult = 20, dollars = 20, mult_odds = 5, dollars_odds = 15 } }, in_pool = function(self,args) return false end, loc_vars = function(self, info_queue, card) local mult_numerator, mult_denominator = SMODS.get_probability_vars(card, 1, card.ability.extra.mult_odds, self.key) local dollars_numerator, dollars_denominator = SMODS.get_probability_vars(card, 1, card.ability.extra.dollars_odds, self.key) return { vars = { card.ability.mult, card.ability.h_x_mult, card.ability.h_dollars, mult_numerator, dollars_numerator, card.ability.extra.mult, mult_denominator, card.ability.extra.dollars, dollars_denominator } } end, calculate = function(self, card, context) if context.main_scoring and context.cardarea == G.play then local ret = {} if SMODS.pseudorandom_probability(card, 'vremade_lucky_mult', 1, card.ability.extra.mult_odds) then card.lucky_trigger = true ret.mult = card.ability.extra.mult end if SMODS.pseudorandom_probability(card, 'vremade_lucky_money', 1, card.ability.extra.dollars_odds) then card.lucky_trigger = true ret.dollars = card.ability.extra.dollars end return ret end end }
SMODS.Enhancement { key = "bonusXmultXsteelXgoldXlucky", name = "Bonus Mult Steel Gold Lucky Card", atlas = "enhancement", pos = { x = 13, y = 3 }, config = { bonus = 30, mult = 4, h_x_mult = 1.5, h_dollars = 3, extra = { mult = 20, dollars = 20, mult_odds = 5, dollars_odds = 15 } }, in_pool = function(self,args) return false end, loc_vars = function(self, info_queue, card) local mult_numerator, mult_denominator = SMODS.get_probability_vars(card, 1, card.ability.extra.mult_odds, self.key) local dollars_numerator, dollars_denominator = SMODS.get_probability_vars(card, 1, card.ability.extra.dollars_odds, self.key) return { vars = { card.ability.bonus, card.ability.mult, card.ability.h_x_mult, card.ability.h_dollars, mult_numerator, dollars_numerator, card.ability.extra.mult, mult_denominator, card.ability.extra.dollars, dollars_denominator } } end, calculate = function(self, card, context) if context.main_scoring and context.cardarea == G.play then local ret = {} if SMODS.pseudorandom_probability(card, 'vremade_lucky_mult', 1, card.ability.extra.mult_odds) then card.lucky_trigger = true ret.mult = card.ability.extra.mult end if SMODS.pseudorandom_probability(card, 'vremade_lucky_money', 1, card.ability.extra.dollars_odds) then card.lucky_trigger = true ret.dollars = card.ability.extra.dollars end return ret end end }
SMODS.Enhancement { key = "wildXsteelXgoldXlucky", name = "Wild Steel Gold Lucky Card", atlas = "enhancement", pos = { x = 13, y = 4 }, config = { h_x_mult = 1.5, h_dollars = 3, extra = { mult = 20, dollars = 20, mult_odds = 5, dollars_odds = 15 } }, any_suit = true, in_pool = function(self,args) return false end, loc_vars = function(self, info_queue, card) local mult_numerator, mult_denominator = SMODS.get_probability_vars(card, 1, card.ability.extra.mult_odds, self.key) local dollars_numerator, dollars_denominator = SMODS.get_probability_vars(card, 1, card.ability.extra.dollars_odds, self.key) return { vars = { card.ability.h_x_mult, card.ability.h_dollars, mult_numerator, dollars_numerator, card.ability.extra.mult, mult_denominator, card.ability.extra.dollars, dollars_denominator } } end, calculate = function(self, card, context) if context.main_scoring and context.cardarea == G.play then local ret = {} if SMODS.pseudorandom_probability(card, 'vremade_lucky_mult', 1, card.ability.extra.mult_odds) then card.lucky_trigger = true ret.mult = card.ability.extra.mult end if SMODS.pseudorandom_probability(card, 'vremade_lucky_money', 1, card.ability.extra.dollars_odds) then card.lucky_trigger = true ret.dollars = card.ability.extra.dollars end return ret end end }
SMODS.Enhancement { key = "bonusXwildXsteelXgoldXlucky", name = "Bonus Wild Steel Gold Lucky Card", atlas = "enhancement", pos = { x = 13, y = 5 }, config = { bonus = 30, h_x_mult = 1.5, h_dollars = 3, extra = { mult = 20, dollars = 20, mult_odds = 5, dollars_odds = 15 } }, any_suit = true, in_pool = function(self,args) return false end, loc_vars = function(self, info_queue, card) local mult_numerator, mult_denominator = SMODS.get_probability_vars(card, 1, card.ability.extra.mult_odds, self.key) local dollars_numerator, dollars_denominator = SMODS.get_probability_vars(card, 1, card.ability.extra.dollars_odds, self.key) return { vars = { card.ability.bonus, card.ability.h_x_mult, card.ability.h_dollars, mult_numerator, dollars_numerator, card.ability.extra.mult, mult_denominator, card.ability.extra.dollars, dollars_denominator } } end, calculate = function(self, card, context) if context.main_scoring and context.cardarea == G.play then local ret = {} if SMODS.pseudorandom_probability(card, 'vremade_lucky_mult', 1, card.ability.extra.mult_odds) then card.lucky_trigger = true ret.mult = card.ability.extra.mult end if SMODS.pseudorandom_probability(card, 'vremade_lucky_money', 1, card.ability.extra.dollars_odds) then card.lucky_trigger = true ret.dollars = card.ability.extra.dollars end return ret end end }
SMODS.Enhancement { key = "multXwildXsteelXgoldXlucky", name = "Mult Wild Steel Gold Lucky Card", atlas = "enhancement", pos = { x = 13, y = 6 }, config = { mult = 4, h_x_mult = 1.5, h_dollars = 3, extra = { mult = 20, dollars = 20, mult_odds = 5, dollars_odds = 15 } }, any_suit = true, in_pool = function(self,args) return false end, loc_vars = function(self, info_queue, card) local mult_numerator, mult_denominator = SMODS.get_probability_vars(card, 1, card.ability.extra.mult_odds, self.key) local dollars_numerator, dollars_denominator = SMODS.get_probability_vars(card, 1, card.ability.extra.dollars_odds, self.key) return { vars = { card.ability.mult, card.ability.h_x_mult, card.ability.h_dollars, mult_numerator, dollars_numerator, card.ability.extra.mult, mult_denominator, card.ability.extra.dollars, dollars_denominator } } end, calculate = function(self, card, context) if context.main_scoring and context.cardarea == G.play then local ret = {} if SMODS.pseudorandom_probability(card, 'vremade_lucky_mult', 1, card.ability.extra.mult_odds) then card.lucky_trigger = true ret.mult = card.ability.extra.mult end if SMODS.pseudorandom_probability(card, 'vremade_lucky_money', 1, card.ability.extra.dollars_odds) then card.lucky_trigger = true ret.dollars = card.ability.extra.dollars end return ret end end }
SMODS.Enhancement { key = "bonusXmultXwildXsteelXgoldXlucky", name = "Bonus Mult Wild Steel Gold Lucky Card", atlas = "enhancement", pos = { x = 13, y = 7 }, config = { bonus = 30, mult = 4, h_x_mult = 1.5, h_dollars = 3, extra = { mult = 20, dollars = 20, mult_odds = 5, dollars_odds = 15 } }, any_suit = true, in_pool = function(self,args) return false end, loc_vars = function(self, info_queue, card) local mult_numerator, mult_denominator = SMODS.get_probability_vars(card, 1, card.ability.extra.mult_odds, self.key) local dollars_numerator, dollars_denominator = SMODS.get_probability_vars(card, 1, card.ability.extra.dollars_odds, self.key) return { vars = { card.ability.bonus, card.ability.mult, card.ability.h_x_mult, card.ability.h_dollars, mult_numerator, dollars_numerator, card.ability.extra.mult, mult_denominator, card.ability.extra.dollars, dollars_denominator } } end, calculate = function(self, card, context) if context.main_scoring and context.cardarea == G.play then local ret = {} if SMODS.pseudorandom_probability(card, 'vremade_lucky_mult', 1, card.ability.extra.mult_odds) then card.lucky_trigger = true ret.mult = card.ability.extra.mult end if SMODS.pseudorandom_probability(card, 'vremade_lucky_money', 1, card.ability.extra.dollars_odds) then card.lucky_trigger = true ret.dollars = card.ability.extra.dollars end return ret end end }
SMODS.Enhancement { key = "glassXsteelXgoldXlucky", name = "Glass Steel Gold Lucky Card", atlas = "enhancement", pos = { x = 13, y = 8 }, config = { Xmult = 2, h_x_mult = 1.5, h_dollars = 3, extra = { odds = 4, mult = 20, dollars = 20, mult_odds = 5, dollars_odds = 15 } }, shatters = true, in_pool = function(self,args) return false end, loc_vars = function(self, info_queue, card) local numerator, denominator = SMODS.get_probability_vars(card, 1, card.ability.extra.odds, self.key) local mult_numerator, mult_denominator = SMODS.get_probability_vars(card, 1, card.ability.extra.mult_odds, self.key) local dollars_numerator, dollars_denominator = SMODS.get_probability_vars(card, 1, card.ability.extra.dollars_odds, self.key) return { vars = { card.ability.Xmult, numerator, denominator, card.ability.h_x_mult, card.ability.h_dollars, mult_numerator, dollars_numerator, card.ability.extra.mult, mult_denominator, card.ability.extra.dollars, dollars_denominator } } end, calculate = function(self, card, context)  if context.main_scoring and context.cardarea == G.play then local ret = {} if SMODS.pseudorandom_probability(card, 'vremade_lucky_mult', 1, card.ability.extra.mult_odds) then card.lucky_trigger = true ret.mult = card.ability.extra.mult end if SMODS.pseudorandom_probability(card, 'vremade_lucky_money', 1, card.ability.extra.dollars_odds) then card.lucky_trigger = true ret.dollars = card.ability.extra.dollars end return ret end  if context.destroy_card and context.cardarea == G.play and context.destroy_card == card and SMODS.pseudorandom_probability(card, self.key, 1, card.ability.extra.odds) then card.glass_trigger = true return { remove = true } end end }
SMODS.Enhancement { key = "bonusXglassXsteelXgoldXlucky", name = "Bonus Glass Steel Gold Lucky Card", atlas = "enhancement", pos = { x = 13, y = 9 }, config = { bonus = 30, Xmult = 2, h_x_mult = 1.5, h_dollars = 3, extra = { odds = 4, mult = 20, dollars = 20, mult_odds = 5, dollars_odds = 15 } }, shatters = true, in_pool = function(self,args) return false end, loc_vars = function(self, info_queue, card) local numerator, denominator = SMODS.get_probability_vars(card, 1, card.ability.extra.odds, self.key) local mult_numerator, mult_denominator = SMODS.get_probability_vars(card, 1, card.ability.extra.mult_odds, self.key) local dollars_numerator, dollars_denominator = SMODS.get_probability_vars(card, 1, card.ability.extra.dollars_odds, self.key) return { vars = { card.ability.bonus, card.ability.Xmult, numerator, denominator, card.ability.h_x_mult, card.ability.h_dollars, mult_numerator, dollars_numerator, card.ability.extra.mult, mult_denominator, card.ability.extra.dollars, dollars_denominator } } end, calculate = function(self, card, context)  if context.main_scoring and context.cardarea == G.play then local ret = {} if SMODS.pseudorandom_probability(card, 'vremade_lucky_mult', 1, card.ability.extra.mult_odds) then card.lucky_trigger = true ret.mult = card.ability.extra.mult end if SMODS.pseudorandom_probability(card, 'vremade_lucky_money', 1, card.ability.extra.dollars_odds) then card.lucky_trigger = true ret.dollars = card.ability.extra.dollars end return ret end  if context.destroy_card and context.cardarea == G.play and context.destroy_card == card and SMODS.pseudorandom_probability(card, self.key, 1, card.ability.extra.odds) then card.glass_trigger = true return { remove = true } end end }
SMODS.Enhancement { key = "multXglassXsteelXgoldXlucky", name = "Mult Glass Steel Gold Lucky Card", atlas = "enhancement", pos = { x = 13, y = 10 }, config = { mult = 4, Xmult = 2, h_x_mult = 1.5, h_dollars = 3, extra = { odds = 4, mult = 20, dollars = 20, mult_odds = 5, dollars_odds = 15 } }, shatters = true, in_pool = function(self,args) return false end, loc_vars = function(self, info_queue, card) local numerator, denominator = SMODS.get_probability_vars(card, 1, card.ability.extra.odds, self.key) local mult_numerator, mult_denominator = SMODS.get_probability_vars(card, 1, card.ability.extra.mult_odds, self.key) local dollars_numerator, dollars_denominator = SMODS.get_probability_vars(card, 1, card.ability.extra.dollars_odds, self.key) return { vars = { card.ability.mult, card.ability.Xmult, numerator, denominator, card.ability.h_x_mult, card.ability.h_dollars, mult_numerator, dollars_numerator, card.ability.extra.mult, mult_denominator, card.ability.extra.dollars, dollars_denominator } } end, calculate = function(self, card, context)  if context.main_scoring and context.cardarea == G.play then local ret = {} if SMODS.pseudorandom_probability(card, 'vremade_lucky_mult', 1, card.ability.extra.mult_odds) then card.lucky_trigger = true ret.mult = card.ability.extra.mult end if SMODS.pseudorandom_probability(card, 'vremade_lucky_money', 1, card.ability.extra.dollars_odds) then card.lucky_trigger = true ret.dollars = card.ability.extra.dollars end return ret end  if context.destroy_card and context.cardarea == G.play and context.destroy_card == card and SMODS.pseudorandom_probability(card, self.key, 1, card.ability.extra.odds) then card.glass_trigger = true return { remove = true } end end }
SMODS.Enhancement { key = "bonusXmultXglassXsteelXgoldXlucky", name = "Bonus Mult Glass Steel Gold Lucky Card", atlas = "enhancement", pos = { x = 13, y = 11 }, config = { bonus = 30, mult = 4, Xmult = 2, h_x_mult = 1.5, h_dollars = 3, extra = { odds = 4, mult = 20, dollars = 20, mult_odds = 5, dollars_odds = 15 } }, shatters = true, in_pool = function(self,args) return false end, loc_vars = function(self, info_queue, card) local numerator, denominator = SMODS.get_probability_vars(card, 1, card.ability.extra.odds, self.key) local mult_numerator, mult_denominator = SMODS.get_probability_vars(card, 1, card.ability.extra.mult_odds, self.key) local dollars_numerator, dollars_denominator = SMODS.get_probability_vars(card, 1, card.ability.extra.dollars_odds, self.key) return { vars = { card.ability.bonus, card.ability.mult, card.ability.Xmult, numerator, denominator, card.ability.h_x_mult, card.ability.h_dollars, mult_numerator, dollars_numerator, card.ability.extra.mult, mult_denominator, card.ability.extra.dollars, dollars_denominator } } end, calculate = function(self, card, context)  if context.main_scoring and context.cardarea == G.play then local ret = {} if SMODS.pseudorandom_probability(card, 'vremade_lucky_mult', 1, card.ability.extra.mult_odds) then card.lucky_trigger = true ret.mult = card.ability.extra.mult end if SMODS.pseudorandom_probability(card, 'vremade_lucky_money', 1, card.ability.extra.dollars_odds) then card.lucky_trigger = true ret.dollars = card.ability.extra.dollars end return ret end  if context.destroy_card and context.cardarea == G.play and context.destroy_card == card and SMODS.pseudorandom_probability(card, self.key, 1, card.ability.extra.odds) then card.glass_trigger = true return { remove = true } end end }
SMODS.Enhancement { key = "wildXglassXsteelXgoldXlucky", name = "Wild Glass Steel Gold Lucky Card", atlas = "enhancement", pos = { x = 13, y = 12 }, config = { Xmult = 2, h_x_mult = 1.5, h_dollars = 3, extra = { odds = 4, mult = 20, dollars = 20, mult_odds = 5, dollars_odds = 15 } }, any_suit = true, shatters = true, in_pool = function(self,args) return false end, loc_vars = function(self, info_queue, card) local numerator, denominator = SMODS.get_probability_vars(card, 1, card.ability.extra.odds, self.key) local mult_numerator, mult_denominator = SMODS.get_probability_vars(card, 1, card.ability.extra.mult_odds, self.key) local dollars_numerator, dollars_denominator = SMODS.get_probability_vars(card, 1, card.ability.extra.dollars_odds, self.key) return { vars = { card.ability.Xmult, numerator, denominator, card.ability.h_x_mult, card.ability.h_dollars, mult_numerator, dollars_numerator, card.ability.extra.mult, mult_denominator, card.ability.extra.dollars, dollars_denominator } } end, calculate = function(self, card, context)  if context.main_scoring and context.cardarea == G.play then local ret = {} if SMODS.pseudorandom_probability(card, 'vremade_lucky_mult', 1, card.ability.extra.mult_odds) then card.lucky_trigger = true ret.mult = card.ability.extra.mult end if SMODS.pseudorandom_probability(card, 'vremade_lucky_money', 1, card.ability.extra.dollars_odds) then card.lucky_trigger = true ret.dollars = card.ability.extra.dollars end return ret end  if context.destroy_card and context.cardarea == G.play and context.destroy_card == card and SMODS.pseudorandom_probability(card, self.key, 1, card.ability.extra.odds) then card.glass_trigger = true return { remove = true } end end }
SMODS.Enhancement { key = "bonusXwildXglassXsteelXgoldXlucky", name = "Bonus Wild Glass Steel Gold Lucky Card", atlas = "enhancement", pos = { x = 13, y = 13 }, config = { bonus = 30, Xmult = 2, h_x_mult = 1.5, h_dollars = 3, extra = { odds = 4, mult = 20, dollars = 20, mult_odds = 5, dollars_odds = 15 } }, any_suit = true, shatters = true, in_pool = function(self,args) return false end, loc_vars = function(self, info_queue, card) local numerator, denominator = SMODS.get_probability_vars(card, 1, card.ability.extra.odds, self.key) local mult_numerator, mult_denominator = SMODS.get_probability_vars(card, 1, card.ability.extra.mult_odds, self.key) local dollars_numerator, dollars_denominator = SMODS.get_probability_vars(card, 1, card.ability.extra.dollars_odds, self.key) return { vars = { card.ability.bonus, card.ability.Xmult, numerator, denominator, card.ability.h_x_mult, card.ability.h_dollars, mult_numerator, dollars_numerator, card.ability.extra.mult, mult_denominator, card.ability.extra.dollars, dollars_denominator } } end, calculate = function(self, card, context)  if context.main_scoring and context.cardarea == G.play then local ret = {} if SMODS.pseudorandom_probability(card, 'vremade_lucky_mult', 1, card.ability.extra.mult_odds) then card.lucky_trigger = true ret.mult = card.ability.extra.mult end if SMODS.pseudorandom_probability(card, 'vremade_lucky_money', 1, card.ability.extra.dollars_odds) then card.lucky_trigger = true ret.dollars = card.ability.extra.dollars end return ret end  if context.destroy_card and context.cardarea == G.play and context.destroy_card == card and SMODS.pseudorandom_probability(card, self.key, 1, card.ability.extra.odds) then card.glass_trigger = true return { remove = true } end end }
SMODS.Enhancement { key = "multXwildXglassXsteelXgoldXlucky", name = "Mult Wild Glass Steel Gold Lucky Card", atlas = "enhancement", pos = { x = 13, y = 14 }, config = { mult = 4, Xmult = 2, h_x_mult = 1.5, h_dollars = 3, extra = { odds = 4, mult = 20, dollars = 20, mult_odds = 5, dollars_odds = 15 } }, any_suit = true, shatters = true, in_pool = function(self,args) return false end, loc_vars = function(self, info_queue, card) local numerator, denominator = SMODS.get_probability_vars(card, 1, card.ability.extra.odds, self.key) local mult_numerator, mult_denominator = SMODS.get_probability_vars(card, 1, card.ability.extra.mult_odds, self.key) local dollars_numerator, dollars_denominator = SMODS.get_probability_vars(card, 1, card.ability.extra.dollars_odds, self.key) return { vars = { card.ability.mult, card.ability.Xmult, numerator, denominator, card.ability.h_x_mult, card.ability.h_dollars, mult_numerator, dollars_numerator, card.ability.extra.mult, mult_denominator, card.ability.extra.dollars, dollars_denominator } } end, calculate = function(self, card, context)  if context.main_scoring and context.cardarea == G.play then local ret = {} if SMODS.pseudorandom_probability(card, 'vremade_lucky_mult', 1, card.ability.extra.mult_odds) then card.lucky_trigger = true ret.mult = card.ability.extra.mult end if SMODS.pseudorandom_probability(card, 'vremade_lucky_money', 1, card.ability.extra.dollars_odds) then card.lucky_trigger = true ret.dollars = card.ability.extra.dollars end return ret end  if context.destroy_card and context.cardarea == G.play and context.destroy_card == card and SMODS.pseudorandom_probability(card, self.key, 1, card.ability.extra.odds) then card.glass_trigger = true return { remove = true } end end }
SMODS.Enhancement { key = "bonusXmultXwildXglassXsteelXgoldXlucky", name = "Bonus Mult Wild Glass Steel Gold Lucky Card", atlas = "enhancement", pos = { x = 13, y = 15 }, config = { bonus = 30, mult = 4, Xmult = 2, h_x_mult = 1.5, h_dollars = 3, extra = { odds = 4, mult = 20, dollars = 20, mult_odds = 5, dollars_odds = 15 } }, any_suit = true, shatters = true, in_pool = function(self,args) return false end, loc_vars = function(self, info_queue, card) local numerator, denominator = SMODS.get_probability_vars(card, 1, card.ability.extra.odds, self.key) local mult_numerator, mult_denominator = SMODS.get_probability_vars(card, 1, card.ability.extra.mult_odds, self.key) local dollars_numerator, dollars_denominator = SMODS.get_probability_vars(card, 1, card.ability.extra.dollars_odds, self.key) return { vars = { card.ability.bonus, card.ability.mult, card.ability.Xmult, numerator, denominator, card.ability.h_x_mult, card.ability.h_dollars, mult_numerator, dollars_numerator, card.ability.extra.mult, mult_denominator, card.ability.extra.dollars, dollars_denominator } } end, calculate = function(self, card, context)  if context.main_scoring and context.cardarea == G.play then local ret = {} if SMODS.pseudorandom_probability(card, 'vremade_lucky_mult', 1, card.ability.extra.mult_odds) then card.lucky_trigger = true ret.mult = card.ability.extra.mult end if SMODS.pseudorandom_probability(card, 'vremade_lucky_money', 1, card.ability.extra.dollars_odds) then card.lucky_trigger = true ret.dollars = card.ability.extra.dollars end return ret end  if context.destroy_card and context.cardarea == G.play and context.destroy_card == card and SMODS.pseudorandom_probability(card, self.key, 1, card.ability.extra.odds) then card.glass_trigger = true return { remove = true } end end }
SMODS.Enhancement { key = "stoneXgoldXlucky", name = "Stone Gold Lucky Card", atlas = "enhancement", pos = { x = 14, y = 0 }, config = { bonus = 50, h_dollars = 3, extra = { mult = 20, dollars = 20, mult_odds = 5, dollars_odds = 15 } }, replace_base_card = true,no_rank = true,no_suit = true,always_scores = true, in_pool = function(self,args) return false end, loc_vars = function(self, info_queue, card) local mult_numerator, mult_denominator = SMODS.get_probability_vars(card, 1, card.ability.extra.mult_odds, self.key) local dollars_numerator, dollars_denominator = SMODS.get_probability_vars(card, 1, card.ability.extra.dollars_odds, self.key) return { vars = { card.ability.bonus, card.ability.h_dollars, mult_numerator, dollars_numerator, card.ability.extra.mult, mult_denominator, card.ability.extra.dollars, dollars_denominator } } end, calculate = function(self, card, context) if context.main_scoring and context.cardarea == G.play then local ret = {} if SMODS.pseudorandom_probability(card, 'vremade_lucky_mult', 1, card.ability.extra.mult_odds) then card.lucky_trigger = true ret.mult = card.ability.extra.mult end if SMODS.pseudorandom_probability(card, 'vremade_lucky_money', 1, card.ability.extra.dollars_odds) then card.lucky_trigger = true ret.dollars = card.ability.extra.dollars end return ret end end }
SMODS.Enhancement { key = "bonusXstoneXgoldXlucky", name = "Bonus Stone Gold Lucky Card", atlas = "enhancement", pos = { x = 14, y = 1 }, config = { bonus = 80, h_dollars = 3, extra = { mult = 20, dollars = 20, mult_odds = 5, dollars_odds = 15 } }, replace_base_card = true,no_rank = true,no_suit = true,always_scores = true, in_pool = function(self,args) return false end, loc_vars = function(self, info_queue, card) local mult_numerator, mult_denominator = SMODS.get_probability_vars(card, 1, card.ability.extra.mult_odds, self.key) local dollars_numerator, dollars_denominator = SMODS.get_probability_vars(card, 1, card.ability.extra.dollars_odds, self.key) return { vars = { card.ability.bonus, card.ability.h_dollars, mult_numerator, dollars_numerator, card.ability.extra.mult, mult_denominator, card.ability.extra.dollars, dollars_denominator } } end, calculate = function(self, card, context) if context.main_scoring and context.cardarea == G.play then local ret = {} if SMODS.pseudorandom_probability(card, 'vremade_lucky_mult', 1, card.ability.extra.mult_odds) then card.lucky_trigger = true ret.mult = card.ability.extra.mult end if SMODS.pseudorandom_probability(card, 'vremade_lucky_money', 1, card.ability.extra.dollars_odds) then card.lucky_trigger = true ret.dollars = card.ability.extra.dollars end return ret end end }
SMODS.Enhancement { key = "multXstoneXgoldXlucky", name = "Mult Stone Gold Lucky Card", atlas = "enhancement", pos = { x = 14, y = 2 }, config = { mult = 4, bonus = 50, h_dollars = 3, extra = { mult = 20, dollars = 20, mult_odds = 5, dollars_odds = 15 } }, replace_base_card = true,no_rank = true,no_suit = true,always_scores = true, in_pool = function(self,args) return false end, loc_vars = function(self, info_queue, card) local mult_numerator, mult_denominator = SMODS.get_probability_vars(card, 1, card.ability.extra.mult_odds, self.key) local dollars_numerator, dollars_denominator = SMODS.get_probability_vars(card, 1, card.ability.extra.dollars_odds, self.key) return { vars = { card.ability.mult, card.ability.bonus, card.ability.h_dollars, mult_numerator, dollars_numerator, card.ability.extra.mult, mult_denominator, card.ability.extra.dollars, dollars_denominator } } end, calculate = function(self, card, context) if context.main_scoring and context.cardarea == G.play then local ret = {} if SMODS.pseudorandom_probability(card, 'vremade_lucky_mult', 1, card.ability.extra.mult_odds) then card.lucky_trigger = true ret.mult = card.ability.extra.mult end if SMODS.pseudorandom_probability(card, 'vremade_lucky_money', 1, card.ability.extra.dollars_odds) then card.lucky_trigger = true ret.dollars = card.ability.extra.dollars end return ret end end }
SMODS.Enhancement { key = "bonusXmultXstoneXgoldXlucky", name = "Bonus Mult Stone Gold Lucky Card", atlas = "enhancement", pos = { x = 14, y = 3 }, config = { bonus = 80, mult = 4, h_dollars = 3, extra = { mult = 20, dollars = 20, mult_odds = 5, dollars_odds = 15 } }, replace_base_card = true,no_rank = true,no_suit = true,always_scores = true, in_pool = function(self,args) return false end, loc_vars = function(self, info_queue, card) local mult_numerator, mult_denominator = SMODS.get_probability_vars(card, 1, card.ability.extra.mult_odds, self.key) local dollars_numerator, dollars_denominator = SMODS.get_probability_vars(card, 1, card.ability.extra.dollars_odds, self.key) return { vars = { card.ability.bonus, card.ability.mult, card.ability.h_dollars, mult_numerator, dollars_numerator, card.ability.extra.mult, mult_denominator, card.ability.extra.dollars, dollars_denominator } } end, calculate = function(self, card, context) if context.main_scoring and context.cardarea == G.play then local ret = {} if SMODS.pseudorandom_probability(card, 'vremade_lucky_mult', 1, card.ability.extra.mult_odds) then card.lucky_trigger = true ret.mult = card.ability.extra.mult end if SMODS.pseudorandom_probability(card, 'vremade_lucky_money', 1, card.ability.extra.dollars_odds) then card.lucky_trigger = true ret.dollars = card.ability.extra.dollars end return ret end end }
SMODS.Enhancement { key = "wildXstoneXgoldXlucky", name = "Wild Stone Gold Lucky Card", atlas = "enhancement", pos = { x = 14, y = 4 }, config = { bonus = 50, h_dollars = 3, extra = { mult = 20, dollars = 20, mult_odds = 5, dollars_odds = 15 } }, any_suit = true, replace_base_card = true,no_rank = true,always_scores = true, in_pool = function(self,args) return false end, loc_vars = function(self, info_queue, card) local mult_numerator, mult_denominator = SMODS.get_probability_vars(card, 1, card.ability.extra.mult_odds, self.key) local dollars_numerator, dollars_denominator = SMODS.get_probability_vars(card, 1, card.ability.extra.dollars_odds, self.key) return { vars = { card.ability.bonus, card.ability.h_dollars, mult_numerator, dollars_numerator, card.ability.extra.mult, mult_denominator, card.ability.extra.dollars, dollars_denominator } } end, calculate = function(self, card, context) if context.main_scoring and context.cardarea == G.play then local ret = {} if SMODS.pseudorandom_probability(card, 'vremade_lucky_mult', 1, card.ability.extra.mult_odds) then card.lucky_trigger = true ret.mult = card.ability.extra.mult end if SMODS.pseudorandom_probability(card, 'vremade_lucky_money', 1, card.ability.extra.dollars_odds) then card.lucky_trigger = true ret.dollars = card.ability.extra.dollars end return ret end end }
SMODS.Enhancement { key = "bonusXwildXstoneXgoldXlucky", name = "Bonus Wild Stone Gold Lucky Card", atlas = "enhancement", pos = { x = 14, y = 5 }, config = { bonus = 80, h_dollars = 3, extra = { mult = 20, dollars = 20, mult_odds = 5, dollars_odds = 15 } }, any_suit = true, replace_base_card = true,no_rank = true,always_scores = true, in_pool = function(self,args) return false end, loc_vars = function(self, info_queue, card) local mult_numerator, mult_denominator = SMODS.get_probability_vars(card, 1, card.ability.extra.mult_odds, self.key) local dollars_numerator, dollars_denominator = SMODS.get_probability_vars(card, 1, card.ability.extra.dollars_odds, self.key) return { vars = { card.ability.bonus, card.ability.h_dollars, mult_numerator, dollars_numerator, card.ability.extra.mult, mult_denominator, card.ability.extra.dollars, dollars_denominator } } end, calculate = function(self, card, context) if context.main_scoring and context.cardarea == G.play then local ret = {} if SMODS.pseudorandom_probability(card, 'vremade_lucky_mult', 1, card.ability.extra.mult_odds) then card.lucky_trigger = true ret.mult = card.ability.extra.mult end if SMODS.pseudorandom_probability(card, 'vremade_lucky_money', 1, card.ability.extra.dollars_odds) then card.lucky_trigger = true ret.dollars = card.ability.extra.dollars end return ret end end }
SMODS.Enhancement { key = "multXwildXstoneXgoldXlucky", name = "Mult Wild Stone Gold Lucky Card", atlas = "enhancement", pos = { x = 14, y = 6 }, config = { mult = 4, bonus = 50, h_dollars = 3, extra = { mult = 20, dollars = 20, mult_odds = 5, dollars_odds = 15 } }, any_suit = true, replace_base_card = true,no_rank = true,always_scores = true, in_pool = function(self,args) return false end, loc_vars = function(self, info_queue, card) local mult_numerator, mult_denominator = SMODS.get_probability_vars(card, 1, card.ability.extra.mult_odds, self.key) local dollars_numerator, dollars_denominator = SMODS.get_probability_vars(card, 1, card.ability.extra.dollars_odds, self.key) return { vars = { card.ability.mult, card.ability.bonus, card.ability.h_dollars, mult_numerator, dollars_numerator, card.ability.extra.mult, mult_denominator, card.ability.extra.dollars, dollars_denominator } } end, calculate = function(self, card, context) if context.main_scoring and context.cardarea == G.play then local ret = {} if SMODS.pseudorandom_probability(card, 'vremade_lucky_mult', 1, card.ability.extra.mult_odds) then card.lucky_trigger = true ret.mult = card.ability.extra.mult end if SMODS.pseudorandom_probability(card, 'vremade_lucky_money', 1, card.ability.extra.dollars_odds) then card.lucky_trigger = true ret.dollars = card.ability.extra.dollars end return ret end end }
SMODS.Enhancement { key = "bonusXmultXwildXstoneXgoldXlucky", name = "Bonus Mult Wild Stone Gold Lucky Card", atlas = "enhancement", pos = { x = 14, y = 7 }, config = { bonus = 80, mult = 4, h_dollars = 3, extra = { mult = 20, dollars = 20, mult_odds = 5, dollars_odds = 15 } }, any_suit = true, replace_base_card = true,no_rank = true,always_scores = true, in_pool = function(self,args) return false end, loc_vars = function(self, info_queue, card) local mult_numerator, mult_denominator = SMODS.get_probability_vars(card, 1, card.ability.extra.mult_odds, self.key) local dollars_numerator, dollars_denominator = SMODS.get_probability_vars(card, 1, card.ability.extra.dollars_odds, self.key) return { vars = { card.ability.bonus, card.ability.mult, card.ability.h_dollars, mult_numerator, dollars_numerator, card.ability.extra.mult, mult_denominator, card.ability.extra.dollars, dollars_denominator } } end, calculate = function(self, card, context) if context.main_scoring and context.cardarea == G.play then local ret = {} if SMODS.pseudorandom_probability(card, 'vremade_lucky_mult', 1, card.ability.extra.mult_odds) then card.lucky_trigger = true ret.mult = card.ability.extra.mult end if SMODS.pseudorandom_probability(card, 'vremade_lucky_money', 1, card.ability.extra.dollars_odds) then card.lucky_trigger = true ret.dollars = card.ability.extra.dollars end return ret end end }
SMODS.Enhancement { key = "glassXstoneXgoldXlucky", name = "Glass Stone Gold Lucky Card", atlas = "enhancement", pos = { x = 14, y = 8 }, config = { Xmult = 2, bonus = 50, h_dollars = 3, extra = { odds = 4, mult = 20, dollars = 20, mult_odds = 5, dollars_odds = 15 } }, shatters = true, replace_base_card = true,no_rank = true,no_suit = true,always_scores = true, in_pool = function(self,args) return false end, loc_vars = function(self, info_queue, card) local numerator, denominator = SMODS.get_probability_vars(card, 1, card.ability.extra.odds, self.key) local mult_numerator, mult_denominator = SMODS.get_probability_vars(card, 1, card.ability.extra.mult_odds, self.key) local dollars_numerator, dollars_denominator = SMODS.get_probability_vars(card, 1, card.ability.extra.dollars_odds, self.key) return { vars = { card.ability.Xmult, numerator, denominator, card.ability.bonus, card.ability.h_dollars, mult_numerator, dollars_numerator, card.ability.extra.mult, mult_denominator, card.ability.extra.dollars, dollars_denominator } } end, calculate = function(self, card, context)  if context.main_scoring and context.cardarea == G.play then local ret = {} if SMODS.pseudorandom_probability(card, 'vremade_lucky_mult', 1, card.ability.extra.mult_odds) then card.lucky_trigger = true ret.mult = card.ability.extra.mult end if SMODS.pseudorandom_probability(card, 'vremade_lucky_money', 1, card.ability.extra.dollars_odds) then card.lucky_trigger = true ret.dollars = card.ability.extra.dollars end return ret end  if context.destroy_card and context.cardarea == G.play and context.destroy_card == card and SMODS.pseudorandom_probability(card, self.key, 1, card.ability.extra.odds) then card.glass_trigger = true return { remove = true } end end }
SMODS.Enhancement { key = "bonusXglassXstoneXgoldXlucky", name = "Bonus Glass Stone Gold Lucky Card", atlas = "enhancement", pos = { x = 14, y = 9 }, config = { bonus = 80, Xmult = 2, h_dollars = 3, extra = { odds = 4, mult = 20, dollars = 20, mult_odds = 5, dollars_odds = 15 } }, shatters = true, replace_base_card = true,no_rank = true,no_suit = true,always_scores = true, in_pool = function(self,args) return false end, loc_vars = function(self, info_queue, card) local numerator, denominator = SMODS.get_probability_vars(card, 1, card.ability.extra.odds, self.key) local mult_numerator, mult_denominator = SMODS.get_probability_vars(card, 1, card.ability.extra.mult_odds, self.key) local dollars_numerator, dollars_denominator = SMODS.get_probability_vars(card, 1, card.ability.extra.dollars_odds, self.key) return { vars = { card.ability.bonus, card.ability.Xmult, numerator, denominator, card.ability.h_dollars, mult_numerator, dollars_numerator, card.ability.extra.mult, mult_denominator, card.ability.extra.dollars, dollars_denominator } } end, calculate = function(self, card, context)  if context.main_scoring and context.cardarea == G.play then local ret = {} if SMODS.pseudorandom_probability(card, 'vremade_lucky_mult', 1, card.ability.extra.mult_odds) then card.lucky_trigger = true ret.mult = card.ability.extra.mult end if SMODS.pseudorandom_probability(card, 'vremade_lucky_money', 1, card.ability.extra.dollars_odds) then card.lucky_trigger = true ret.dollars = card.ability.extra.dollars end return ret end  if context.destroy_card and context.cardarea == G.play and context.destroy_card == card and SMODS.pseudorandom_probability(card, self.key, 1, card.ability.extra.odds) then card.glass_trigger = true return { remove = true } end end }
SMODS.Enhancement { key = "multXglassXstoneXgoldXlucky", name = "Mult Glass Stone Gold Lucky Card", atlas = "enhancement", pos = { x = 14, y = 10 }, config = { mult = 4, Xmult = 2, bonus = 50, h_dollars = 3, extra = { odds = 4, mult = 20, dollars = 20, mult_odds = 5, dollars_odds = 15 } }, shatters = true, replace_base_card = true,no_rank = true,no_suit = true,always_scores = true, in_pool = function(self,args) return false end, loc_vars = function(self, info_queue, card) local numerator, denominator = SMODS.get_probability_vars(card, 1, card.ability.extra.odds, self.key) local mult_numerator, mult_denominator = SMODS.get_probability_vars(card, 1, card.ability.extra.mult_odds, self.key) local dollars_numerator, dollars_denominator = SMODS.get_probability_vars(card, 1, card.ability.extra.dollars_odds, self.key) return { vars = { card.ability.mult, card.ability.Xmult, numerator, denominator, card.ability.bonus, card.ability.h_dollars, mult_numerator, dollars_numerator, card.ability.extra.mult, mult_denominator, card.ability.extra.dollars, dollars_denominator } } end, calculate = function(self, card, context)  if context.main_scoring and context.cardarea == G.play then local ret = {} if SMODS.pseudorandom_probability(card, 'vremade_lucky_mult', 1, card.ability.extra.mult_odds) then card.lucky_trigger = true ret.mult = card.ability.extra.mult end if SMODS.pseudorandom_probability(card, 'vremade_lucky_money', 1, card.ability.extra.dollars_odds) then card.lucky_trigger = true ret.dollars = card.ability.extra.dollars end return ret end  if context.destroy_card and context.cardarea == G.play and context.destroy_card == card and SMODS.pseudorandom_probability(card, self.key, 1, card.ability.extra.odds) then card.glass_trigger = true return { remove = true } end end }
SMODS.Enhancement { key = "bonusXmultXglassXstoneXgoldXlucky", name = "Bonus Mult Glass Stone Gold Lucky Card", atlas = "enhancement", pos = { x = 14, y = 11 }, config = { bonus = 80, mult = 4, Xmult = 2, h_dollars = 3, extra = { odds = 4, mult = 20, dollars = 20, mult_odds = 5, dollars_odds = 15 } }, shatters = true, replace_base_card = true,no_rank = true,no_suit = true,always_scores = true, in_pool = function(self,args) return false end, loc_vars = function(self, info_queue, card) local numerator, denominator = SMODS.get_probability_vars(card, 1, card.ability.extra.odds, self.key) local mult_numerator, mult_denominator = SMODS.get_probability_vars(card, 1, card.ability.extra.mult_odds, self.key) local dollars_numerator, dollars_denominator = SMODS.get_probability_vars(card, 1, card.ability.extra.dollars_odds, self.key) return { vars = { card.ability.bonus, card.ability.mult, card.ability.Xmult, numerator, denominator, card.ability.h_dollars, mult_numerator, dollars_numerator, card.ability.extra.mult, mult_denominator, card.ability.extra.dollars, dollars_denominator } } end, calculate = function(self, card, context)  if context.main_scoring and context.cardarea == G.play then local ret = {} if SMODS.pseudorandom_probability(card, 'vremade_lucky_mult', 1, card.ability.extra.mult_odds) then card.lucky_trigger = true ret.mult = card.ability.extra.mult end if SMODS.pseudorandom_probability(card, 'vremade_lucky_money', 1, card.ability.extra.dollars_odds) then card.lucky_trigger = true ret.dollars = card.ability.extra.dollars end return ret end  if context.destroy_card and context.cardarea == G.play and context.destroy_card == card and SMODS.pseudorandom_probability(card, self.key, 1, card.ability.extra.odds) then card.glass_trigger = true return { remove = true } end end }
SMODS.Enhancement { key = "wildXglassXstoneXgoldXlucky", name = "Wild Glass Stone Gold Lucky Card", atlas = "enhancement", pos = { x = 14, y = 12 }, config = { Xmult = 2, bonus = 50, h_dollars = 3, extra = { odds = 4, mult = 20, dollars = 20, mult_odds = 5, dollars_odds = 15 } }, any_suit = true, shatters = true, replace_base_card = true,no_rank = true,always_scores = true, in_pool = function(self,args) return false end, loc_vars = function(self, info_queue, card) local numerator, denominator = SMODS.get_probability_vars(card, 1, card.ability.extra.odds, self.key) local mult_numerator, mult_denominator = SMODS.get_probability_vars(card, 1, card.ability.extra.mult_odds, self.key) local dollars_numerator, dollars_denominator = SMODS.get_probability_vars(card, 1, card.ability.extra.dollars_odds, self.key) return { vars = { card.ability.Xmult, numerator, denominator, card.ability.bonus, card.ability.h_dollars, mult_numerator, dollars_numerator, card.ability.extra.mult, mult_denominator, card.ability.extra.dollars, dollars_denominator } } end, calculate = function(self, card, context)  if context.main_scoring and context.cardarea == G.play then local ret = {} if SMODS.pseudorandom_probability(card, 'vremade_lucky_mult', 1, card.ability.extra.mult_odds) then card.lucky_trigger = true ret.mult = card.ability.extra.mult end if SMODS.pseudorandom_probability(card, 'vremade_lucky_money', 1, card.ability.extra.dollars_odds) then card.lucky_trigger = true ret.dollars = card.ability.extra.dollars end return ret end  if context.destroy_card and context.cardarea == G.play and context.destroy_card == card and SMODS.pseudorandom_probability(card, self.key, 1, card.ability.extra.odds) then card.glass_trigger = true return { remove = true } end end }
SMODS.Enhancement { key = "bonusXwildXglassXstoneXgoldXlucky", name = "Bonus Wild Glass Stone Gold Lucky Card", atlas = "enhancement", pos = { x = 14, y = 13 }, config = { bonus = 80, Xmult = 2, h_dollars = 3, extra = { odds = 4, mult = 20, dollars = 20, mult_odds = 5, dollars_odds = 15 } }, any_suit = true, shatters = true, replace_base_card = true,no_rank = true,always_scores = true, in_pool = function(self,args) return false end, loc_vars = function(self, info_queue, card) local numerator, denominator = SMODS.get_probability_vars(card, 1, card.ability.extra.odds, self.key) local mult_numerator, mult_denominator = SMODS.get_probability_vars(card, 1, card.ability.extra.mult_odds, self.key) local dollars_numerator, dollars_denominator = SMODS.get_probability_vars(card, 1, card.ability.extra.dollars_odds, self.key) return { vars = { card.ability.bonus, card.ability.Xmult, numerator, denominator, card.ability.h_dollars, mult_numerator, dollars_numerator, card.ability.extra.mult, mult_denominator, card.ability.extra.dollars, dollars_denominator } } end, calculate = function(self, card, context)  if context.main_scoring and context.cardarea == G.play then local ret = {} if SMODS.pseudorandom_probability(card, 'vremade_lucky_mult', 1, card.ability.extra.mult_odds) then card.lucky_trigger = true ret.mult = card.ability.extra.mult end if SMODS.pseudorandom_probability(card, 'vremade_lucky_money', 1, card.ability.extra.dollars_odds) then card.lucky_trigger = true ret.dollars = card.ability.extra.dollars end return ret end  if context.destroy_card and context.cardarea == G.play and context.destroy_card == card and SMODS.pseudorandom_probability(card, self.key, 1, card.ability.extra.odds) then card.glass_trigger = true return { remove = true } end end }
SMODS.Enhancement { key = "multXwildXglassXstoneXgoldXlucky", name = "Mult Wild Glass Stone Gold Lucky Card", atlas = "enhancement", pos = { x = 14, y = 14 }, config = { mult = 4, Xmult = 2, bonus = 50, h_dollars = 3, extra = { odds = 4, mult = 20, dollars = 20, mult_odds = 5, dollars_odds = 15 } }, any_suit = true, shatters = true, replace_base_card = true,no_rank = true,always_scores = true, in_pool = function(self,args) return false end, loc_vars = function(self, info_queue, card) local numerator, denominator = SMODS.get_probability_vars(card, 1, card.ability.extra.odds, self.key) local mult_numerator, mult_denominator = SMODS.get_probability_vars(card, 1, card.ability.extra.mult_odds, self.key) local dollars_numerator, dollars_denominator = SMODS.get_probability_vars(card, 1, card.ability.extra.dollars_odds, self.key) return { vars = { card.ability.mult, card.ability.Xmult, numerator, denominator, card.ability.bonus, card.ability.h_dollars, mult_numerator, dollars_numerator, card.ability.extra.mult, mult_denominator, card.ability.extra.dollars, dollars_denominator } } end, calculate = function(self, card, context)  if context.main_scoring and context.cardarea == G.play then local ret = {} if SMODS.pseudorandom_probability(card, 'vremade_lucky_mult', 1, card.ability.extra.mult_odds) then card.lucky_trigger = true ret.mult = card.ability.extra.mult end if SMODS.pseudorandom_probability(card, 'vremade_lucky_money', 1, card.ability.extra.dollars_odds) then card.lucky_trigger = true ret.dollars = card.ability.extra.dollars end return ret end  if context.destroy_card and context.cardarea == G.play and context.destroy_card == card and SMODS.pseudorandom_probability(card, self.key, 1, card.ability.extra.odds) then card.glass_trigger = true return { remove = true } end end }
SMODS.Enhancement { key = "bonusXmultXwildXglassXstoneXgoldXlucky", name = "Bonus Mult Wild Glass Stone Gold Lucky Card", atlas = "enhancement", pos = { x = 14, y = 15 }, config = { bonus = 80, mult = 4, Xmult = 2, h_dollars = 3, extra = { odds = 4, mult = 20, dollars = 20, mult_odds = 5, dollars_odds = 15 } }, any_suit = true, shatters = true, replace_base_card = true,no_rank = true,always_scores = true, in_pool = function(self,args) return false end, loc_vars = function(self, info_queue, card) local numerator, denominator = SMODS.get_probability_vars(card, 1, card.ability.extra.odds, self.key) local mult_numerator, mult_denominator = SMODS.get_probability_vars(card, 1, card.ability.extra.mult_odds, self.key) local dollars_numerator, dollars_denominator = SMODS.get_probability_vars(card, 1, card.ability.extra.dollars_odds, self.key) return { vars = { card.ability.bonus, card.ability.mult, card.ability.Xmult, numerator, denominator, card.ability.h_dollars, mult_numerator, dollars_numerator, card.ability.extra.mult, mult_denominator, card.ability.extra.dollars, dollars_denominator } } end, calculate = function(self, card, context)  if context.main_scoring and context.cardarea == G.play then local ret = {} if SMODS.pseudorandom_probability(card, 'vremade_lucky_mult', 1, card.ability.extra.mult_odds) then card.lucky_trigger = true ret.mult = card.ability.extra.mult end if SMODS.pseudorandom_probability(card, 'vremade_lucky_money', 1, card.ability.extra.dollars_odds) then card.lucky_trigger = true ret.dollars = card.ability.extra.dollars end return ret end  if context.destroy_card and context.cardarea == G.play and context.destroy_card == card and SMODS.pseudorandom_probability(card, self.key, 1, card.ability.extra.odds) then card.glass_trigger = true return { remove = true } end end }
SMODS.Enhancement { key = "steelXstoneXgoldXlucky", name = "Steel Stone Gold Lucky Card", atlas = "enhancement", pos = { x = 15, y = 0 }, config = { h_x_mult = 1.5, bonus = 50, h_dollars = 3, extra = { mult = 20, dollars = 20, mult_odds = 5, dollars_odds = 15 } }, replace_base_card = true,no_rank = true,no_suit = true,always_scores = true, in_pool = function(self,args) return false end, loc_vars = function(self, info_queue, card) local mult_numerator, mult_denominator = SMODS.get_probability_vars(card, 1, card.ability.extra.mult_odds, self.key) local dollars_numerator, dollars_denominator = SMODS.get_probability_vars(card, 1, card.ability.extra.dollars_odds, self.key) return { vars = { card.ability.h_x_mult, card.ability.bonus, card.ability.h_dollars, mult_numerator, dollars_numerator, card.ability.extra.mult, mult_denominator, card.ability.extra.dollars, dollars_denominator } } end, calculate = function(self, card, context) if context.main_scoring and context.cardarea == G.play then local ret = {} if SMODS.pseudorandom_probability(card, 'vremade_lucky_mult', 1, card.ability.extra.mult_odds) then card.lucky_trigger = true ret.mult = card.ability.extra.mult end if SMODS.pseudorandom_probability(card, 'vremade_lucky_money', 1, card.ability.extra.dollars_odds) then card.lucky_trigger = true ret.dollars = card.ability.extra.dollars end return ret end end }
SMODS.Enhancement { key = "bonusXsteelXstoneXgoldXlucky", name = "Bonus Steel Stone Gold Lucky Card", atlas = "enhancement", pos = { x = 15, y = 1 }, config = { bonus = 80, h_x_mult = 1.5, h_dollars = 3, extra = { mult = 20, dollars = 20, mult_odds = 5, dollars_odds = 15 } }, replace_base_card = true,no_rank = true,no_suit = true,always_scores = true, in_pool = function(self,args) return false end, loc_vars = function(self, info_queue, card) local mult_numerator, mult_denominator = SMODS.get_probability_vars(card, 1, card.ability.extra.mult_odds, self.key) local dollars_numerator, dollars_denominator = SMODS.get_probability_vars(card, 1, card.ability.extra.dollars_odds, self.key) return { vars = { card.ability.bonus, card.ability.h_x_mult, card.ability.h_dollars, mult_numerator, dollars_numerator, card.ability.extra.mult, mult_denominator, card.ability.extra.dollars, dollars_denominator } } end, calculate = function(self, card, context) if context.main_scoring and context.cardarea == G.play then local ret = {} if SMODS.pseudorandom_probability(card, 'vremade_lucky_mult', 1, card.ability.extra.mult_odds) then card.lucky_trigger = true ret.mult = card.ability.extra.mult end if SMODS.pseudorandom_probability(card, 'vremade_lucky_money', 1, card.ability.extra.dollars_odds) then card.lucky_trigger = true ret.dollars = card.ability.extra.dollars end return ret end end }
SMODS.Enhancement { key = "multXsteelXstoneXgoldXlucky", name = "Mult Steel Stone Gold Lucky Card", atlas = "enhancement", pos = { x = 15, y = 2 }, config = { mult = 4, h_x_mult = 1.5, bonus = 50, h_dollars = 3, extra = { mult = 20, dollars = 20, mult_odds = 5, dollars_odds = 15 } }, replace_base_card = true,no_rank = true,no_suit = true,always_scores = true, in_pool = function(self,args) return false end, loc_vars = function(self, info_queue, card) local mult_numerator, mult_denominator = SMODS.get_probability_vars(card, 1, card.ability.extra.mult_odds, self.key) local dollars_numerator, dollars_denominator = SMODS.get_probability_vars(card, 1, card.ability.extra.dollars_odds, self.key) return { vars = { card.ability.mult, card.ability.h_x_mult, card.ability.bonus, card.ability.h_dollars, mult_numerator, dollars_numerator, card.ability.extra.mult, mult_denominator, card.ability.extra.dollars, dollars_denominator } } end, calculate = function(self, card, context) if context.main_scoring and context.cardarea == G.play then local ret = {} if SMODS.pseudorandom_probability(card, 'vremade_lucky_mult', 1, card.ability.extra.mult_odds) then card.lucky_trigger = true ret.mult = card.ability.extra.mult end if SMODS.pseudorandom_probability(card, 'vremade_lucky_money', 1, card.ability.extra.dollars_odds) then card.lucky_trigger = true ret.dollars = card.ability.extra.dollars end return ret end end }
SMODS.Enhancement { key = "bonusXmultXsteelXstoneXgoldXlucky", name = "Bonus Mult Steel Stone Gold Lucky Card", atlas = "enhancement", pos = { x = 15, y = 3 }, config = { bonus = 80, mult = 4, h_x_mult = 1.5, h_dollars = 3, extra = { mult = 20, dollars = 20, mult_odds = 5, dollars_odds = 15 } }, replace_base_card = true,no_rank = true,no_suit = true,always_scores = true, in_pool = function(self,args) return false end, loc_vars = function(self, info_queue, card) local mult_numerator, mult_denominator = SMODS.get_probability_vars(card, 1, card.ability.extra.mult_odds, self.key) local dollars_numerator, dollars_denominator = SMODS.get_probability_vars(card, 1, card.ability.extra.dollars_odds, self.key) return { vars = { card.ability.bonus, card.ability.mult, card.ability.h_x_mult, card.ability.h_dollars, mult_numerator, dollars_numerator, card.ability.extra.mult, mult_denominator, card.ability.extra.dollars, dollars_denominator } } end, calculate = function(self, card, context) if context.main_scoring and context.cardarea == G.play then local ret = {} if SMODS.pseudorandom_probability(card, 'vremade_lucky_mult', 1, card.ability.extra.mult_odds) then card.lucky_trigger = true ret.mult = card.ability.extra.mult end if SMODS.pseudorandom_probability(card, 'vremade_lucky_money', 1, card.ability.extra.dollars_odds) then card.lucky_trigger = true ret.dollars = card.ability.extra.dollars end return ret end end }
SMODS.Enhancement { key = "wildXsteelXstoneXgoldXlucky", name = "Wild Steel Stone Gold Lucky Card", atlas = "enhancement", pos = { x = 15, y = 4 }, config = { h_x_mult = 1.5, bonus = 50, h_dollars = 3, extra = { mult = 20, dollars = 20, mult_odds = 5, dollars_odds = 15 } }, any_suit = true, replace_base_card = true,no_rank = true,always_scores = true, in_pool = function(self,args) return false end, loc_vars = function(self, info_queue, card) local mult_numerator, mult_denominator = SMODS.get_probability_vars(card, 1, card.ability.extra.mult_odds, self.key) local dollars_numerator, dollars_denominator = SMODS.get_probability_vars(card, 1, card.ability.extra.dollars_odds, self.key) return { vars = { card.ability.h_x_mult, card.ability.bonus, card.ability.h_dollars, mult_numerator, dollars_numerator, card.ability.extra.mult, mult_denominator, card.ability.extra.dollars, dollars_denominator } } end, calculate = function(self, card, context) if context.main_scoring and context.cardarea == G.play then local ret = {} if SMODS.pseudorandom_probability(card, 'vremade_lucky_mult', 1, card.ability.extra.mult_odds) then card.lucky_trigger = true ret.mult = card.ability.extra.mult end if SMODS.pseudorandom_probability(card, 'vremade_lucky_money', 1, card.ability.extra.dollars_odds) then card.lucky_trigger = true ret.dollars = card.ability.extra.dollars end return ret end end }
SMODS.Enhancement { key = "bonusXwildXsteelXstoneXgoldXlucky", name = "Bonus Wild Steel Stone Gold Lucky Card", atlas = "enhancement", pos = { x = 15, y = 5 }, config = { bonus = 80, h_x_mult = 1.5, h_dollars = 3, extra = { mult = 20, dollars = 20, mult_odds = 5, dollars_odds = 15 } }, any_suit = true, replace_base_card = true,no_rank = true,always_scores = true, in_pool = function(self,args) return false end, loc_vars = function(self, info_queue, card) local mult_numerator, mult_denominator = SMODS.get_probability_vars(card, 1, card.ability.extra.mult_odds, self.key) local dollars_numerator, dollars_denominator = SMODS.get_probability_vars(card, 1, card.ability.extra.dollars_odds, self.key) return { vars = { card.ability.bonus, card.ability.h_x_mult, card.ability.h_dollars, mult_numerator, dollars_numerator, card.ability.extra.mult, mult_denominator, card.ability.extra.dollars, dollars_denominator } } end, calculate = function(self, card, context) if context.main_scoring and context.cardarea == G.play then local ret = {} if SMODS.pseudorandom_probability(card, 'vremade_lucky_mult', 1, card.ability.extra.mult_odds) then card.lucky_trigger = true ret.mult = card.ability.extra.mult end if SMODS.pseudorandom_probability(card, 'vremade_lucky_money', 1, card.ability.extra.dollars_odds) then card.lucky_trigger = true ret.dollars = card.ability.extra.dollars end return ret end end }
SMODS.Enhancement { key = "multXwildXsteelXstoneXgoldXlucky", name = "Mult Wild Steel Stone Gold Lucky Card", atlas = "enhancement", pos = { x = 15, y = 6 }, config = { mult = 4, h_x_mult = 1.5, bonus = 50, h_dollars = 3, extra = { mult = 20, dollars = 20, mult_odds = 5, dollars_odds = 15 } }, any_suit = true, replace_base_card = true,no_rank = true,always_scores = true, in_pool = function(self,args) return false end, loc_vars = function(self, info_queue, card) local mult_numerator, mult_denominator = SMODS.get_probability_vars(card, 1, card.ability.extra.mult_odds, self.key) local dollars_numerator, dollars_denominator = SMODS.get_probability_vars(card, 1, card.ability.extra.dollars_odds, self.key) return { vars = { card.ability.mult, card.ability.h_x_mult, card.ability.bonus, card.ability.h_dollars, mult_numerator, dollars_numerator, card.ability.extra.mult, mult_denominator, card.ability.extra.dollars, dollars_denominator } } end, calculate = function(self, card, context) if context.main_scoring and context.cardarea == G.play then local ret = {} if SMODS.pseudorandom_probability(card, 'vremade_lucky_mult', 1, card.ability.extra.mult_odds) then card.lucky_trigger = true ret.mult = card.ability.extra.mult end if SMODS.pseudorandom_probability(card, 'vremade_lucky_money', 1, card.ability.extra.dollars_odds) then card.lucky_trigger = true ret.dollars = card.ability.extra.dollars end return ret end end }
SMODS.Enhancement { key = "bonusXmultXwildXsteelXstoneXgoldXlucky", name = "Bonus Mult Wild Steel Stone Gold Lucky Card", atlas = "enhancement", pos = { x = 15, y = 7 }, config = { bonus = 80, mult = 4, h_x_mult = 1.5, h_dollars = 3, extra = { mult = 20, dollars = 20, mult_odds = 5, dollars_odds = 15 } }, any_suit = true, replace_base_card = true,no_rank = true,always_scores = true, in_pool = function(self,args) return false end, loc_vars = function(self, info_queue, card) local mult_numerator, mult_denominator = SMODS.get_probability_vars(card, 1, card.ability.extra.mult_odds, self.key) local dollars_numerator, dollars_denominator = SMODS.get_probability_vars(card, 1, card.ability.extra.dollars_odds, self.key) return { vars = { card.ability.bonus, card.ability.mult, card.ability.h_x_mult, card.ability.h_dollars, mult_numerator, dollars_numerator, card.ability.extra.mult, mult_denominator, card.ability.extra.dollars, dollars_denominator } } end, calculate = function(self, card, context) if context.main_scoring and context.cardarea == G.play then local ret = {} if SMODS.pseudorandom_probability(card, 'vremade_lucky_mult', 1, card.ability.extra.mult_odds) then card.lucky_trigger = true ret.mult = card.ability.extra.mult end if SMODS.pseudorandom_probability(card, 'vremade_lucky_money', 1, card.ability.extra.dollars_odds) then card.lucky_trigger = true ret.dollars = card.ability.extra.dollars end return ret end end }
SMODS.Enhancement { key = "glassXsteelXstoneXgoldXlucky", name = "Glass Steel Stone Gold Lucky Card", atlas = "enhancement", pos = { x = 15, y = 8 }, config = { Xmult = 2, h_x_mult = 1.5, bonus = 50, h_dollars = 3, extra = { odds = 4, mult = 20, dollars = 20, mult_odds = 5, dollars_odds = 15 } }, shatters = true, replace_base_card = true,no_rank = true,no_suit = true,always_scores = true, in_pool = function(self,args) return false end, loc_vars = function(self, info_queue, card) local numerator, denominator = SMODS.get_probability_vars(card, 1, card.ability.extra.odds, self.key) local mult_numerator, mult_denominator = SMODS.get_probability_vars(card, 1, card.ability.extra.mult_odds, self.key) local dollars_numerator, dollars_denominator = SMODS.get_probability_vars(card, 1, card.ability.extra.dollars_odds, self.key) return { vars = { card.ability.Xmult, numerator, denominator, card.ability.h_x_mult, card.ability.bonus, card.ability.h_dollars, mult_numerator, dollars_numerator, card.ability.extra.mult, mult_denominator, card.ability.extra.dollars, dollars_denominator } } end, calculate = function(self, card, context)  if context.main_scoring and context.cardarea == G.play then local ret = {} if SMODS.pseudorandom_probability(card, 'vremade_lucky_mult', 1, card.ability.extra.mult_odds) then card.lucky_trigger = true ret.mult = card.ability.extra.mult end if SMODS.pseudorandom_probability(card, 'vremade_lucky_money', 1, card.ability.extra.dollars_odds) then card.lucky_trigger = true ret.dollars = card.ability.extra.dollars end return ret end  if context.destroy_card and context.cardarea == G.play and context.destroy_card == card and SMODS.pseudorandom_probability(card, self.key, 1, card.ability.extra.odds) then card.glass_trigger = true return { remove = true } end end }
SMODS.Enhancement { key = "bonusXglassXsteelXstoneXgoldXlucky", name = "Bonus Glass Steel Stone Gold Lucky Card", atlas = "enhancement", pos = { x = 15, y = 9 }, config = { bonus = 80, Xmult = 2, h_x_mult = 1.5, h_dollars = 3, extra = { odds = 4, mult = 20, dollars = 20, mult_odds = 5, dollars_odds = 15 } }, shatters = true, replace_base_card = true,no_rank = true,no_suit = true,always_scores = true, in_pool = function(self,args) return false end, loc_vars = function(self, info_queue, card) local numerator, denominator = SMODS.get_probability_vars(card, 1, card.ability.extra.odds, self.key) local mult_numerator, mult_denominator = SMODS.get_probability_vars(card, 1, card.ability.extra.mult_odds, self.key) local dollars_numerator, dollars_denominator = SMODS.get_probability_vars(card, 1, card.ability.extra.dollars_odds, self.key) return { vars = { card.ability.bonus, card.ability.Xmult, numerator, denominator, card.ability.h_x_mult, card.ability.h_dollars, mult_numerator, dollars_numerator, card.ability.extra.mult, mult_denominator, card.ability.extra.dollars, dollars_denominator } } end, calculate = function(self, card, context)  if context.main_scoring and context.cardarea == G.play then local ret = {} if SMODS.pseudorandom_probability(card, 'vremade_lucky_mult', 1, card.ability.extra.mult_odds) then card.lucky_trigger = true ret.mult = card.ability.extra.mult end if SMODS.pseudorandom_probability(card, 'vremade_lucky_money', 1, card.ability.extra.dollars_odds) then card.lucky_trigger = true ret.dollars = card.ability.extra.dollars end return ret end  if context.destroy_card and context.cardarea == G.play and context.destroy_card == card and SMODS.pseudorandom_probability(card, self.key, 1, card.ability.extra.odds) then card.glass_trigger = true return { remove = true } end end }
SMODS.Enhancement { key = "multXglassXsteelXstoneXgoldXlucky", name = "Mult Glass Steel Stone Gold Lucky Card", atlas = "enhancement", pos = { x = 15, y = 10 }, config = { mult = 4, Xmult = 2, h_x_mult = 1.5, bonus = 50, h_dollars = 3, extra = { odds = 4, mult = 20, dollars = 20, mult_odds = 5, dollars_odds = 15 } }, shatters = true, replace_base_card = true,no_rank = true,no_suit = true,always_scores = true, in_pool = function(self,args) return false end, loc_vars = function(self, info_queue, card) local numerator, denominator = SMODS.get_probability_vars(card, 1, card.ability.extra.odds, self.key) local mult_numerator, mult_denominator = SMODS.get_probability_vars(card, 1, card.ability.extra.mult_odds, self.key) local dollars_numerator, dollars_denominator = SMODS.get_probability_vars(card, 1, card.ability.extra.dollars_odds, self.key) return { vars = { card.ability.mult, card.ability.Xmult, numerator, denominator, card.ability.h_x_mult, card.ability.bonus, card.ability.h_dollars, mult_numerator, dollars_numerator, card.ability.extra.mult, mult_denominator, card.ability.extra.dollars, dollars_denominator } } end, calculate = function(self, card, context)  if context.main_scoring and context.cardarea == G.play then local ret = {} if SMODS.pseudorandom_probability(card, 'vremade_lucky_mult', 1, card.ability.extra.mult_odds) then card.lucky_trigger = true ret.mult = card.ability.extra.mult end if SMODS.pseudorandom_probability(card, 'vremade_lucky_money', 1, card.ability.extra.dollars_odds) then card.lucky_trigger = true ret.dollars = card.ability.extra.dollars end return ret end  if context.destroy_card and context.cardarea == G.play and context.destroy_card == card and SMODS.pseudorandom_probability(card, self.key, 1, card.ability.extra.odds) then card.glass_trigger = true return { remove = true } end end }
SMODS.Enhancement { key = "bonusXmultXglassXsteelXstoneXgoldXlucky", name = "Bonus Mult Glass Steel Stone Gold Lucky Card", atlas = "enhancement", pos = { x = 15, y = 11 }, config = { bonus = 80, mult = 4, Xmult = 2, h_x_mult = 1.5, h_dollars = 3, extra = { odds = 4, mult = 20, dollars = 20, mult_odds = 5, dollars_odds = 15 } }, shatters = true, replace_base_card = true,no_rank = true,no_suit = true,always_scores = true, in_pool = function(self,args) return false end, loc_vars = function(self, info_queue, card) local numerator, denominator = SMODS.get_probability_vars(card, 1, card.ability.extra.odds, self.key) local mult_numerator, mult_denominator = SMODS.get_probability_vars(card, 1, card.ability.extra.mult_odds, self.key) local dollars_numerator, dollars_denominator = SMODS.get_probability_vars(card, 1, card.ability.extra.dollars_odds, self.key) return { vars = { card.ability.bonus, card.ability.mult, card.ability.Xmult, numerator, denominator, card.ability.h_x_mult, card.ability.h_dollars, mult_numerator, dollars_numerator, card.ability.extra.mult, mult_denominator, card.ability.extra.dollars, dollars_denominator } } end, calculate = function(self, card, context)  if context.main_scoring and context.cardarea == G.play then local ret = {} if SMODS.pseudorandom_probability(card, 'vremade_lucky_mult', 1, card.ability.extra.mult_odds) then card.lucky_trigger = true ret.mult = card.ability.extra.mult end if SMODS.pseudorandom_probability(card, 'vremade_lucky_money', 1, card.ability.extra.dollars_odds) then card.lucky_trigger = true ret.dollars = card.ability.extra.dollars end return ret end  if context.destroy_card and context.cardarea == G.play and context.destroy_card == card and SMODS.pseudorandom_probability(card, self.key, 1, card.ability.extra.odds) then card.glass_trigger = true return { remove = true } end end }
SMODS.Enhancement { key = "wildXglassXsteelXstoneXgoldXlucky", name = "Wild Glass Steel Stone Gold Lucky Card", atlas = "enhancement", pos = { x = 15, y = 12 }, config = { Xmult = 2, h_x_mult = 1.5, bonus = 50, h_dollars = 3, extra = { odds = 4, mult = 20, dollars = 20, mult_odds = 5, dollars_odds = 15 } }, any_suit = true, shatters = true, replace_base_card = true,no_rank = true,always_scores = true, in_pool = function(self,args) return false end, loc_vars = function(self, info_queue, card) local numerator, denominator = SMODS.get_probability_vars(card, 1, card.ability.extra.odds, self.key) local mult_numerator, mult_denominator = SMODS.get_probability_vars(card, 1, card.ability.extra.mult_odds, self.key) local dollars_numerator, dollars_denominator = SMODS.get_probability_vars(card, 1, card.ability.extra.dollars_odds, self.key) return { vars = { card.ability.Xmult, numerator, denominator, card.ability.h_x_mult, card.ability.bonus, card.ability.h_dollars, mult_numerator, dollars_numerator, card.ability.extra.mult, mult_denominator, card.ability.extra.dollars, dollars_denominator } } end, calculate = function(self, card, context)  if context.main_scoring and context.cardarea == G.play then local ret = {} if SMODS.pseudorandom_probability(card, 'vremade_lucky_mult', 1, card.ability.extra.mult_odds) then card.lucky_trigger = true ret.mult = card.ability.extra.mult end if SMODS.pseudorandom_probability(card, 'vremade_lucky_money', 1, card.ability.extra.dollars_odds) then card.lucky_trigger = true ret.dollars = card.ability.extra.dollars end return ret end  if context.destroy_card and context.cardarea == G.play and context.destroy_card == card and SMODS.pseudorandom_probability(card, self.key, 1, card.ability.extra.odds) then card.glass_trigger = true return { remove = true } end end }
SMODS.Enhancement { key = "bonusXwildXglassXsteelXstoneXgoldXlucky", name = "Bonus Wild Glass Steel Stone Gold Lucky Card", atlas = "enhancement", pos = { x = 15, y = 13 }, config = { bonus = 80, Xmult = 2, h_x_mult = 1.5, h_dollars = 3, extra = { odds = 4, mult = 20, dollars = 20, mult_odds = 5, dollars_odds = 15 } }, any_suit = true, shatters = true, replace_base_card = true,no_rank = true,always_scores = true, in_pool = function(self,args) return false end, loc_vars = function(self, info_queue, card) local numerator, denominator = SMODS.get_probability_vars(card, 1, card.ability.extra.odds, self.key) local mult_numerator, mult_denominator = SMODS.get_probability_vars(card, 1, card.ability.extra.mult_odds, self.key) local dollars_numerator, dollars_denominator = SMODS.get_probability_vars(card, 1, card.ability.extra.dollars_odds, self.key) return { vars = { card.ability.bonus, card.ability.Xmult, numerator, denominator, card.ability.h_x_mult, card.ability.h_dollars, mult_numerator, dollars_numerator, card.ability.extra.mult, mult_denominator, card.ability.extra.dollars, dollars_denominator } } end, calculate = function(self, card, context)  if context.main_scoring and context.cardarea == G.play then local ret = {} if SMODS.pseudorandom_probability(card, 'vremade_lucky_mult', 1, card.ability.extra.mult_odds) then card.lucky_trigger = true ret.mult = card.ability.extra.mult end if SMODS.pseudorandom_probability(card, 'vremade_lucky_money', 1, card.ability.extra.dollars_odds) then card.lucky_trigger = true ret.dollars = card.ability.extra.dollars end return ret end  if context.destroy_card and context.cardarea == G.play and context.destroy_card == card and SMODS.pseudorandom_probability(card, self.key, 1, card.ability.extra.odds) then card.glass_trigger = true return { remove = true } end end }
SMODS.Enhancement { key = "multXwildXglassXsteelXstoneXgoldXlucky", name = "Mult Wild Glass Steel Stone Gold Lucky Card", atlas = "enhancement", pos = { x = 15, y = 14 }, config = { mult = 4, Xmult = 2, h_x_mult = 1.5, bonus = 50, h_dollars = 3, extra = { odds = 4, mult = 20, dollars = 20, mult_odds = 5, dollars_odds = 15 } }, any_suit = true, shatters = true, replace_base_card = true,no_rank = true,always_scores = true, in_pool = function(self,args) return false end, loc_vars = function(self, info_queue, card) local numerator, denominator = SMODS.get_probability_vars(card, 1, card.ability.extra.odds, self.key) local mult_numerator, mult_denominator = SMODS.get_probability_vars(card, 1, card.ability.extra.mult_odds, self.key) local dollars_numerator, dollars_denominator = SMODS.get_probability_vars(card, 1, card.ability.extra.dollars_odds, self.key) return { vars = { card.ability.mult, card.ability.Xmult, numerator, denominator, card.ability.h_x_mult, card.ability.bonus, card.ability.h_dollars, mult_numerator, dollars_numerator, card.ability.extra.mult, mult_denominator, card.ability.extra.dollars, dollars_denominator } } end, calculate = function(self, card, context)  if context.main_scoring and context.cardarea == G.play then local ret = {} if SMODS.pseudorandom_probability(card, 'vremade_lucky_mult', 1, card.ability.extra.mult_odds) then card.lucky_trigger = true ret.mult = card.ability.extra.mult end if SMODS.pseudorandom_probability(card, 'vremade_lucky_money', 1, card.ability.extra.dollars_odds) then card.lucky_trigger = true ret.dollars = card.ability.extra.dollars end return ret end  if context.destroy_card and context.cardarea == G.play and context.destroy_card == card and SMODS.pseudorandom_probability(card, self.key, 1, card.ability.extra.odds) then card.glass_trigger = true return { remove = true } end end }
SMODS.Enhancement { key = "bonusXmultXwildXglassXsteelXstoneXgoldXlucky", name = "Bonus Mult Wild Glass Steel Stone Gold Lucky Card", atlas = "enhancement", pos = { x = 15, y = 15 }, config = { bonus = 80, mult = 4, Xmult = 2, h_x_mult = 1.5, h_dollars = 3, extra = { odds = 4, mult = 20, dollars = 20, mult_odds = 5, dollars_odds = 15 } }, any_suit = true, shatters = true, replace_base_card = true,no_rank = true,always_scores = true, in_pool = function(self,args) return false end, loc_vars = function(self, info_queue, card) local numerator, denominator = SMODS.get_probability_vars(card, 1, card.ability.extra.odds, self.key) local mult_numerator, mult_denominator = SMODS.get_probability_vars(card, 1, card.ability.extra.mult_odds, self.key) local dollars_numerator, dollars_denominator = SMODS.get_probability_vars(card, 1, card.ability.extra.dollars_odds, self.key) return { vars = { card.ability.bonus, card.ability.mult, card.ability.Xmult, numerator, denominator, card.ability.h_x_mult, card.ability.h_dollars, mult_numerator, dollars_numerator, card.ability.extra.mult, mult_denominator, card.ability.extra.dollars, dollars_denominator } } end, calculate = function(self, card, context)  if context.main_scoring and context.cardarea == G.play then local ret = {} if SMODS.pseudorandom_probability(card, 'vremade_lucky_mult', 1, card.ability.extra.mult_odds) then card.lucky_trigger = true ret.mult = card.ability.extra.mult end if SMODS.pseudorandom_probability(card, 'vremade_lucky_money', 1, card.ability.extra.dollars_odds) then card.lucky_trigger = true ret.dollars = card.ability.extra.dollars end return ret end  if context.destroy_card and context.cardarea == G.play and context.destroy_card == card and SMODS.pseudorandom_probability(card, self.key, 1, card.ability.extra.odds) then card.glass_trigger = true return { remove = true } end end }
